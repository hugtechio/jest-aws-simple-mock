
/**
* @description mocks_v2:mockDataPipeline.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockDataPipeline = {
  activatePipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activatePipeline', 'DataPipeline', result, true, true, mock)
  },
  activatePipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activatePipeline', 'DataPipeline', result, false, true, mock)
  },
  activatePipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activatePipeline', 'DataPipeline', result, true, false, mock)
  },
  addTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTags', 'DataPipeline', result, true, true, mock)
  },
  addTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTags', 'DataPipeline', result, false, true, mock)
  },
  addTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTags', 'DataPipeline', result, true, false, mock)
  },
  createPipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPipeline', 'DataPipeline', result, true, true, mock)
  },
  createPipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPipeline', 'DataPipeline', result, false, true, mock)
  },
  createPipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPipeline', 'DataPipeline', result, true, false, mock)
  },
  deactivatePipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deactivatePipeline', 'DataPipeline', result, true, true, mock)
  },
  deactivatePipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deactivatePipeline', 'DataPipeline', result, false, true, mock)
  },
  deactivatePipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deactivatePipeline', 'DataPipeline', result, true, false, mock)
  },
  deletePipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePipeline', 'DataPipeline', result, true, true, mock)
  },
  deletePipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePipeline', 'DataPipeline', result, false, true, mock)
  },
  deletePipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePipeline', 'DataPipeline', result, true, false, mock)
  },
  describeObjects: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeObjects', 'DataPipeline', result, true, true, mock)
  },
  describeObjectsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeObjects', 'DataPipeline', result, false, true, mock)
  },
  describeObjectsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeObjects', 'DataPipeline', result, true, false, mock)
  },
  describePipelines: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePipelines', 'DataPipeline', result, true, true, mock)
  },
  describePipelinesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePipelines', 'DataPipeline', result, false, true, mock)
  },
  describePipelinesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePipelines', 'DataPipeline', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataPipeline', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataPipeline', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataPipeline', result, true, false, mock)
  },
  evaluateExpression: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('evaluateExpression', 'DataPipeline', result, true, true, mock)
  },
  evaluateExpressionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('evaluateExpression', 'DataPipeline', result, false, true, mock)
  },
  evaluateExpressionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('evaluateExpression', 'DataPipeline', result, true, false, mock)
  },
  getPipelineDefinition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineDefinition', 'DataPipeline', result, true, true, mock)
  },
  getPipelineDefinitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineDefinition', 'DataPipeline', result, false, true, mock)
  },
  getPipelineDefinitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineDefinition', 'DataPipeline', result, true, false, mock)
  },
  listPipelines: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelines', 'DataPipeline', result, true, true, mock)
  },
  listPipelinesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelines', 'DataPipeline', result, false, true, mock)
  },
  listPipelinesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelines', 'DataPipeline', result, true, false, mock)
  },
  pollForTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForTask', 'DataPipeline', result, true, true, mock)
  },
  pollForTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForTask', 'DataPipeline', result, false, true, mock)
  },
  pollForTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForTask', 'DataPipeline', result, true, false, mock)
  },
  putPipelineDefinition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPipelineDefinition', 'DataPipeline', result, true, true, mock)
  },
  putPipelineDefinitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPipelineDefinition', 'DataPipeline', result, false, true, mock)
  },
  putPipelineDefinitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPipelineDefinition', 'DataPipeline', result, true, false, mock)
  },
  queryObjects: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('queryObjects', 'DataPipeline', result, true, true, mock)
  },
  queryObjectsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('queryObjects', 'DataPipeline', result, false, true, mock)
  },
  queryObjectsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('queryObjects', 'DataPipeline', result, true, false, mock)
  },
  removeTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTags', 'DataPipeline', result, true, true, mock)
  },
  removeTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTags', 'DataPipeline', result, false, true, mock)
  },
  removeTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTags', 'DataPipeline', result, true, false, mock)
  },
  reportTaskProgress: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reportTaskProgress', 'DataPipeline', result, true, true, mock)
  },
  reportTaskProgressAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reportTaskProgress', 'DataPipeline', result, false, true, mock)
  },
  reportTaskProgressThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reportTaskProgress', 'DataPipeline', result, true, false, mock)
  },
  reportTaskRunnerHeartbeat: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reportTaskRunnerHeartbeat', 'DataPipeline', result, true, true, mock)
  },
  reportTaskRunnerHeartbeatAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reportTaskRunnerHeartbeat', 'DataPipeline', result, false, true, mock)
  },
  reportTaskRunnerHeartbeatThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reportTaskRunnerHeartbeat', 'DataPipeline', result, true, false, mock)
  },
  setStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setStatus', 'DataPipeline', result, true, true, mock)
  },
  setStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setStatus', 'DataPipeline', result, false, true, mock)
  },
  setStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setStatus', 'DataPipeline', result, true, false, mock)
  },
  setTaskStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setTaskStatus', 'DataPipeline', result, true, true, mock)
  },
  setTaskStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setTaskStatus', 'DataPipeline', result, false, true, mock)
  },
  setTaskStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setTaskStatus', 'DataPipeline', result, true, false, mock)
  },
  validatePipelineDefinition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('validatePipelineDefinition', 'DataPipeline', result, true, true, mock)
  },
  validatePipelineDefinitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('validatePipelineDefinition', 'DataPipeline', result, false, true, mock)
  },
  validatePipelineDefinitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('validatePipelineDefinition', 'DataPipeline', result, true, false, mock)
  },
}
