
/**
* @description mocks_v2:mockFSx.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockFSx = {
  associateFileSystemAliases: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateFileSystemAliases', 'FSx', result, true, true, mock)
  },
  associateFileSystemAliasesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateFileSystemAliases', 'FSx', result, false, true, mock)
  },
  associateFileSystemAliasesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateFileSystemAliases', 'FSx', result, true, false, mock)
  },
  cancelDataRepositoryTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelDataRepositoryTask', 'FSx', result, true, true, mock)
  },
  cancelDataRepositoryTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelDataRepositoryTask', 'FSx', result, false, true, mock)
  },
  cancelDataRepositoryTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelDataRepositoryTask', 'FSx', result, true, false, mock)
  },
  copyBackup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('copyBackup', 'FSx', result, true, true, mock)
  },
  copyBackupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('copyBackup', 'FSx', result, false, true, mock)
  },
  copyBackupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('copyBackup', 'FSx', result, true, false, mock)
  },
  createBackup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackup', 'FSx', result, true, true, mock)
  },
  createBackupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackup', 'FSx', result, false, true, mock)
  },
  createBackupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackup', 'FSx', result, true, false, mock)
  },
  createDataRepositoryTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataRepositoryTask', 'FSx', result, true, true, mock)
  },
  createDataRepositoryTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataRepositoryTask', 'FSx', result, false, true, mock)
  },
  createDataRepositoryTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataRepositoryTask', 'FSx', result, true, false, mock)
  },
  createFileSystem: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFileSystem', 'FSx', result, true, true, mock)
  },
  createFileSystemAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFileSystem', 'FSx', result, false, true, mock)
  },
  createFileSystemThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFileSystem', 'FSx', result, true, false, mock)
  },
  createFileSystemFromBackup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFileSystemFromBackup', 'FSx', result, true, true, mock)
  },
  createFileSystemFromBackupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFileSystemFromBackup', 'FSx', result, false, true, mock)
  },
  createFileSystemFromBackupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFileSystemFromBackup', 'FSx', result, true, false, mock)
  },
  createStorageVirtualMachine: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createStorageVirtualMachine', 'FSx', result, true, true, mock)
  },
  createStorageVirtualMachineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createStorageVirtualMachine', 'FSx', result, false, true, mock)
  },
  createStorageVirtualMachineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createStorageVirtualMachine', 'FSx', result, true, false, mock)
  },
  createVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVolume', 'FSx', result, true, true, mock)
  },
  createVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVolume', 'FSx', result, false, true, mock)
  },
  createVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVolume', 'FSx', result, true, false, mock)
  },
  createVolumeFromBackup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVolumeFromBackup', 'FSx', result, true, true, mock)
  },
  createVolumeFromBackupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVolumeFromBackup', 'FSx', result, false, true, mock)
  },
  createVolumeFromBackupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVolumeFromBackup', 'FSx', result, true, false, mock)
  },
  deleteBackup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackup', 'FSx', result, true, true, mock)
  },
  deleteBackupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackup', 'FSx', result, false, true, mock)
  },
  deleteBackupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackup', 'FSx', result, true, false, mock)
  },
  deleteFileSystem: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFileSystem', 'FSx', result, true, true, mock)
  },
  deleteFileSystemAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFileSystem', 'FSx', result, false, true, mock)
  },
  deleteFileSystemThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFileSystem', 'FSx', result, true, false, mock)
  },
  deleteStorageVirtualMachine: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteStorageVirtualMachine', 'FSx', result, true, true, mock)
  },
  deleteStorageVirtualMachineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteStorageVirtualMachine', 'FSx', result, false, true, mock)
  },
  deleteStorageVirtualMachineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteStorageVirtualMachine', 'FSx', result, true, false, mock)
  },
  deleteVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVolume', 'FSx', result, true, true, mock)
  },
  deleteVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVolume', 'FSx', result, false, true, mock)
  },
  deleteVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVolume', 'FSx', result, true, false, mock)
  },
  describeBackups: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBackups', 'FSx', result, true, true, mock)
  },
  describeBackupsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBackups', 'FSx', result, false, true, mock)
  },
  describeBackupsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBackups', 'FSx', result, true, false, mock)
  },
  describeDataRepositoryTasks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataRepositoryTasks', 'FSx', result, true, true, mock)
  },
  describeDataRepositoryTasksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataRepositoryTasks', 'FSx', result, false, true, mock)
  },
  describeDataRepositoryTasksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataRepositoryTasks', 'FSx', result, true, false, mock)
  },
  describeFileSystemAliases: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystemAliases', 'FSx', result, true, true, mock)
  },
  describeFileSystemAliasesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystemAliases', 'FSx', result, false, true, mock)
  },
  describeFileSystemAliasesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystemAliases', 'FSx', result, true, false, mock)
  },
  describeFileSystems: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystems', 'FSx', result, true, true, mock)
  },
  describeFileSystemsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystems', 'FSx', result, false, true, mock)
  },
  describeFileSystemsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystems', 'FSx', result, true, false, mock)
  },
  describeStorageVirtualMachines: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeStorageVirtualMachines', 'FSx', result, true, true, mock)
  },
  describeStorageVirtualMachinesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeStorageVirtualMachines', 'FSx', result, false, true, mock)
  },
  describeStorageVirtualMachinesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeStorageVirtualMachines', 'FSx', result, true, false, mock)
  },
  describeVolumes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVolumes', 'FSx', result, true, true, mock)
  },
  describeVolumesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVolumes', 'FSx', result, false, true, mock)
  },
  describeVolumesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVolumes', 'FSx', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'FSx', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'FSx', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'FSx', result, true, false, mock)
  },
  disassociateFileSystemAliases: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateFileSystemAliases', 'FSx', result, true, true, mock)
  },
  disassociateFileSystemAliasesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateFileSystemAliases', 'FSx', result, false, true, mock)
  },
  disassociateFileSystemAliasesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateFileSystemAliases', 'FSx', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'FSx', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'FSx', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'FSx', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'FSx', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'FSx', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'FSx', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'FSx', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'FSx', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'FSx', result, true, false, mock)
  },
  updateFileSystem: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFileSystem', 'FSx', result, true, true, mock)
  },
  updateFileSystemAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFileSystem', 'FSx', result, false, true, mock)
  },
  updateFileSystemThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFileSystem', 'FSx', result, true, false, mock)
  },
  updateStorageVirtualMachine: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateStorageVirtualMachine', 'FSx', result, true, true, mock)
  },
  updateStorageVirtualMachineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateStorageVirtualMachine', 'FSx', result, false, true, mock)
  },
  updateStorageVirtualMachineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateStorageVirtualMachine', 'FSx', result, true, false, mock)
  },
  updateVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVolume', 'FSx', result, true, true, mock)
  },
  updateVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVolume', 'FSx', result, false, true, mock)
  },
  updateVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVolume', 'FSx', result, true, false, mock)
  },
}
