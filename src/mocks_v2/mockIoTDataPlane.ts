
/**
* @description mocks_v2:mockIoTDataPlane.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockIoTDataPlane = {
  deleteThingShadow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteThingShadow', 'IoTDataPlane', result, true, true, mock)
  },
  deleteThingShadowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteThingShadow', 'IoTDataPlane', result, false, true, mock)
  },
  deleteThingShadowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteThingShadow', 'IoTDataPlane', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoTDataPlane', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoTDataPlane', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoTDataPlane', result, true, false, mock)
  },
  getRetainedMessage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getRetainedMessage', 'IoTDataPlane', result, true, true, mock)
  },
  getRetainedMessageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getRetainedMessage', 'IoTDataPlane', result, false, true, mock)
  },
  getRetainedMessageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getRetainedMessage', 'IoTDataPlane', result, true, false, mock)
  },
  getThingShadow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getThingShadow', 'IoTDataPlane', result, true, true, mock)
  },
  getThingShadowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getThingShadow', 'IoTDataPlane', result, false, true, mock)
  },
  getThingShadowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getThingShadow', 'IoTDataPlane', result, true, false, mock)
  },
  listNamedShadowsForThing: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNamedShadowsForThing', 'IoTDataPlane', result, true, true, mock)
  },
  listNamedShadowsForThingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNamedShadowsForThing', 'IoTDataPlane', result, false, true, mock)
  },
  listNamedShadowsForThingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNamedShadowsForThing', 'IoTDataPlane', result, true, false, mock)
  },
  listRetainedMessages: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRetainedMessages', 'IoTDataPlane', result, true, true, mock)
  },
  listRetainedMessagesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRetainedMessages', 'IoTDataPlane', result, false, true, mock)
  },
  listRetainedMessagesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRetainedMessages', 'IoTDataPlane', result, true, false, mock)
  },
  publish: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('publish', 'IoTDataPlane', result, true, true, mock)
  },
  publishAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('publish', 'IoTDataPlane', result, false, true, mock)
  },
  publishThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('publish', 'IoTDataPlane', result, true, false, mock)
  },
  updateThingShadow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateThingShadow', 'IoTDataPlane', result, true, true, mock)
  },
  updateThingShadowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateThingShadow', 'IoTDataPlane', result, false, true, mock)
  },
  updateThingShadowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateThingShadow', 'IoTDataPlane', result, true, false, mock)
  },
}
