
/**
* @description mocks_v2:mockWellArchitected.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockWellArchitected = {
  associateLenses: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateLenses', 'WellArchitected', result, true, true, mock)
  },
  associateLensesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateLenses', 'WellArchitected', result, false, true, mock)
  },
  associateLensesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateLenses', 'WellArchitected', result, true, false, mock)
  },
  createMilestone: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMilestone', 'WellArchitected', result, true, true, mock)
  },
  createMilestoneAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMilestone', 'WellArchitected', result, false, true, mock)
  },
  createMilestoneThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMilestone', 'WellArchitected', result, true, false, mock)
  },
  createWorkload: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkload', 'WellArchitected', result, true, true, mock)
  },
  createWorkloadAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkload', 'WellArchitected', result, false, true, mock)
  },
  createWorkloadThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkload', 'WellArchitected', result, true, false, mock)
  },
  createWorkloadShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkloadShare', 'WellArchitected', result, true, true, mock)
  },
  createWorkloadShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkloadShare', 'WellArchitected', result, false, true, mock)
  },
  createWorkloadShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkloadShare', 'WellArchitected', result, true, false, mock)
  },
  deleteWorkload: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkload', 'WellArchitected', result, true, true, mock)
  },
  deleteWorkloadAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkload', 'WellArchitected', result, false, true, mock)
  },
  deleteWorkloadThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkload', 'WellArchitected', result, true, false, mock)
  },
  deleteWorkloadShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkloadShare', 'WellArchitected', result, true, true, mock)
  },
  deleteWorkloadShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkloadShare', 'WellArchitected', result, false, true, mock)
  },
  deleteWorkloadShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkloadShare', 'WellArchitected', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'WellArchitected', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'WellArchitected', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'WellArchitected', result, true, false, mock)
  },
  disassociateLenses: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateLenses', 'WellArchitected', result, true, true, mock)
  },
  disassociateLensesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateLenses', 'WellArchitected', result, false, true, mock)
  },
  disassociateLensesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateLenses', 'WellArchitected', result, true, false, mock)
  },
  getAnswer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAnswer', 'WellArchitected', result, true, true, mock)
  },
  getAnswerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAnswer', 'WellArchitected', result, false, true, mock)
  },
  getAnswerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAnswer', 'WellArchitected', result, true, false, mock)
  },
  getLensReview: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensReview', 'WellArchitected', result, true, true, mock)
  },
  getLensReviewAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensReview', 'WellArchitected', result, false, true, mock)
  },
  getLensReviewThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensReview', 'WellArchitected', result, true, false, mock)
  },
  getLensReviewReport: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensReviewReport', 'WellArchitected', result, true, true, mock)
  },
  getLensReviewReportAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensReviewReport', 'WellArchitected', result, false, true, mock)
  },
  getLensReviewReportThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensReviewReport', 'WellArchitected', result, true, false, mock)
  },
  getLensVersionDifference: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensVersionDifference', 'WellArchitected', result, true, true, mock)
  },
  getLensVersionDifferenceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensVersionDifference', 'WellArchitected', result, false, true, mock)
  },
  getLensVersionDifferenceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLensVersionDifference', 'WellArchitected', result, true, false, mock)
  },
  getMilestone: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMilestone', 'WellArchitected', result, true, true, mock)
  },
  getMilestoneAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMilestone', 'WellArchitected', result, false, true, mock)
  },
  getMilestoneThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMilestone', 'WellArchitected', result, true, false, mock)
  },
  getWorkload: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getWorkload', 'WellArchitected', result, true, true, mock)
  },
  getWorkloadAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getWorkload', 'WellArchitected', result, false, true, mock)
  },
  getWorkloadThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getWorkload', 'WellArchitected', result, true, false, mock)
  },
  listAnswers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnswers', 'WellArchitected', result, true, true, mock)
  },
  listAnswersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnswers', 'WellArchitected', result, false, true, mock)
  },
  listAnswersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnswers', 'WellArchitected', result, true, false, mock)
  },
  listLensReviewImprovements: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLensReviewImprovements', 'WellArchitected', result, true, true, mock)
  },
  listLensReviewImprovementsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLensReviewImprovements', 'WellArchitected', result, false, true, mock)
  },
  listLensReviewImprovementsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLensReviewImprovements', 'WellArchitected', result, true, false, mock)
  },
  listLensReviews: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLensReviews', 'WellArchitected', result, true, true, mock)
  },
  listLensReviewsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLensReviews', 'WellArchitected', result, false, true, mock)
  },
  listLensReviewsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLensReviews', 'WellArchitected', result, true, false, mock)
  },
  listLenses: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLenses', 'WellArchitected', result, true, true, mock)
  },
  listLensesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLenses', 'WellArchitected', result, false, true, mock)
  },
  listLensesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLenses', 'WellArchitected', result, true, false, mock)
  },
  listMilestones: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMilestones', 'WellArchitected', result, true, true, mock)
  },
  listMilestonesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMilestones', 'WellArchitected', result, false, true, mock)
  },
  listMilestonesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMilestones', 'WellArchitected', result, true, false, mock)
  },
  listNotifications: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNotifications', 'WellArchitected', result, true, true, mock)
  },
  listNotificationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNotifications', 'WellArchitected', result, false, true, mock)
  },
  listNotificationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNotifications', 'WellArchitected', result, true, false, mock)
  },
  listShareInvitations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listShareInvitations', 'WellArchitected', result, true, true, mock)
  },
  listShareInvitationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listShareInvitations', 'WellArchitected', result, false, true, mock)
  },
  listShareInvitationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listShareInvitations', 'WellArchitected', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'WellArchitected', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'WellArchitected', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'WellArchitected', result, true, false, mock)
  },
  listWorkloadShares: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkloadShares', 'WellArchitected', result, true, true, mock)
  },
  listWorkloadSharesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkloadShares', 'WellArchitected', result, false, true, mock)
  },
  listWorkloadSharesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkloadShares', 'WellArchitected', result, true, false, mock)
  },
  listWorkloads: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkloads', 'WellArchitected', result, true, true, mock)
  },
  listWorkloadsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkloads', 'WellArchitected', result, false, true, mock)
  },
  listWorkloadsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkloads', 'WellArchitected', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'WellArchitected', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'WellArchitected', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'WellArchitected', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'WellArchitected', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'WellArchitected', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'WellArchitected', result, true, false, mock)
  },
  updateAnswer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAnswer', 'WellArchitected', result, true, true, mock)
  },
  updateAnswerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAnswer', 'WellArchitected', result, false, true, mock)
  },
  updateAnswerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAnswer', 'WellArchitected', result, true, false, mock)
  },
  updateLensReview: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLensReview', 'WellArchitected', result, true, true, mock)
  },
  updateLensReviewAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLensReview', 'WellArchitected', result, false, true, mock)
  },
  updateLensReviewThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLensReview', 'WellArchitected', result, true, false, mock)
  },
  updateShareInvitation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateShareInvitation', 'WellArchitected', result, true, true, mock)
  },
  updateShareInvitationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateShareInvitation', 'WellArchitected', result, false, true, mock)
  },
  updateShareInvitationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateShareInvitation', 'WellArchitected', result, true, false, mock)
  },
  updateWorkload: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkload', 'WellArchitected', result, true, true, mock)
  },
  updateWorkloadAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkload', 'WellArchitected', result, false, true, mock)
  },
  updateWorkloadThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkload', 'WellArchitected', result, true, false, mock)
  },
  updateWorkloadShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkloadShare', 'WellArchitected', result, true, true, mock)
  },
  updateWorkloadShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkloadShare', 'WellArchitected', result, false, true, mock)
  },
  updateWorkloadShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkloadShare', 'WellArchitected', result, true, false, mock)
  },
  upgradeLensReview: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('upgradeLensReview', 'WellArchitected', result, true, true, mock)
  },
  upgradeLensReviewAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('upgradeLensReview', 'WellArchitected', result, false, true, mock)
  },
  upgradeLensReviewThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('upgradeLensReview', 'WellArchitected', result, true, false, mock)
  },
}
