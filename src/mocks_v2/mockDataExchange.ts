
/**
* @description mocks_v2:mockDataExchange.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockDataExchange = {
  cancelJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelJob', 'DataExchange', result, true, true, mock)
  },
  cancelJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelJob', 'DataExchange', result, false, true, mock)
  },
  cancelJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelJob', 'DataExchange', result, true, false, mock)
  },
  createDataSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSet', 'DataExchange', result, true, true, mock)
  },
  createDataSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSet', 'DataExchange', result, false, true, mock)
  },
  createDataSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSet', 'DataExchange', result, true, false, mock)
  },
  createEventAction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createEventAction', 'DataExchange', result, true, true, mock)
  },
  createEventActionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createEventAction', 'DataExchange', result, false, true, mock)
  },
  createEventActionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createEventAction', 'DataExchange', result, true, false, mock)
  },
  createJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createJob', 'DataExchange', result, true, true, mock)
  },
  createJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createJob', 'DataExchange', result, false, true, mock)
  },
  createJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createJob', 'DataExchange', result, true, false, mock)
  },
  createRevision: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createRevision', 'DataExchange', result, true, true, mock)
  },
  createRevisionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createRevision', 'DataExchange', result, false, true, mock)
  },
  createRevisionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createRevision', 'DataExchange', result, true, false, mock)
  },
  deleteAsset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAsset', 'DataExchange', result, true, true, mock)
  },
  deleteAssetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAsset', 'DataExchange', result, false, true, mock)
  },
  deleteAssetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAsset', 'DataExchange', result, true, false, mock)
  },
  deleteDataSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSet', 'DataExchange', result, true, true, mock)
  },
  deleteDataSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSet', 'DataExchange', result, false, true, mock)
  },
  deleteDataSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSet', 'DataExchange', result, true, false, mock)
  },
  deleteEventAction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteEventAction', 'DataExchange', result, true, true, mock)
  },
  deleteEventActionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteEventAction', 'DataExchange', result, false, true, mock)
  },
  deleteEventActionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteEventAction', 'DataExchange', result, true, false, mock)
  },
  deleteRevision: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteRevision', 'DataExchange', result, true, true, mock)
  },
  deleteRevisionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteRevision', 'DataExchange', result, false, true, mock)
  },
  deleteRevisionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteRevision', 'DataExchange', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataExchange', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataExchange', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataExchange', result, true, false, mock)
  },
  getAsset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAsset', 'DataExchange', result, true, true, mock)
  },
  getAssetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAsset', 'DataExchange', result, false, true, mock)
  },
  getAssetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAsset', 'DataExchange', result, true, false, mock)
  },
  getDataSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataSet', 'DataExchange', result, true, true, mock)
  },
  getDataSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataSet', 'DataExchange', result, false, true, mock)
  },
  getDataSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataSet', 'DataExchange', result, true, false, mock)
  },
  getEventAction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getEventAction', 'DataExchange', result, true, true, mock)
  },
  getEventActionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getEventAction', 'DataExchange', result, false, true, mock)
  },
  getEventActionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getEventAction', 'DataExchange', result, true, false, mock)
  },
  getJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJob', 'DataExchange', result, true, true, mock)
  },
  getJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJob', 'DataExchange', result, false, true, mock)
  },
  getJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJob', 'DataExchange', result, true, false, mock)
  },
  getRevision: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getRevision', 'DataExchange', result, true, true, mock)
  },
  getRevisionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getRevision', 'DataExchange', result, false, true, mock)
  },
  getRevisionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getRevision', 'DataExchange', result, true, false, mock)
  },
  listDataSetRevisions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSetRevisions', 'DataExchange', result, true, true, mock)
  },
  listDataSetRevisionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSetRevisions', 'DataExchange', result, false, true, mock)
  },
  listDataSetRevisionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSetRevisions', 'DataExchange', result, true, false, mock)
  },
  listDataSets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSets', 'DataExchange', result, true, true, mock)
  },
  listDataSetsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSets', 'DataExchange', result, false, true, mock)
  },
  listDataSetsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSets', 'DataExchange', result, true, false, mock)
  },
  listEventActions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEventActions', 'DataExchange', result, true, true, mock)
  },
  listEventActionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEventActions', 'DataExchange', result, false, true, mock)
  },
  listEventActionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEventActions', 'DataExchange', result, true, false, mock)
  },
  listJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listJobs', 'DataExchange', result, true, true, mock)
  },
  listJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listJobs', 'DataExchange', result, false, true, mock)
  },
  listJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listJobs', 'DataExchange', result, true, false, mock)
  },
  listRevisionAssets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRevisionAssets', 'DataExchange', result, true, true, mock)
  },
  listRevisionAssetsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRevisionAssets', 'DataExchange', result, false, true, mock)
  },
  listRevisionAssetsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRevisionAssets', 'DataExchange', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DataExchange', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DataExchange', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DataExchange', result, true, false, mock)
  },
  startJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startJob', 'DataExchange', result, true, true, mock)
  },
  startJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startJob', 'DataExchange', result, false, true, mock)
  },
  startJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startJob', 'DataExchange', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'DataExchange', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'DataExchange', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'DataExchange', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'DataExchange', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'DataExchange', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'DataExchange', result, true, false, mock)
  },
  updateAsset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAsset', 'DataExchange', result, true, true, mock)
  },
  updateAssetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAsset', 'DataExchange', result, false, true, mock)
  },
  updateAssetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAsset', 'DataExchange', result, true, false, mock)
  },
  updateDataSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSet', 'DataExchange', result, true, true, mock)
  },
  updateDataSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSet', 'DataExchange', result, false, true, mock)
  },
  updateDataSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSet', 'DataExchange', result, true, false, mock)
  },
  updateEventAction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateEventAction', 'DataExchange', result, true, true, mock)
  },
  updateEventActionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateEventAction', 'DataExchange', result, false, true, mock)
  },
  updateEventActionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateEventAction', 'DataExchange', result, true, false, mock)
  },
  updateRevision: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRevision', 'DataExchange', result, true, true, mock)
  },
  updateRevisionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRevision', 'DataExchange', result, false, true, mock)
  },
  updateRevisionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRevision', 'DataExchange', result, true, false, mock)
  },
}
