
/**
* @description mocks_v2:mockDirectoryService.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockDirectoryService = {
  acceptSharedDirectory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptSharedDirectory', 'DirectoryService', result, true, true, mock)
  },
  acceptSharedDirectoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptSharedDirectory', 'DirectoryService', result, false, true, mock)
  },
  acceptSharedDirectoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptSharedDirectory', 'DirectoryService', result, true, false, mock)
  },
  addIpRoutes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addIpRoutes', 'DirectoryService', result, true, true, mock)
  },
  addIpRoutesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addIpRoutes', 'DirectoryService', result, false, true, mock)
  },
  addIpRoutesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addIpRoutes', 'DirectoryService', result, true, false, mock)
  },
  addRegion: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addRegion', 'DirectoryService', result, true, true, mock)
  },
  addRegionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addRegion', 'DirectoryService', result, false, true, mock)
  },
  addRegionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addRegion', 'DirectoryService', result, true, false, mock)
  },
  addTagsToResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTagsToResource', 'DirectoryService', result, true, true, mock)
  },
  addTagsToResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTagsToResource', 'DirectoryService', result, false, true, mock)
  },
  addTagsToResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTagsToResource', 'DirectoryService', result, true, false, mock)
  },
  cancelSchemaExtension: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelSchemaExtension', 'DirectoryService', result, true, true, mock)
  },
  cancelSchemaExtensionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelSchemaExtension', 'DirectoryService', result, false, true, mock)
  },
  cancelSchemaExtensionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelSchemaExtension', 'DirectoryService', result, true, false, mock)
  },
  connectDirectory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('connectDirectory', 'DirectoryService', result, true, true, mock)
  },
  connectDirectoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('connectDirectory', 'DirectoryService', result, false, true, mock)
  },
  connectDirectoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('connectDirectory', 'DirectoryService', result, true, false, mock)
  },
  createAlias: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAlias', 'DirectoryService', result, true, true, mock)
  },
  createAliasAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAlias', 'DirectoryService', result, false, true, mock)
  },
  createAliasThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAlias', 'DirectoryService', result, true, false, mock)
  },
  createComputer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createComputer', 'DirectoryService', result, true, true, mock)
  },
  createComputerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createComputer', 'DirectoryService', result, false, true, mock)
  },
  createComputerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createComputer', 'DirectoryService', result, true, false, mock)
  },
  createConditionalForwarder: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConditionalForwarder', 'DirectoryService', result, true, true, mock)
  },
  createConditionalForwarderAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConditionalForwarder', 'DirectoryService', result, false, true, mock)
  },
  createConditionalForwarderThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConditionalForwarder', 'DirectoryService', result, true, false, mock)
  },
  createDirectory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDirectory', 'DirectoryService', result, true, true, mock)
  },
  createDirectoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDirectory', 'DirectoryService', result, false, true, mock)
  },
  createDirectoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDirectory', 'DirectoryService', result, true, false, mock)
  },
  createLogSubscription: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLogSubscription', 'DirectoryService', result, true, true, mock)
  },
  createLogSubscriptionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLogSubscription', 'DirectoryService', result, false, true, mock)
  },
  createLogSubscriptionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLogSubscription', 'DirectoryService', result, true, false, mock)
  },
  createMicrosoftAD: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMicrosoftAD', 'DirectoryService', result, true, true, mock)
  },
  createMicrosoftADAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMicrosoftAD', 'DirectoryService', result, false, true, mock)
  },
  createMicrosoftADThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMicrosoftAD', 'DirectoryService', result, true, false, mock)
  },
  createSnapshot: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshot', 'DirectoryService', result, true, true, mock)
  },
  createSnapshotAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshot', 'DirectoryService', result, false, true, mock)
  },
  createSnapshotThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshot', 'DirectoryService', result, true, false, mock)
  },
  createTrust: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTrust', 'DirectoryService', result, true, true, mock)
  },
  createTrustAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTrust', 'DirectoryService', result, false, true, mock)
  },
  createTrustThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTrust', 'DirectoryService', result, true, false, mock)
  },
  deleteConditionalForwarder: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConditionalForwarder', 'DirectoryService', result, true, true, mock)
  },
  deleteConditionalForwarderAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConditionalForwarder', 'DirectoryService', result, false, true, mock)
  },
  deleteConditionalForwarderThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConditionalForwarder', 'DirectoryService', result, true, false, mock)
  },
  deleteDirectory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDirectory', 'DirectoryService', result, true, true, mock)
  },
  deleteDirectoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDirectory', 'DirectoryService', result, false, true, mock)
  },
  deleteDirectoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDirectory', 'DirectoryService', result, true, false, mock)
  },
  deleteLogSubscription: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLogSubscription', 'DirectoryService', result, true, true, mock)
  },
  deleteLogSubscriptionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLogSubscription', 'DirectoryService', result, false, true, mock)
  },
  deleteLogSubscriptionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLogSubscription', 'DirectoryService', result, true, false, mock)
  },
  deleteSnapshot: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSnapshot', 'DirectoryService', result, true, true, mock)
  },
  deleteSnapshotAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSnapshot', 'DirectoryService', result, false, true, mock)
  },
  deleteSnapshotThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSnapshot', 'DirectoryService', result, true, false, mock)
  },
  deleteTrust: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTrust', 'DirectoryService', result, true, true, mock)
  },
  deleteTrustAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTrust', 'DirectoryService', result, false, true, mock)
  },
  deleteTrustThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTrust', 'DirectoryService', result, true, false, mock)
  },
  deregisterCertificate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterCertificate', 'DirectoryService', result, true, true, mock)
  },
  deregisterCertificateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterCertificate', 'DirectoryService', result, false, true, mock)
  },
  deregisterCertificateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterCertificate', 'DirectoryService', result, true, false, mock)
  },
  deregisterEventTopic: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterEventTopic', 'DirectoryService', result, true, true, mock)
  },
  deregisterEventTopicAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterEventTopic', 'DirectoryService', result, false, true, mock)
  },
  deregisterEventTopicThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterEventTopic', 'DirectoryService', result, true, false, mock)
  },
  describeCertificate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCertificate', 'DirectoryService', result, true, true, mock)
  },
  describeCertificateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCertificate', 'DirectoryService', result, false, true, mock)
  },
  describeCertificateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCertificate', 'DirectoryService', result, true, false, mock)
  },
  describeClientAuthenticationSettings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeClientAuthenticationSettings', 'DirectoryService', result, true, true, mock)
  },
  describeClientAuthenticationSettingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeClientAuthenticationSettings', 'DirectoryService', result, false, true, mock)
  },
  describeClientAuthenticationSettingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeClientAuthenticationSettings', 'DirectoryService', result, true, false, mock)
  },
  describeConditionalForwarders: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConditionalForwarders', 'DirectoryService', result, true, true, mock)
  },
  describeConditionalForwardersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConditionalForwarders', 'DirectoryService', result, false, true, mock)
  },
  describeConditionalForwardersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConditionalForwarders', 'DirectoryService', result, true, false, mock)
  },
  describeDirectories: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDirectories', 'DirectoryService', result, true, true, mock)
  },
  describeDirectoriesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDirectories', 'DirectoryService', result, false, true, mock)
  },
  describeDirectoriesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDirectories', 'DirectoryService', result, true, false, mock)
  },
  describeDomainControllers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainControllers', 'DirectoryService', result, true, true, mock)
  },
  describeDomainControllersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainControllers', 'DirectoryService', result, false, true, mock)
  },
  describeDomainControllersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainControllers', 'DirectoryService', result, true, false, mock)
  },
  describeEventTopics: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeEventTopics', 'DirectoryService', result, true, true, mock)
  },
  describeEventTopicsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeEventTopics', 'DirectoryService', result, false, true, mock)
  },
  describeEventTopicsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeEventTopics', 'DirectoryService', result, true, false, mock)
  },
  describeLDAPSSettings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLDAPSSettings', 'DirectoryService', result, true, true, mock)
  },
  describeLDAPSSettingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLDAPSSettings', 'DirectoryService', result, false, true, mock)
  },
  describeLDAPSSettingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLDAPSSettings', 'DirectoryService', result, true, false, mock)
  },
  describeRegions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeRegions', 'DirectoryService', result, true, true, mock)
  },
  describeRegionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeRegions', 'DirectoryService', result, false, true, mock)
  },
  describeRegionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeRegions', 'DirectoryService', result, true, false, mock)
  },
  describeSharedDirectories: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSharedDirectories', 'DirectoryService', result, true, true, mock)
  },
  describeSharedDirectoriesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSharedDirectories', 'DirectoryService', result, false, true, mock)
  },
  describeSharedDirectoriesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSharedDirectories', 'DirectoryService', result, true, false, mock)
  },
  describeSnapshots: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSnapshots', 'DirectoryService', result, true, true, mock)
  },
  describeSnapshotsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSnapshots', 'DirectoryService', result, false, true, mock)
  },
  describeSnapshotsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSnapshots', 'DirectoryService', result, true, false, mock)
  },
  describeTrusts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTrusts', 'DirectoryService', result, true, true, mock)
  },
  describeTrustsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTrusts', 'DirectoryService', result, false, true, mock)
  },
  describeTrustsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTrusts', 'DirectoryService', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DirectoryService', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DirectoryService', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DirectoryService', result, true, false, mock)
  },
  disableClientAuthentication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableClientAuthentication', 'DirectoryService', result, true, true, mock)
  },
  disableClientAuthenticationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableClientAuthentication', 'DirectoryService', result, false, true, mock)
  },
  disableClientAuthenticationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableClientAuthentication', 'DirectoryService', result, true, false, mock)
  },
  disableLDAPS: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableLDAPS', 'DirectoryService', result, true, true, mock)
  },
  disableLDAPSAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableLDAPS', 'DirectoryService', result, false, true, mock)
  },
  disableLDAPSThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableLDAPS', 'DirectoryService', result, true, false, mock)
  },
  disableRadius: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableRadius', 'DirectoryService', result, true, true, mock)
  },
  disableRadiusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableRadius', 'DirectoryService', result, false, true, mock)
  },
  disableRadiusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableRadius', 'DirectoryService', result, true, false, mock)
  },
  disableSso: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableSso', 'DirectoryService', result, true, true, mock)
  },
  disableSsoAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableSso', 'DirectoryService', result, false, true, mock)
  },
  disableSsoThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableSso', 'DirectoryService', result, true, false, mock)
  },
  enableClientAuthentication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableClientAuthentication', 'DirectoryService', result, true, true, mock)
  },
  enableClientAuthenticationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableClientAuthentication', 'DirectoryService', result, false, true, mock)
  },
  enableClientAuthenticationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableClientAuthentication', 'DirectoryService', result, true, false, mock)
  },
  enableLDAPS: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableLDAPS', 'DirectoryService', result, true, true, mock)
  },
  enableLDAPSAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableLDAPS', 'DirectoryService', result, false, true, mock)
  },
  enableLDAPSThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableLDAPS', 'DirectoryService', result, true, false, mock)
  },
  enableRadius: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableRadius', 'DirectoryService', result, true, true, mock)
  },
  enableRadiusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableRadius', 'DirectoryService', result, false, true, mock)
  },
  enableRadiusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableRadius', 'DirectoryService', result, true, false, mock)
  },
  enableSso: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableSso', 'DirectoryService', result, true, true, mock)
  },
  enableSsoAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableSso', 'DirectoryService', result, false, true, mock)
  },
  enableSsoThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableSso', 'DirectoryService', result, true, false, mock)
  },
  getDirectoryLimits: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDirectoryLimits', 'DirectoryService', result, true, true, mock)
  },
  getDirectoryLimitsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDirectoryLimits', 'DirectoryService', result, false, true, mock)
  },
  getDirectoryLimitsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDirectoryLimits', 'DirectoryService', result, true, false, mock)
  },
  getSnapshotLimits: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSnapshotLimits', 'DirectoryService', result, true, true, mock)
  },
  getSnapshotLimitsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSnapshotLimits', 'DirectoryService', result, false, true, mock)
  },
  getSnapshotLimitsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSnapshotLimits', 'DirectoryService', result, true, false, mock)
  },
  listCertificates: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCertificates', 'DirectoryService', result, true, true, mock)
  },
  listCertificatesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCertificates', 'DirectoryService', result, false, true, mock)
  },
  listCertificatesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCertificates', 'DirectoryService', result, true, false, mock)
  },
  listIpRoutes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIpRoutes', 'DirectoryService', result, true, true, mock)
  },
  listIpRoutesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIpRoutes', 'DirectoryService', result, false, true, mock)
  },
  listIpRoutesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIpRoutes', 'DirectoryService', result, true, false, mock)
  },
  listLogSubscriptions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLogSubscriptions', 'DirectoryService', result, true, true, mock)
  },
  listLogSubscriptionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLogSubscriptions', 'DirectoryService', result, false, true, mock)
  },
  listLogSubscriptionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLogSubscriptions', 'DirectoryService', result, true, false, mock)
  },
  listSchemaExtensions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemaExtensions', 'DirectoryService', result, true, true, mock)
  },
  listSchemaExtensionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemaExtensions', 'DirectoryService', result, false, true, mock)
  },
  listSchemaExtensionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemaExtensions', 'DirectoryService', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DirectoryService', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DirectoryService', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DirectoryService', result, true, false, mock)
  },
  registerCertificate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerCertificate', 'DirectoryService', result, true, true, mock)
  },
  registerCertificateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerCertificate', 'DirectoryService', result, false, true, mock)
  },
  registerCertificateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerCertificate', 'DirectoryService', result, true, false, mock)
  },
  registerEventTopic: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerEventTopic', 'DirectoryService', result, true, true, mock)
  },
  registerEventTopicAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerEventTopic', 'DirectoryService', result, false, true, mock)
  },
  registerEventTopicThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerEventTopic', 'DirectoryService', result, true, false, mock)
  },
  rejectSharedDirectory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectSharedDirectory', 'DirectoryService', result, true, true, mock)
  },
  rejectSharedDirectoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectSharedDirectory', 'DirectoryService', result, false, true, mock)
  },
  rejectSharedDirectoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectSharedDirectory', 'DirectoryService', result, true, false, mock)
  },
  removeIpRoutes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeIpRoutes', 'DirectoryService', result, true, true, mock)
  },
  removeIpRoutesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeIpRoutes', 'DirectoryService', result, false, true, mock)
  },
  removeIpRoutesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeIpRoutes', 'DirectoryService', result, true, false, mock)
  },
  removeRegion: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeRegion', 'DirectoryService', result, true, true, mock)
  },
  removeRegionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeRegion', 'DirectoryService', result, false, true, mock)
  },
  removeRegionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeRegion', 'DirectoryService', result, true, false, mock)
  },
  removeTagsFromResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTagsFromResource', 'DirectoryService', result, true, true, mock)
  },
  removeTagsFromResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTagsFromResource', 'DirectoryService', result, false, true, mock)
  },
  removeTagsFromResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTagsFromResource', 'DirectoryService', result, true, false, mock)
  },
  resetUserPassword: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resetUserPassword', 'DirectoryService', result, true, true, mock)
  },
  resetUserPasswordAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resetUserPassword', 'DirectoryService', result, false, true, mock)
  },
  resetUserPasswordThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resetUserPassword', 'DirectoryService', result, true, false, mock)
  },
  restoreFromSnapshot: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('restoreFromSnapshot', 'DirectoryService', result, true, true, mock)
  },
  restoreFromSnapshotAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('restoreFromSnapshot', 'DirectoryService', result, false, true, mock)
  },
  restoreFromSnapshotThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('restoreFromSnapshot', 'DirectoryService', result, true, false, mock)
  },
  shareDirectory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('shareDirectory', 'DirectoryService', result, true, true, mock)
  },
  shareDirectoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('shareDirectory', 'DirectoryService', result, false, true, mock)
  },
  shareDirectoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('shareDirectory', 'DirectoryService', result, true, false, mock)
  },
  startSchemaExtension: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSchemaExtension', 'DirectoryService', result, true, true, mock)
  },
  startSchemaExtensionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSchemaExtension', 'DirectoryService', result, false, true, mock)
  },
  startSchemaExtensionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSchemaExtension', 'DirectoryService', result, true, false, mock)
  },
  unshareDirectory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unshareDirectory', 'DirectoryService', result, true, true, mock)
  },
  unshareDirectoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unshareDirectory', 'DirectoryService', result, false, true, mock)
  },
  unshareDirectoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unshareDirectory', 'DirectoryService', result, true, false, mock)
  },
  updateConditionalForwarder: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConditionalForwarder', 'DirectoryService', result, true, true, mock)
  },
  updateConditionalForwarderAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConditionalForwarder', 'DirectoryService', result, false, true, mock)
  },
  updateConditionalForwarderThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConditionalForwarder', 'DirectoryService', result, true, false, mock)
  },
  updateNumberOfDomainControllers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNumberOfDomainControllers', 'DirectoryService', result, true, true, mock)
  },
  updateNumberOfDomainControllersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNumberOfDomainControllers', 'DirectoryService', result, false, true, mock)
  },
  updateNumberOfDomainControllersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNumberOfDomainControllers', 'DirectoryService', result, true, false, mock)
  },
  updateRadius: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRadius', 'DirectoryService', result, true, true, mock)
  },
  updateRadiusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRadius', 'DirectoryService', result, false, true, mock)
  },
  updateRadiusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRadius', 'DirectoryService', result, true, false, mock)
  },
  updateTrust: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTrust', 'DirectoryService', result, true, true, mock)
  },
  updateTrustAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTrust', 'DirectoryService', result, false, true, mock)
  },
  updateTrustThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTrust', 'DirectoryService', result, true, false, mock)
  },
  verifyTrust: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('verifyTrust', 'DirectoryService', result, true, true, mock)
  },
  verifyTrustAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('verifyTrust', 'DirectoryService', result, false, true, mock)
  },
  verifyTrustThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('verifyTrust', 'DirectoryService', result, true, false, mock)
  },
}
