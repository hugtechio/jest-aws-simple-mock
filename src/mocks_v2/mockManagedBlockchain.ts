
/**
* @description mocks_v2:mockManagedBlockchain.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockManagedBlockchain = {
  createMember: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMember', 'ManagedBlockchain', result, true, true, mock)
  },
  createMemberAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMember', 'ManagedBlockchain', result, false, true, mock)
  },
  createMemberThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMember', 'ManagedBlockchain', result, true, false, mock)
  },
  createNetwork: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNetwork', 'ManagedBlockchain', result, true, true, mock)
  },
  createNetworkAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNetwork', 'ManagedBlockchain', result, false, true, mock)
  },
  createNetworkThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNetwork', 'ManagedBlockchain', result, true, false, mock)
  },
  createNode: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNode', 'ManagedBlockchain', result, true, true, mock)
  },
  createNodeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNode', 'ManagedBlockchain', result, false, true, mock)
  },
  createNodeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNode', 'ManagedBlockchain', result, true, false, mock)
  },
  createProposal: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProposal', 'ManagedBlockchain', result, true, true, mock)
  },
  createProposalAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProposal', 'ManagedBlockchain', result, false, true, mock)
  },
  createProposalThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProposal', 'ManagedBlockchain', result, true, false, mock)
  },
  deleteMember: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMember', 'ManagedBlockchain', result, true, true, mock)
  },
  deleteMemberAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMember', 'ManagedBlockchain', result, false, true, mock)
  },
  deleteMemberThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMember', 'ManagedBlockchain', result, true, false, mock)
  },
  deleteNode: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteNode', 'ManagedBlockchain', result, true, true, mock)
  },
  deleteNodeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteNode', 'ManagedBlockchain', result, false, true, mock)
  },
  deleteNodeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteNode', 'ManagedBlockchain', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ManagedBlockchain', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ManagedBlockchain', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ManagedBlockchain', result, true, false, mock)
  },
  getMember: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMember', 'ManagedBlockchain', result, true, true, mock)
  },
  getMemberAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMember', 'ManagedBlockchain', result, false, true, mock)
  },
  getMemberThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMember', 'ManagedBlockchain', result, true, false, mock)
  },
  getNetwork: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNetwork', 'ManagedBlockchain', result, true, true, mock)
  },
  getNetworkAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNetwork', 'ManagedBlockchain', result, false, true, mock)
  },
  getNetworkThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNetwork', 'ManagedBlockchain', result, true, false, mock)
  },
  getNode: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNode', 'ManagedBlockchain', result, true, true, mock)
  },
  getNodeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNode', 'ManagedBlockchain', result, false, true, mock)
  },
  getNodeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNode', 'ManagedBlockchain', result, true, false, mock)
  },
  getProposal: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProposal', 'ManagedBlockchain', result, true, true, mock)
  },
  getProposalAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProposal', 'ManagedBlockchain', result, false, true, mock)
  },
  getProposalThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProposal', 'ManagedBlockchain', result, true, false, mock)
  },
  listInvitations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInvitations', 'ManagedBlockchain', result, true, true, mock)
  },
  listInvitationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInvitations', 'ManagedBlockchain', result, false, true, mock)
  },
  listInvitationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInvitations', 'ManagedBlockchain', result, true, false, mock)
  },
  listMembers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMembers', 'ManagedBlockchain', result, true, true, mock)
  },
  listMembersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMembers', 'ManagedBlockchain', result, false, true, mock)
  },
  listMembersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMembers', 'ManagedBlockchain', result, true, false, mock)
  },
  listNetworks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNetworks', 'ManagedBlockchain', result, true, true, mock)
  },
  listNetworksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNetworks', 'ManagedBlockchain', result, false, true, mock)
  },
  listNetworksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNetworks', 'ManagedBlockchain', result, true, false, mock)
  },
  listNodes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNodes', 'ManagedBlockchain', result, true, true, mock)
  },
  listNodesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNodes', 'ManagedBlockchain', result, false, true, mock)
  },
  listNodesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNodes', 'ManagedBlockchain', result, true, false, mock)
  },
  listProposalVotes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProposalVotes', 'ManagedBlockchain', result, true, true, mock)
  },
  listProposalVotesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProposalVotes', 'ManagedBlockchain', result, false, true, mock)
  },
  listProposalVotesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProposalVotes', 'ManagedBlockchain', result, true, false, mock)
  },
  listProposals: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProposals', 'ManagedBlockchain', result, true, true, mock)
  },
  listProposalsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProposals', 'ManagedBlockchain', result, false, true, mock)
  },
  listProposalsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProposals', 'ManagedBlockchain', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'ManagedBlockchain', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'ManagedBlockchain', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'ManagedBlockchain', result, true, false, mock)
  },
  rejectInvitation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInvitation', 'ManagedBlockchain', result, true, true, mock)
  },
  rejectInvitationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInvitation', 'ManagedBlockchain', result, false, true, mock)
  },
  rejectInvitationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInvitation', 'ManagedBlockchain', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'ManagedBlockchain', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'ManagedBlockchain', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'ManagedBlockchain', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'ManagedBlockchain', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'ManagedBlockchain', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'ManagedBlockchain', result, true, false, mock)
  },
  updateMember: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMember', 'ManagedBlockchain', result, true, true, mock)
  },
  updateMemberAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMember', 'ManagedBlockchain', result, false, true, mock)
  },
  updateMemberThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMember', 'ManagedBlockchain', result, true, false, mock)
  },
  updateNode: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNode', 'ManagedBlockchain', result, true, true, mock)
  },
  updateNodeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNode', 'ManagedBlockchain', result, false, true, mock)
  },
  updateNodeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNode', 'ManagedBlockchain', result, true, false, mock)
  },
  voteOnProposal: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('voteOnProposal', 'ManagedBlockchain', result, true, true, mock)
  },
  voteOnProposalAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('voteOnProposal', 'ManagedBlockchain', result, false, true, mock)
  },
  voteOnProposalThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('voteOnProposal', 'ManagedBlockchain', result, true, false, mock)
  },
}
