
/**
* @description mocks_v2:mockCloudControl.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockCloudControl = {
  cancelResourceRequest: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelResourceRequest', 'CloudControl', result, true, true, mock)
  },
  cancelResourceRequestAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelResourceRequest', 'CloudControl', result, false, true, mock)
  },
  cancelResourceRequestThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelResourceRequest', 'CloudControl', result, true, false, mock)
  },
  createResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResource', 'CloudControl', result, true, true, mock)
  },
  createResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResource', 'CloudControl', result, false, true, mock)
  },
  createResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResource', 'CloudControl', result, true, false, mock)
  },
  deleteResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResource', 'CloudControl', result, true, true, mock)
  },
  deleteResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResource', 'CloudControl', result, false, true, mock)
  },
  deleteResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResource', 'CloudControl', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CloudControl', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CloudControl', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CloudControl', result, true, false, mock)
  },
  getResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResource', 'CloudControl', result, true, true, mock)
  },
  getResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResource', 'CloudControl', result, false, true, mock)
  },
  getResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResource', 'CloudControl', result, true, false, mock)
  },
  getResourceRequestStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourceRequestStatus', 'CloudControl', result, true, true, mock)
  },
  getResourceRequestStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourceRequestStatus', 'CloudControl', result, false, true, mock)
  },
  getResourceRequestStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourceRequestStatus', 'CloudControl', result, true, false, mock)
  },
  listResourceRequests: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResourceRequests', 'CloudControl', result, true, true, mock)
  },
  listResourceRequestsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResourceRequests', 'CloudControl', result, false, true, mock)
  },
  listResourceRequestsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResourceRequests', 'CloudControl', result, true, false, mock)
  },
  listResources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResources', 'CloudControl', result, true, true, mock)
  },
  listResourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResources', 'CloudControl', result, false, true, mock)
  },
  listResourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResources', 'CloudControl', result, true, false, mock)
  },
  updateResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResource', 'CloudControl', result, true, true, mock)
  },
  updateResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResource', 'CloudControl', result, false, true, mock)
  },
  updateResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResource', 'CloudControl', result, true, false, mock)
  },
}
