
/**
* @description mocks_v2:mockAmplifyBackend.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockAmplifyBackend = {
  cloneBackend: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cloneBackend', 'AmplifyBackend', result, true, true, mock)
  },
  cloneBackendAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cloneBackend', 'AmplifyBackend', result, false, true, mock)
  },
  cloneBackendThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cloneBackend', 'AmplifyBackend', result, true, false, mock)
  },
  createBackend: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackend', 'AmplifyBackend', result, true, true, mock)
  },
  createBackendAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackend', 'AmplifyBackend', result, false, true, mock)
  },
  createBackendThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackend', 'AmplifyBackend', result, true, false, mock)
  },
  createBackendAPI: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendAPI', 'AmplifyBackend', result, true, true, mock)
  },
  createBackendAPIAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendAPI', 'AmplifyBackend', result, false, true, mock)
  },
  createBackendAPIThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendAPI', 'AmplifyBackend', result, true, false, mock)
  },
  createBackendAuth: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendAuth', 'AmplifyBackend', result, true, true, mock)
  },
  createBackendAuthAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendAuth', 'AmplifyBackend', result, false, true, mock)
  },
  createBackendAuthThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendAuth', 'AmplifyBackend', result, true, false, mock)
  },
  createBackendConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendConfig', 'AmplifyBackend', result, true, true, mock)
  },
  createBackendConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendConfig', 'AmplifyBackend', result, false, true, mock)
  },
  createBackendConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendConfig', 'AmplifyBackend', result, true, false, mock)
  },
  createBackendStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendStorage', 'AmplifyBackend', result, true, true, mock)
  },
  createBackendStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendStorage', 'AmplifyBackend', result, false, true, mock)
  },
  createBackendStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendStorage', 'AmplifyBackend', result, true, false, mock)
  },
  createToken: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createToken', 'AmplifyBackend', result, true, true, mock)
  },
  createTokenAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createToken', 'AmplifyBackend', result, false, true, mock)
  },
  createTokenThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createToken', 'AmplifyBackend', result, true, false, mock)
  },
  deleteBackend: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackend', 'AmplifyBackend', result, true, true, mock)
  },
  deleteBackendAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackend', 'AmplifyBackend', result, false, true, mock)
  },
  deleteBackendThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackend', 'AmplifyBackend', result, true, false, mock)
  },
  deleteBackendAPI: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendAPI', 'AmplifyBackend', result, true, true, mock)
  },
  deleteBackendAPIAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendAPI', 'AmplifyBackend', result, false, true, mock)
  },
  deleteBackendAPIThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendAPI', 'AmplifyBackend', result, true, false, mock)
  },
  deleteBackendAuth: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendAuth', 'AmplifyBackend', result, true, true, mock)
  },
  deleteBackendAuthAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendAuth', 'AmplifyBackend', result, false, true, mock)
  },
  deleteBackendAuthThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendAuth', 'AmplifyBackend', result, true, false, mock)
  },
  deleteBackendStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendStorage', 'AmplifyBackend', result, true, true, mock)
  },
  deleteBackendStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendStorage', 'AmplifyBackend', result, false, true, mock)
  },
  deleteBackendStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendStorage', 'AmplifyBackend', result, true, false, mock)
  },
  deleteToken: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteToken', 'AmplifyBackend', result, true, true, mock)
  },
  deleteTokenAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteToken', 'AmplifyBackend', result, false, true, mock)
  },
  deleteTokenThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteToken', 'AmplifyBackend', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AmplifyBackend', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AmplifyBackend', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AmplifyBackend', result, true, false, mock)
  },
  generateBackendAPIModels: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('generateBackendAPIModels', 'AmplifyBackend', result, true, true, mock)
  },
  generateBackendAPIModelsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('generateBackendAPIModels', 'AmplifyBackend', result, false, true, mock)
  },
  generateBackendAPIModelsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('generateBackendAPIModels', 'AmplifyBackend', result, true, false, mock)
  },
  getBackend: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackend', 'AmplifyBackend', result, true, true, mock)
  },
  getBackendAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackend', 'AmplifyBackend', result, false, true, mock)
  },
  getBackendThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackend', 'AmplifyBackend', result, true, false, mock)
  },
  getBackendAPI: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAPI', 'AmplifyBackend', result, true, true, mock)
  },
  getBackendAPIAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAPI', 'AmplifyBackend', result, false, true, mock)
  },
  getBackendAPIThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAPI', 'AmplifyBackend', result, true, false, mock)
  },
  getBackendAPIModels: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAPIModels', 'AmplifyBackend', result, true, true, mock)
  },
  getBackendAPIModelsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAPIModels', 'AmplifyBackend', result, false, true, mock)
  },
  getBackendAPIModelsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAPIModels', 'AmplifyBackend', result, true, false, mock)
  },
  getBackendAuth: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAuth', 'AmplifyBackend', result, true, true, mock)
  },
  getBackendAuthAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAuth', 'AmplifyBackend', result, false, true, mock)
  },
  getBackendAuthThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendAuth', 'AmplifyBackend', result, true, false, mock)
  },
  getBackendJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendJob', 'AmplifyBackend', result, true, true, mock)
  },
  getBackendJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendJob', 'AmplifyBackend', result, false, true, mock)
  },
  getBackendJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendJob', 'AmplifyBackend', result, true, false, mock)
  },
  getBackendStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendStorage', 'AmplifyBackend', result, true, true, mock)
  },
  getBackendStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendStorage', 'AmplifyBackend', result, false, true, mock)
  },
  getBackendStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendStorage', 'AmplifyBackend', result, true, false, mock)
  },
  getToken: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getToken', 'AmplifyBackend', result, true, true, mock)
  },
  getTokenAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getToken', 'AmplifyBackend', result, false, true, mock)
  },
  getTokenThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getToken', 'AmplifyBackend', result, true, false, mock)
  },
  importBackendAuth: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('importBackendAuth', 'AmplifyBackend', result, true, true, mock)
  },
  importBackendAuthAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('importBackendAuth', 'AmplifyBackend', result, false, true, mock)
  },
  importBackendAuthThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('importBackendAuth', 'AmplifyBackend', result, true, false, mock)
  },
  importBackendStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('importBackendStorage', 'AmplifyBackend', result, true, true, mock)
  },
  importBackendStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('importBackendStorage', 'AmplifyBackend', result, false, true, mock)
  },
  importBackendStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('importBackendStorage', 'AmplifyBackend', result, true, false, mock)
  },
  listBackendJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBackendJobs', 'AmplifyBackend', result, true, true, mock)
  },
  listBackendJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBackendJobs', 'AmplifyBackend', result, false, true, mock)
  },
  listBackendJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBackendJobs', 'AmplifyBackend', result, true, false, mock)
  },
  listS3Buckets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listS3Buckets', 'AmplifyBackend', result, true, true, mock)
  },
  listS3BucketsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listS3Buckets', 'AmplifyBackend', result, false, true, mock)
  },
  listS3BucketsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listS3Buckets', 'AmplifyBackend', result, true, false, mock)
  },
  removeAllBackends: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeAllBackends', 'AmplifyBackend', result, true, true, mock)
  },
  removeAllBackendsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeAllBackends', 'AmplifyBackend', result, false, true, mock)
  },
  removeAllBackendsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeAllBackends', 'AmplifyBackend', result, true, false, mock)
  },
  removeBackendConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeBackendConfig', 'AmplifyBackend', result, true, true, mock)
  },
  removeBackendConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeBackendConfig', 'AmplifyBackend', result, false, true, mock)
  },
  removeBackendConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeBackendConfig', 'AmplifyBackend', result, true, false, mock)
  },
  updateBackendAPI: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendAPI', 'AmplifyBackend', result, true, true, mock)
  },
  updateBackendAPIAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendAPI', 'AmplifyBackend', result, false, true, mock)
  },
  updateBackendAPIThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendAPI', 'AmplifyBackend', result, true, false, mock)
  },
  updateBackendAuth: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendAuth', 'AmplifyBackend', result, true, true, mock)
  },
  updateBackendAuthAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendAuth', 'AmplifyBackend', result, false, true, mock)
  },
  updateBackendAuthThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendAuth', 'AmplifyBackend', result, true, false, mock)
  },
  updateBackendConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendConfig', 'AmplifyBackend', result, true, true, mock)
  },
  updateBackendConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendConfig', 'AmplifyBackend', result, false, true, mock)
  },
  updateBackendConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendConfig', 'AmplifyBackend', result, true, false, mock)
  },
  updateBackendJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendJob', 'AmplifyBackend', result, true, true, mock)
  },
  updateBackendJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendJob', 'AmplifyBackend', result, false, true, mock)
  },
  updateBackendJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendJob', 'AmplifyBackend', result, true, false, mock)
  },
  updateBackendStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendStorage', 'AmplifyBackend', result, true, true, mock)
  },
  updateBackendStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendStorage', 'AmplifyBackend', result, false, true, mock)
  },
  updateBackendStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBackendStorage', 'AmplifyBackend', result, true, false, mock)
  },
}
