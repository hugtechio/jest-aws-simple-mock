
/**
* @description mocks_v2:mockMediaPackage.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockMediaPackage = {
  configureLogs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('configureLogs', 'MediaPackage', result, true, true, mock)
  },
  configureLogsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('configureLogs', 'MediaPackage', result, false, true, mock)
  },
  configureLogsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('configureLogs', 'MediaPackage', result, true, false, mock)
  },
  createChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaPackage', result, true, true, mock)
  },
  createChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaPackage', result, false, true, mock)
  },
  createChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaPackage', result, true, false, mock)
  },
  createHarvestJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createHarvestJob', 'MediaPackage', result, true, true, mock)
  },
  createHarvestJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createHarvestJob', 'MediaPackage', result, false, true, mock)
  },
  createHarvestJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createHarvestJob', 'MediaPackage', result, true, false, mock)
  },
  createOriginEndpoint: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOriginEndpoint', 'MediaPackage', result, true, true, mock)
  },
  createOriginEndpointAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOriginEndpoint', 'MediaPackage', result, false, true, mock)
  },
  createOriginEndpointThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOriginEndpoint', 'MediaPackage', result, true, false, mock)
  },
  deleteChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaPackage', result, true, true, mock)
  },
  deleteChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaPackage', result, false, true, mock)
  },
  deleteChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaPackage', result, true, false, mock)
  },
  deleteOriginEndpoint: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOriginEndpoint', 'MediaPackage', result, true, true, mock)
  },
  deleteOriginEndpointAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOriginEndpoint', 'MediaPackage', result, false, true, mock)
  },
  deleteOriginEndpointThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOriginEndpoint', 'MediaPackage', result, true, false, mock)
  },
  describeChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaPackage', result, true, true, mock)
  },
  describeChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaPackage', result, false, true, mock)
  },
  describeChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaPackage', result, true, false, mock)
  },
  describeHarvestJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeHarvestJob', 'MediaPackage', result, true, true, mock)
  },
  describeHarvestJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeHarvestJob', 'MediaPackage', result, false, true, mock)
  },
  describeHarvestJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeHarvestJob', 'MediaPackage', result, true, false, mock)
  },
  describeOriginEndpoint: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOriginEndpoint', 'MediaPackage', result, true, true, mock)
  },
  describeOriginEndpointAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOriginEndpoint', 'MediaPackage', result, false, true, mock)
  },
  describeOriginEndpointThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOriginEndpoint', 'MediaPackage', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaPackage', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaPackage', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaPackage', result, true, false, mock)
  },
  listChannels: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaPackage', result, true, true, mock)
  },
  listChannelsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaPackage', result, false, true, mock)
  },
  listChannelsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaPackage', result, true, false, mock)
  },
  listHarvestJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listHarvestJobs', 'MediaPackage', result, true, true, mock)
  },
  listHarvestJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listHarvestJobs', 'MediaPackage', result, false, true, mock)
  },
  listHarvestJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listHarvestJobs', 'MediaPackage', result, true, false, mock)
  },
  listOriginEndpoints: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOriginEndpoints', 'MediaPackage', result, true, true, mock)
  },
  listOriginEndpointsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOriginEndpoints', 'MediaPackage', result, false, true, mock)
  },
  listOriginEndpointsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOriginEndpoints', 'MediaPackage', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaPackage', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaPackage', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaPackage', result, true, false, mock)
  },
  rotateChannelCredentials: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rotateChannelCredentials', 'MediaPackage', result, true, true, mock)
  },
  rotateChannelCredentialsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rotateChannelCredentials', 'MediaPackage', result, false, true, mock)
  },
  rotateChannelCredentialsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rotateChannelCredentials', 'MediaPackage', result, true, false, mock)
  },
  rotateIngestEndpointCredentials: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rotateIngestEndpointCredentials', 'MediaPackage', result, true, true, mock)
  },
  rotateIngestEndpointCredentialsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rotateIngestEndpointCredentials', 'MediaPackage', result, false, true, mock)
  },
  rotateIngestEndpointCredentialsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rotateIngestEndpointCredentials', 'MediaPackage', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaPackage', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaPackage', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaPackage', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaPackage', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaPackage', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaPackage', result, true, false, mock)
  },
  updateChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaPackage', result, true, true, mock)
  },
  updateChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaPackage', result, false, true, mock)
  },
  updateChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaPackage', result, true, false, mock)
  },
  updateOriginEndpoint: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateOriginEndpoint', 'MediaPackage', result, true, true, mock)
  },
  updateOriginEndpointAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateOriginEndpoint', 'MediaPackage', result, false, true, mock)
  },
  updateOriginEndpointThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateOriginEndpoint', 'MediaPackage', result, true, false, mock)
  },
}
