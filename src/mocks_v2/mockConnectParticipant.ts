
/**
* @description mocks_v2:mockConnectParticipant.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockConnectParticipant = {
  completeAttachmentUpload: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('completeAttachmentUpload', 'ConnectParticipant', result, true, true, mock)
  },
  completeAttachmentUploadAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('completeAttachmentUpload', 'ConnectParticipant', result, false, true, mock)
  },
  completeAttachmentUploadThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('completeAttachmentUpload', 'ConnectParticipant', result, true, false, mock)
  },
  createParticipantConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createParticipantConnection', 'ConnectParticipant', result, true, true, mock)
  },
  createParticipantConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createParticipantConnection', 'ConnectParticipant', result, false, true, mock)
  },
  createParticipantConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createParticipantConnection', 'ConnectParticipant', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ConnectParticipant', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ConnectParticipant', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ConnectParticipant', result, true, false, mock)
  },
  disconnectParticipant: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disconnectParticipant', 'ConnectParticipant', result, true, true, mock)
  },
  disconnectParticipantAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disconnectParticipant', 'ConnectParticipant', result, false, true, mock)
  },
  disconnectParticipantThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disconnectParticipant', 'ConnectParticipant', result, true, false, mock)
  },
  getAttachment: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAttachment', 'ConnectParticipant', result, true, true, mock)
  },
  getAttachmentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAttachment', 'ConnectParticipant', result, false, true, mock)
  },
  getAttachmentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAttachment', 'ConnectParticipant', result, true, false, mock)
  },
  getTranscript: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTranscript', 'ConnectParticipant', result, true, true, mock)
  },
  getTranscriptAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTranscript', 'ConnectParticipant', result, false, true, mock)
  },
  getTranscriptThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTranscript', 'ConnectParticipant', result, true, false, mock)
  },
  sendEvent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('sendEvent', 'ConnectParticipant', result, true, true, mock)
  },
  sendEventAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('sendEvent', 'ConnectParticipant', result, false, true, mock)
  },
  sendEventThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('sendEvent', 'ConnectParticipant', result, true, false, mock)
  },
  sendMessage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('sendMessage', 'ConnectParticipant', result, true, true, mock)
  },
  sendMessageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('sendMessage', 'ConnectParticipant', result, false, true, mock)
  },
  sendMessageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('sendMessage', 'ConnectParticipant', result, true, false, mock)
  },
  startAttachmentUpload: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startAttachmentUpload', 'ConnectParticipant', result, true, true, mock)
  },
  startAttachmentUploadAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startAttachmentUpload', 'ConnectParticipant', result, false, true, mock)
  },
  startAttachmentUploadThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startAttachmentUpload', 'ConnectParticipant', result, true, false, mock)
  },
}
