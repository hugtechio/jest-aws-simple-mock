
/**
* @description mocks_v2:mockApplicationDiscoveryService.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockApplicationDiscoveryService = {
  associateConfigurationItemsToApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateConfigurationItemsToApplication', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  associateConfigurationItemsToApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateConfigurationItemsToApplication', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  associateConfigurationItemsToApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateConfigurationItemsToApplication', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  batchDeleteImportData: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteImportData', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  batchDeleteImportDataAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteImportData', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  batchDeleteImportDataThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteImportData', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  createApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplication', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  createApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplication', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  createApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplication', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  createTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTags', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  createTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTags', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  createTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTags', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  deleteApplications: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApplications', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  deleteApplicationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApplications', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  deleteApplicationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApplications', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  deleteTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTags', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  deleteTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTags', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  deleteTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTags', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  describeAgents: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAgents', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  describeAgentsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAgents', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  describeAgentsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAgents', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  describeConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfigurations', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  describeConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfigurations', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  describeConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfigurations', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  describeContinuousExports: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeContinuousExports', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  describeContinuousExportsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeContinuousExports', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  describeContinuousExportsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeContinuousExports', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  describeExportConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExportConfigurations', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  describeExportConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExportConfigurations', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  describeExportConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExportConfigurations', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  describeExportTasks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExportTasks', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  describeExportTasksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExportTasks', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  describeExportTasksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExportTasks', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  describeImportTasks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeImportTasks', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  describeImportTasksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeImportTasks', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  describeImportTasksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeImportTasks', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  describeTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTags', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  describeTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTags', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  describeTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTags', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  disassociateConfigurationItemsFromApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateConfigurationItemsFromApplication', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  disassociateConfigurationItemsFromApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateConfigurationItemsFromApplication', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  disassociateConfigurationItemsFromApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateConfigurationItemsFromApplication', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  exportConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('exportConfigurations', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  exportConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('exportConfigurations', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  exportConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('exportConfigurations', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  getDiscoverySummary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDiscoverySummary', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  getDiscoverySummaryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDiscoverySummary', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  getDiscoverySummaryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDiscoverySummary', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  listConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurations', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  listConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurations', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  listConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurations', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  listServerNeighbors: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listServerNeighbors', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  listServerNeighborsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listServerNeighbors', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  listServerNeighborsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listServerNeighbors', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  startContinuousExport: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startContinuousExport', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  startContinuousExportAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startContinuousExport', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  startContinuousExportThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startContinuousExport', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  startDataCollectionByAgentIds: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataCollectionByAgentIds', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  startDataCollectionByAgentIdsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataCollectionByAgentIds', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  startDataCollectionByAgentIdsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataCollectionByAgentIds', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  startExportTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startExportTask', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  startExportTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startExportTask', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  startExportTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startExportTask', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  startImportTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startImportTask', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  startImportTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startImportTask', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  startImportTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startImportTask', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  stopContinuousExport: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopContinuousExport', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  stopContinuousExportAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopContinuousExport', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  stopContinuousExportThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopContinuousExport', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  stopDataCollectionByAgentIds: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDataCollectionByAgentIds', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  stopDataCollectionByAgentIdsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDataCollectionByAgentIds', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  stopDataCollectionByAgentIdsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDataCollectionByAgentIds', 'ApplicationDiscoveryService', result, true, false, mock)
  },
  updateApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApplication', 'ApplicationDiscoveryService', result, true, true, mock)
  },
  updateApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApplication', 'ApplicationDiscoveryService', result, false, true, mock)
  },
  updateApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApplication', 'ApplicationDiscoveryService', result, true, false, mock)
  },
}
