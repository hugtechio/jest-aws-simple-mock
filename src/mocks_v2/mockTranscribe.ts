
/**
* @description mocks_v2:mockTranscribe.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockTranscribe = {
  createCallAnalyticsCategory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCallAnalyticsCategory', 'Transcribe', result, true, true, mock)
  },
  createCallAnalyticsCategoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCallAnalyticsCategory', 'Transcribe', result, false, true, mock)
  },
  createCallAnalyticsCategoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCallAnalyticsCategory', 'Transcribe', result, true, false, mock)
  },
  createLanguageModel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLanguageModel', 'Transcribe', result, true, true, mock)
  },
  createLanguageModelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLanguageModel', 'Transcribe', result, false, true, mock)
  },
  createLanguageModelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLanguageModel', 'Transcribe', result, true, false, mock)
  },
  createMedicalVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMedicalVocabulary', 'Transcribe', result, true, true, mock)
  },
  createMedicalVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMedicalVocabulary', 'Transcribe', result, false, true, mock)
  },
  createMedicalVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMedicalVocabulary', 'Transcribe', result, true, false, mock)
  },
  createVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVocabulary', 'Transcribe', result, true, true, mock)
  },
  createVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVocabulary', 'Transcribe', result, false, true, mock)
  },
  createVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVocabulary', 'Transcribe', result, true, false, mock)
  },
  createVocabularyFilter: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVocabularyFilter', 'Transcribe', result, true, true, mock)
  },
  createVocabularyFilterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVocabularyFilter', 'Transcribe', result, false, true, mock)
  },
  createVocabularyFilterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVocabularyFilter', 'Transcribe', result, true, false, mock)
  },
  deleteCallAnalyticsCategory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCallAnalyticsCategory', 'Transcribe', result, true, true, mock)
  },
  deleteCallAnalyticsCategoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCallAnalyticsCategory', 'Transcribe', result, false, true, mock)
  },
  deleteCallAnalyticsCategoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCallAnalyticsCategory', 'Transcribe', result, true, false, mock)
  },
  deleteCallAnalyticsJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCallAnalyticsJob', 'Transcribe', result, true, true, mock)
  },
  deleteCallAnalyticsJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCallAnalyticsJob', 'Transcribe', result, false, true, mock)
  },
  deleteCallAnalyticsJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCallAnalyticsJob', 'Transcribe', result, true, false, mock)
  },
  deleteLanguageModel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLanguageModel', 'Transcribe', result, true, true, mock)
  },
  deleteLanguageModelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLanguageModel', 'Transcribe', result, false, true, mock)
  },
  deleteLanguageModelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLanguageModel', 'Transcribe', result, true, false, mock)
  },
  deleteMedicalTranscriptionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMedicalTranscriptionJob', 'Transcribe', result, true, true, mock)
  },
  deleteMedicalTranscriptionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMedicalTranscriptionJob', 'Transcribe', result, false, true, mock)
  },
  deleteMedicalTranscriptionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMedicalTranscriptionJob', 'Transcribe', result, true, false, mock)
  },
  deleteMedicalVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMedicalVocabulary', 'Transcribe', result, true, true, mock)
  },
  deleteMedicalVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMedicalVocabulary', 'Transcribe', result, false, true, mock)
  },
  deleteMedicalVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMedicalVocabulary', 'Transcribe', result, true, false, mock)
  },
  deleteTranscriptionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTranscriptionJob', 'Transcribe', result, true, true, mock)
  },
  deleteTranscriptionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTranscriptionJob', 'Transcribe', result, false, true, mock)
  },
  deleteTranscriptionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTranscriptionJob', 'Transcribe', result, true, false, mock)
  },
  deleteVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVocabulary', 'Transcribe', result, true, true, mock)
  },
  deleteVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVocabulary', 'Transcribe', result, false, true, mock)
  },
  deleteVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVocabulary', 'Transcribe', result, true, false, mock)
  },
  deleteVocabularyFilter: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVocabularyFilter', 'Transcribe', result, true, true, mock)
  },
  deleteVocabularyFilterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVocabularyFilter', 'Transcribe', result, false, true, mock)
  },
  deleteVocabularyFilterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVocabularyFilter', 'Transcribe', result, true, false, mock)
  },
  describeLanguageModel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLanguageModel', 'Transcribe', result, true, true, mock)
  },
  describeLanguageModelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLanguageModel', 'Transcribe', result, false, true, mock)
  },
  describeLanguageModelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLanguageModel', 'Transcribe', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Transcribe', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Transcribe', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Transcribe', result, true, false, mock)
  },
  getCallAnalyticsCategory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCallAnalyticsCategory', 'Transcribe', result, true, true, mock)
  },
  getCallAnalyticsCategoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCallAnalyticsCategory', 'Transcribe', result, false, true, mock)
  },
  getCallAnalyticsCategoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCallAnalyticsCategory', 'Transcribe', result, true, false, mock)
  },
  getCallAnalyticsJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCallAnalyticsJob', 'Transcribe', result, true, true, mock)
  },
  getCallAnalyticsJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCallAnalyticsJob', 'Transcribe', result, false, true, mock)
  },
  getCallAnalyticsJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCallAnalyticsJob', 'Transcribe', result, true, false, mock)
  },
  getMedicalTranscriptionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMedicalTranscriptionJob', 'Transcribe', result, true, true, mock)
  },
  getMedicalTranscriptionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMedicalTranscriptionJob', 'Transcribe', result, false, true, mock)
  },
  getMedicalTranscriptionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMedicalTranscriptionJob', 'Transcribe', result, true, false, mock)
  },
  getMedicalVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMedicalVocabulary', 'Transcribe', result, true, true, mock)
  },
  getMedicalVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMedicalVocabulary', 'Transcribe', result, false, true, mock)
  },
  getMedicalVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMedicalVocabulary', 'Transcribe', result, true, false, mock)
  },
  getTranscriptionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTranscriptionJob', 'Transcribe', result, true, true, mock)
  },
  getTranscriptionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTranscriptionJob', 'Transcribe', result, false, true, mock)
  },
  getTranscriptionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTranscriptionJob', 'Transcribe', result, true, false, mock)
  },
  getVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getVocabulary', 'Transcribe', result, true, true, mock)
  },
  getVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getVocabulary', 'Transcribe', result, false, true, mock)
  },
  getVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getVocabulary', 'Transcribe', result, true, false, mock)
  },
  getVocabularyFilter: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getVocabularyFilter', 'Transcribe', result, true, true, mock)
  },
  getVocabularyFilterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getVocabularyFilter', 'Transcribe', result, false, true, mock)
  },
  getVocabularyFilterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getVocabularyFilter', 'Transcribe', result, true, false, mock)
  },
  listCallAnalyticsCategories: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCallAnalyticsCategories', 'Transcribe', result, true, true, mock)
  },
  listCallAnalyticsCategoriesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCallAnalyticsCategories', 'Transcribe', result, false, true, mock)
  },
  listCallAnalyticsCategoriesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCallAnalyticsCategories', 'Transcribe', result, true, false, mock)
  },
  listCallAnalyticsJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCallAnalyticsJobs', 'Transcribe', result, true, true, mock)
  },
  listCallAnalyticsJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCallAnalyticsJobs', 'Transcribe', result, false, true, mock)
  },
  listCallAnalyticsJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCallAnalyticsJobs', 'Transcribe', result, true, false, mock)
  },
  listLanguageModels: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLanguageModels', 'Transcribe', result, true, true, mock)
  },
  listLanguageModelsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLanguageModels', 'Transcribe', result, false, true, mock)
  },
  listLanguageModelsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLanguageModels', 'Transcribe', result, true, false, mock)
  },
  listMedicalTranscriptionJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMedicalTranscriptionJobs', 'Transcribe', result, true, true, mock)
  },
  listMedicalTranscriptionJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMedicalTranscriptionJobs', 'Transcribe', result, false, true, mock)
  },
  listMedicalTranscriptionJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMedicalTranscriptionJobs', 'Transcribe', result, true, false, mock)
  },
  listMedicalVocabularies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMedicalVocabularies', 'Transcribe', result, true, true, mock)
  },
  listMedicalVocabulariesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMedicalVocabularies', 'Transcribe', result, false, true, mock)
  },
  listMedicalVocabulariesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMedicalVocabularies', 'Transcribe', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Transcribe', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Transcribe', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Transcribe', result, true, false, mock)
  },
  listTranscriptionJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTranscriptionJobs', 'Transcribe', result, true, true, mock)
  },
  listTranscriptionJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTranscriptionJobs', 'Transcribe', result, false, true, mock)
  },
  listTranscriptionJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTranscriptionJobs', 'Transcribe', result, true, false, mock)
  },
  listVocabularies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVocabularies', 'Transcribe', result, true, true, mock)
  },
  listVocabulariesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVocabularies', 'Transcribe', result, false, true, mock)
  },
  listVocabulariesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVocabularies', 'Transcribe', result, true, false, mock)
  },
  listVocabularyFilters: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVocabularyFilters', 'Transcribe', result, true, true, mock)
  },
  listVocabularyFiltersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVocabularyFilters', 'Transcribe', result, false, true, mock)
  },
  listVocabularyFiltersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVocabularyFilters', 'Transcribe', result, true, false, mock)
  },
  startCallAnalyticsJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startCallAnalyticsJob', 'Transcribe', result, true, true, mock)
  },
  startCallAnalyticsJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startCallAnalyticsJob', 'Transcribe', result, false, true, mock)
  },
  startCallAnalyticsJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startCallAnalyticsJob', 'Transcribe', result, true, false, mock)
  },
  startMedicalTranscriptionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startMedicalTranscriptionJob', 'Transcribe', result, true, true, mock)
  },
  startMedicalTranscriptionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startMedicalTranscriptionJob', 'Transcribe', result, false, true, mock)
  },
  startMedicalTranscriptionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startMedicalTranscriptionJob', 'Transcribe', result, true, false, mock)
  },
  startTranscriptionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTranscriptionJob', 'Transcribe', result, true, true, mock)
  },
  startTranscriptionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTranscriptionJob', 'Transcribe', result, false, true, mock)
  },
  startTranscriptionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTranscriptionJob', 'Transcribe', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Transcribe', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Transcribe', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Transcribe', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Transcribe', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Transcribe', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Transcribe', result, true, false, mock)
  },
  updateCallAnalyticsCategory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateCallAnalyticsCategory', 'Transcribe', result, true, true, mock)
  },
  updateCallAnalyticsCategoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateCallAnalyticsCategory', 'Transcribe', result, false, true, mock)
  },
  updateCallAnalyticsCategoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateCallAnalyticsCategory', 'Transcribe', result, true, false, mock)
  },
  updateMedicalVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMedicalVocabulary', 'Transcribe', result, true, true, mock)
  },
  updateMedicalVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMedicalVocabulary', 'Transcribe', result, false, true, mock)
  },
  updateMedicalVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMedicalVocabulary', 'Transcribe', result, true, false, mock)
  },
  updateVocabulary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVocabulary', 'Transcribe', result, true, true, mock)
  },
  updateVocabularyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVocabulary', 'Transcribe', result, false, true, mock)
  },
  updateVocabularyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVocabulary', 'Transcribe', result, true, false, mock)
  },
  updateVocabularyFilter: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVocabularyFilter', 'Transcribe', result, true, true, mock)
  },
  updateVocabularyFilterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVocabularyFilter', 'Transcribe', result, false, true, mock)
  },
  updateVocabularyFilterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVocabularyFilter', 'Transcribe', result, true, false, mock)
  },
}
