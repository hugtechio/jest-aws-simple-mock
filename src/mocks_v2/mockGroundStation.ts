
/**
* @description mocks_v2:mockGroundStation.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockGroundStation = {
  cancelContact: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelContact', 'GroundStation', result, true, true, mock)
  },
  cancelContactAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelContact', 'GroundStation', result, false, true, mock)
  },
  cancelContactThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelContact', 'GroundStation', result, true, false, mock)
  },
  createConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConfig', 'GroundStation', result, true, true, mock)
  },
  createConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConfig', 'GroundStation', result, false, true, mock)
  },
  createConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConfig', 'GroundStation', result, true, false, mock)
  },
  createDataflowEndpointGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataflowEndpointGroup', 'GroundStation', result, true, true, mock)
  },
  createDataflowEndpointGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataflowEndpointGroup', 'GroundStation', result, false, true, mock)
  },
  createDataflowEndpointGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataflowEndpointGroup', 'GroundStation', result, true, false, mock)
  },
  createMissionProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMissionProfile', 'GroundStation', result, true, true, mock)
  },
  createMissionProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMissionProfile', 'GroundStation', result, false, true, mock)
  },
  createMissionProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMissionProfile', 'GroundStation', result, true, false, mock)
  },
  deleteConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConfig', 'GroundStation', result, true, true, mock)
  },
  deleteConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConfig', 'GroundStation', result, false, true, mock)
  },
  deleteConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConfig', 'GroundStation', result, true, false, mock)
  },
  deleteDataflowEndpointGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataflowEndpointGroup', 'GroundStation', result, true, true, mock)
  },
  deleteDataflowEndpointGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataflowEndpointGroup', 'GroundStation', result, false, true, mock)
  },
  deleteDataflowEndpointGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataflowEndpointGroup', 'GroundStation', result, true, false, mock)
  },
  deleteMissionProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMissionProfile', 'GroundStation', result, true, true, mock)
  },
  deleteMissionProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMissionProfile', 'GroundStation', result, false, true, mock)
  },
  deleteMissionProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMissionProfile', 'GroundStation', result, true, false, mock)
  },
  describeContact: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeContact', 'GroundStation', result, true, true, mock)
  },
  describeContactAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeContact', 'GroundStation', result, false, true, mock)
  },
  describeContactThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeContact', 'GroundStation', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'GroundStation', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'GroundStation', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'GroundStation', result, true, false, mock)
  },
  getConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getConfig', 'GroundStation', result, true, true, mock)
  },
  getConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getConfig', 'GroundStation', result, false, true, mock)
  },
  getConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getConfig', 'GroundStation', result, true, false, mock)
  },
  getDataflowEndpointGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataflowEndpointGroup', 'GroundStation', result, true, true, mock)
  },
  getDataflowEndpointGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataflowEndpointGroup', 'GroundStation', result, false, true, mock)
  },
  getDataflowEndpointGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataflowEndpointGroup', 'GroundStation', result, true, false, mock)
  },
  getMinuteUsage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMinuteUsage', 'GroundStation', result, true, true, mock)
  },
  getMinuteUsageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMinuteUsage', 'GroundStation', result, false, true, mock)
  },
  getMinuteUsageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMinuteUsage', 'GroundStation', result, true, false, mock)
  },
  getMissionProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMissionProfile', 'GroundStation', result, true, true, mock)
  },
  getMissionProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMissionProfile', 'GroundStation', result, false, true, mock)
  },
  getMissionProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMissionProfile', 'GroundStation', result, true, false, mock)
  },
  getSatellite: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSatellite', 'GroundStation', result, true, true, mock)
  },
  getSatelliteAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSatellite', 'GroundStation', result, false, true, mock)
  },
  getSatelliteThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSatellite', 'GroundStation', result, true, false, mock)
  },
  listConfigs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigs', 'GroundStation', result, true, true, mock)
  },
  listConfigsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigs', 'GroundStation', result, false, true, mock)
  },
  listConfigsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigs', 'GroundStation', result, true, false, mock)
  },
  listContacts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listContacts', 'GroundStation', result, true, true, mock)
  },
  listContactsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listContacts', 'GroundStation', result, false, true, mock)
  },
  listContactsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listContacts', 'GroundStation', result, true, false, mock)
  },
  listDataflowEndpointGroups: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataflowEndpointGroups', 'GroundStation', result, true, true, mock)
  },
  listDataflowEndpointGroupsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataflowEndpointGroups', 'GroundStation', result, false, true, mock)
  },
  listDataflowEndpointGroupsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataflowEndpointGroups', 'GroundStation', result, true, false, mock)
  },
  listGroundStations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGroundStations', 'GroundStation', result, true, true, mock)
  },
  listGroundStationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGroundStations', 'GroundStation', result, false, true, mock)
  },
  listGroundStationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGroundStations', 'GroundStation', result, true, false, mock)
  },
  listMissionProfiles: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMissionProfiles', 'GroundStation', result, true, true, mock)
  },
  listMissionProfilesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMissionProfiles', 'GroundStation', result, false, true, mock)
  },
  listMissionProfilesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMissionProfiles', 'GroundStation', result, true, false, mock)
  },
  listSatellites: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSatellites', 'GroundStation', result, true, true, mock)
  },
  listSatellitesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSatellites', 'GroundStation', result, false, true, mock)
  },
  listSatellitesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSatellites', 'GroundStation', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'GroundStation', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'GroundStation', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'GroundStation', result, true, false, mock)
  },
  reserveContact: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reserveContact', 'GroundStation', result, true, true, mock)
  },
  reserveContactAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reserveContact', 'GroundStation', result, false, true, mock)
  },
  reserveContactThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('reserveContact', 'GroundStation', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'GroundStation', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'GroundStation', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'GroundStation', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'GroundStation', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'GroundStation', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'GroundStation', result, true, false, mock)
  },
  updateConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConfig', 'GroundStation', result, true, true, mock)
  },
  updateConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConfig', 'GroundStation', result, false, true, mock)
  },
  updateConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConfig', 'GroundStation', result, true, false, mock)
  },
  updateMissionProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMissionProfile', 'GroundStation', result, true, true, mock)
  },
  updateMissionProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMissionProfile', 'GroundStation', result, false, true, mock)
  },
  updateMissionProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMissionProfile', 'GroundStation', result, true, false, mock)
  },
}
