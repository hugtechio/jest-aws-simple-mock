
/**
* @description mocks_v2:mockOutposts.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockOutposts = {
  createOrder: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOrder', 'Outposts', result, true, true, mock)
  },
  createOrderAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOrder', 'Outposts', result, false, true, mock)
  },
  createOrderThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOrder', 'Outposts', result, true, false, mock)
  },
  createOutpost: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOutpost', 'Outposts', result, true, true, mock)
  },
  createOutpostAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOutpost', 'Outposts', result, false, true, mock)
  },
  createOutpostThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOutpost', 'Outposts', result, true, false, mock)
  },
  deleteOutpost: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOutpost', 'Outposts', result, true, true, mock)
  },
  deleteOutpostAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOutpost', 'Outposts', result, false, true, mock)
  },
  deleteOutpostThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOutpost', 'Outposts', result, true, false, mock)
  },
  deleteSite: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSite', 'Outposts', result, true, true, mock)
  },
  deleteSiteAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSite', 'Outposts', result, false, true, mock)
  },
  deleteSiteThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSite', 'Outposts', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Outposts', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Outposts', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Outposts', result, true, false, mock)
  },
  getOutpost: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getOutpost', 'Outposts', result, true, true, mock)
  },
  getOutpostAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getOutpost', 'Outposts', result, false, true, mock)
  },
  getOutpostThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getOutpost', 'Outposts', result, true, false, mock)
  },
  getOutpostInstanceTypes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getOutpostInstanceTypes', 'Outposts', result, true, true, mock)
  },
  getOutpostInstanceTypesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getOutpostInstanceTypes', 'Outposts', result, false, true, mock)
  },
  getOutpostInstanceTypesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getOutpostInstanceTypes', 'Outposts', result, true, false, mock)
  },
  listOutposts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOutposts', 'Outposts', result, true, true, mock)
  },
  listOutpostsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOutposts', 'Outposts', result, false, true, mock)
  },
  listOutpostsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOutposts', 'Outposts', result, true, false, mock)
  },
  listSites: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSites', 'Outposts', result, true, true, mock)
  },
  listSitesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSites', 'Outposts', result, false, true, mock)
  },
  listSitesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSites', 'Outposts', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Outposts', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Outposts', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Outposts', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Outposts', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Outposts', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Outposts', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Outposts', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Outposts', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Outposts', result, true, false, mock)
  },
}
