
/**
* @description mocks_v2:mockMediaConnect.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockMediaConnect = {
  addFlowMediaStreams: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowMediaStreams', 'MediaConnect', result, true, true, mock)
  },
  addFlowMediaStreamsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowMediaStreams', 'MediaConnect', result, false, true, mock)
  },
  addFlowMediaStreamsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowMediaStreams', 'MediaConnect', result, true, false, mock)
  },
  addFlowOutputs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowOutputs', 'MediaConnect', result, true, true, mock)
  },
  addFlowOutputsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowOutputs', 'MediaConnect', result, false, true, mock)
  },
  addFlowOutputsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowOutputs', 'MediaConnect', result, true, false, mock)
  },
  addFlowSources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowSources', 'MediaConnect', result, true, true, mock)
  },
  addFlowSourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowSources', 'MediaConnect', result, false, true, mock)
  },
  addFlowSourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowSources', 'MediaConnect', result, true, false, mock)
  },
  addFlowVpcInterfaces: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowVpcInterfaces', 'MediaConnect', result, true, true, mock)
  },
  addFlowVpcInterfacesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowVpcInterfaces', 'MediaConnect', result, false, true, mock)
  },
  addFlowVpcInterfacesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addFlowVpcInterfaces', 'MediaConnect', result, true, false, mock)
  },
  createFlow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFlow', 'MediaConnect', result, true, true, mock)
  },
  createFlowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFlow', 'MediaConnect', result, false, true, mock)
  },
  createFlowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFlow', 'MediaConnect', result, true, false, mock)
  },
  deleteFlow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFlow', 'MediaConnect', result, true, true, mock)
  },
  deleteFlowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFlow', 'MediaConnect', result, false, true, mock)
  },
  deleteFlowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFlow', 'MediaConnect', result, true, false, mock)
  },
  describeFlow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFlow', 'MediaConnect', result, true, true, mock)
  },
  describeFlowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFlow', 'MediaConnect', result, false, true, mock)
  },
  describeFlowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFlow', 'MediaConnect', result, true, false, mock)
  },
  describeOffering: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOffering', 'MediaConnect', result, true, true, mock)
  },
  describeOfferingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOffering', 'MediaConnect', result, false, true, mock)
  },
  describeOfferingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOffering', 'MediaConnect', result, true, false, mock)
  },
  describeReservation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservation', 'MediaConnect', result, true, true, mock)
  },
  describeReservationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservation', 'MediaConnect', result, false, true, mock)
  },
  describeReservationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservation', 'MediaConnect', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaConnect', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaConnect', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaConnect', result, true, false, mock)
  },
  grantFlowEntitlements: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('grantFlowEntitlements', 'MediaConnect', result, true, true, mock)
  },
  grantFlowEntitlementsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('grantFlowEntitlements', 'MediaConnect', result, false, true, mock)
  },
  grantFlowEntitlementsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('grantFlowEntitlements', 'MediaConnect', result, true, false, mock)
  },
  listEntitlements: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEntitlements', 'MediaConnect', result, true, true, mock)
  },
  listEntitlementsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEntitlements', 'MediaConnect', result, false, true, mock)
  },
  listEntitlementsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEntitlements', 'MediaConnect', result, true, false, mock)
  },
  listFlows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFlows', 'MediaConnect', result, true, true, mock)
  },
  listFlowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFlows', 'MediaConnect', result, false, true, mock)
  },
  listFlowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFlows', 'MediaConnect', result, true, false, mock)
  },
  listOfferings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOfferings', 'MediaConnect', result, true, true, mock)
  },
  listOfferingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOfferings', 'MediaConnect', result, false, true, mock)
  },
  listOfferingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOfferings', 'MediaConnect', result, true, false, mock)
  },
  listReservations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReservations', 'MediaConnect', result, true, true, mock)
  },
  listReservationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReservations', 'MediaConnect', result, false, true, mock)
  },
  listReservationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReservations', 'MediaConnect', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaConnect', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaConnect', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaConnect', result, true, false, mock)
  },
  purchaseOffering: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseOffering', 'MediaConnect', result, true, true, mock)
  },
  purchaseOfferingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseOffering', 'MediaConnect', result, false, true, mock)
  },
  purchaseOfferingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseOffering', 'MediaConnect', result, true, false, mock)
  },
  removeFlowMediaStream: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowMediaStream', 'MediaConnect', result, true, true, mock)
  },
  removeFlowMediaStreamAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowMediaStream', 'MediaConnect', result, false, true, mock)
  },
  removeFlowMediaStreamThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowMediaStream', 'MediaConnect', result, true, false, mock)
  },
  removeFlowOutput: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowOutput', 'MediaConnect', result, true, true, mock)
  },
  removeFlowOutputAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowOutput', 'MediaConnect', result, false, true, mock)
  },
  removeFlowOutputThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowOutput', 'MediaConnect', result, true, false, mock)
  },
  removeFlowSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowSource', 'MediaConnect', result, true, true, mock)
  },
  removeFlowSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowSource', 'MediaConnect', result, false, true, mock)
  },
  removeFlowSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowSource', 'MediaConnect', result, true, false, mock)
  },
  removeFlowVpcInterface: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowVpcInterface', 'MediaConnect', result, true, true, mock)
  },
  removeFlowVpcInterfaceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowVpcInterface', 'MediaConnect', result, false, true, mock)
  },
  removeFlowVpcInterfaceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeFlowVpcInterface', 'MediaConnect', result, true, false, mock)
  },
  revokeFlowEntitlement: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeFlowEntitlement', 'MediaConnect', result, true, true, mock)
  },
  revokeFlowEntitlementAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeFlowEntitlement', 'MediaConnect', result, false, true, mock)
  },
  revokeFlowEntitlementThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeFlowEntitlement', 'MediaConnect', result, true, false, mock)
  },
  startFlow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startFlow', 'MediaConnect', result, true, true, mock)
  },
  startFlowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startFlow', 'MediaConnect', result, false, true, mock)
  },
  startFlowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startFlow', 'MediaConnect', result, true, false, mock)
  },
  stopFlow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopFlow', 'MediaConnect', result, true, true, mock)
  },
  stopFlowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopFlow', 'MediaConnect', result, false, true, mock)
  },
  stopFlowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopFlow', 'MediaConnect', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaConnect', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaConnect', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaConnect', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaConnect', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaConnect', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaConnect', result, true, false, mock)
  },
  updateFlow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlow', 'MediaConnect', result, true, true, mock)
  },
  updateFlowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlow', 'MediaConnect', result, false, true, mock)
  },
  updateFlowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlow', 'MediaConnect', result, true, false, mock)
  },
  updateFlowEntitlement: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowEntitlement', 'MediaConnect', result, true, true, mock)
  },
  updateFlowEntitlementAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowEntitlement', 'MediaConnect', result, false, true, mock)
  },
  updateFlowEntitlementThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowEntitlement', 'MediaConnect', result, true, false, mock)
  },
  updateFlowMediaStream: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowMediaStream', 'MediaConnect', result, true, true, mock)
  },
  updateFlowMediaStreamAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowMediaStream', 'MediaConnect', result, false, true, mock)
  },
  updateFlowMediaStreamThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowMediaStream', 'MediaConnect', result, true, false, mock)
  },
  updateFlowOutput: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowOutput', 'MediaConnect', result, true, true, mock)
  },
  updateFlowOutputAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowOutput', 'MediaConnect', result, false, true, mock)
  },
  updateFlowOutputThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowOutput', 'MediaConnect', result, true, false, mock)
  },
  updateFlowSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowSource', 'MediaConnect', result, true, true, mock)
  },
  updateFlowSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowSource', 'MediaConnect', result, false, true, mock)
  },
  updateFlowSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFlowSource', 'MediaConnect', result, true, false, mock)
  },
}
