
/**
* @description mocks_v2:mockIotDeviceAdvisor.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockIotDeviceAdvisor = {
  createSuiteDefinition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSuiteDefinition', 'IotDeviceAdvisor', result, true, true, mock)
  },
  createSuiteDefinitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSuiteDefinition', 'IotDeviceAdvisor', result, false, true, mock)
  },
  createSuiteDefinitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSuiteDefinition', 'IotDeviceAdvisor', result, true, false, mock)
  },
  deleteSuiteDefinition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSuiteDefinition', 'IotDeviceAdvisor', result, true, true, mock)
  },
  deleteSuiteDefinitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSuiteDefinition', 'IotDeviceAdvisor', result, false, true, mock)
  },
  deleteSuiteDefinitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSuiteDefinition', 'IotDeviceAdvisor', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IotDeviceAdvisor', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IotDeviceAdvisor', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IotDeviceAdvisor', result, true, false, mock)
  },
  getSuiteDefinition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteDefinition', 'IotDeviceAdvisor', result, true, true, mock)
  },
  getSuiteDefinitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteDefinition', 'IotDeviceAdvisor', result, false, true, mock)
  },
  getSuiteDefinitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteDefinition', 'IotDeviceAdvisor', result, true, false, mock)
  },
  getSuiteRun: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteRun', 'IotDeviceAdvisor', result, true, true, mock)
  },
  getSuiteRunAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteRun', 'IotDeviceAdvisor', result, false, true, mock)
  },
  getSuiteRunThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteRun', 'IotDeviceAdvisor', result, true, false, mock)
  },
  getSuiteRunReport: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteRunReport', 'IotDeviceAdvisor', result, true, true, mock)
  },
  getSuiteRunReportAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteRunReport', 'IotDeviceAdvisor', result, false, true, mock)
  },
  getSuiteRunReportThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSuiteRunReport', 'IotDeviceAdvisor', result, true, false, mock)
  },
  listSuiteDefinitions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSuiteDefinitions', 'IotDeviceAdvisor', result, true, true, mock)
  },
  listSuiteDefinitionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSuiteDefinitions', 'IotDeviceAdvisor', result, false, true, mock)
  },
  listSuiteDefinitionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSuiteDefinitions', 'IotDeviceAdvisor', result, true, false, mock)
  },
  listSuiteRuns: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSuiteRuns', 'IotDeviceAdvisor', result, true, true, mock)
  },
  listSuiteRunsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSuiteRuns', 'IotDeviceAdvisor', result, false, true, mock)
  },
  listSuiteRunsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSuiteRuns', 'IotDeviceAdvisor', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'IotDeviceAdvisor', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'IotDeviceAdvisor', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'IotDeviceAdvisor', result, true, false, mock)
  },
  startSuiteRun: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSuiteRun', 'IotDeviceAdvisor', result, true, true, mock)
  },
  startSuiteRunAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSuiteRun', 'IotDeviceAdvisor', result, false, true, mock)
  },
  startSuiteRunThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSuiteRun', 'IotDeviceAdvisor', result, true, false, mock)
  },
  stopSuiteRun: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopSuiteRun', 'IotDeviceAdvisor', result, true, true, mock)
  },
  stopSuiteRunAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopSuiteRun', 'IotDeviceAdvisor', result, false, true, mock)
  },
  stopSuiteRunThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopSuiteRun', 'IotDeviceAdvisor', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'IotDeviceAdvisor', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'IotDeviceAdvisor', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'IotDeviceAdvisor', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'IotDeviceAdvisor', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'IotDeviceAdvisor', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'IotDeviceAdvisor', result, true, false, mock)
  },
  updateSuiteDefinition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSuiteDefinition', 'IotDeviceAdvisor', result, true, true, mock)
  },
  updateSuiteDefinitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSuiteDefinition', 'IotDeviceAdvisor', result, false, true, mock)
  },
  updateSuiteDefinitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSuiteDefinition', 'IotDeviceAdvisor', result, true, false, mock)
  },
}
