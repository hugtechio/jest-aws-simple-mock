
/**
* @description mocks_v2:mockLookoutEquipment.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockLookoutEquipment = {
  createDataset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataset', 'LookoutEquipment', result, true, true, mock)
  },
  createDatasetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataset', 'LookoutEquipment', result, false, true, mock)
  },
  createDatasetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataset', 'LookoutEquipment', result, true, false, mock)
  },
  createInferenceScheduler: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInferenceScheduler', 'LookoutEquipment', result, true, true, mock)
  },
  createInferenceSchedulerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInferenceScheduler', 'LookoutEquipment', result, false, true, mock)
  },
  createInferenceSchedulerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInferenceScheduler', 'LookoutEquipment', result, true, false, mock)
  },
  createModel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createModel', 'LookoutEquipment', result, true, true, mock)
  },
  createModelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createModel', 'LookoutEquipment', result, false, true, mock)
  },
  createModelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createModel', 'LookoutEquipment', result, true, false, mock)
  },
  deleteDataset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataset', 'LookoutEquipment', result, true, true, mock)
  },
  deleteDatasetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataset', 'LookoutEquipment', result, false, true, mock)
  },
  deleteDatasetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataset', 'LookoutEquipment', result, true, false, mock)
  },
  deleteInferenceScheduler: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInferenceScheduler', 'LookoutEquipment', result, true, true, mock)
  },
  deleteInferenceSchedulerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInferenceScheduler', 'LookoutEquipment', result, false, true, mock)
  },
  deleteInferenceSchedulerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInferenceScheduler', 'LookoutEquipment', result, true, false, mock)
  },
  deleteModel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteModel', 'LookoutEquipment', result, true, true, mock)
  },
  deleteModelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteModel', 'LookoutEquipment', result, false, true, mock)
  },
  deleteModelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteModel', 'LookoutEquipment', result, true, false, mock)
  },
  describeDataIngestionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataIngestionJob', 'LookoutEquipment', result, true, true, mock)
  },
  describeDataIngestionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataIngestionJob', 'LookoutEquipment', result, false, true, mock)
  },
  describeDataIngestionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataIngestionJob', 'LookoutEquipment', result, true, false, mock)
  },
  describeDataset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataset', 'LookoutEquipment', result, true, true, mock)
  },
  describeDatasetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataset', 'LookoutEquipment', result, false, true, mock)
  },
  describeDatasetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataset', 'LookoutEquipment', result, true, false, mock)
  },
  describeInferenceScheduler: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInferenceScheduler', 'LookoutEquipment', result, true, true, mock)
  },
  describeInferenceSchedulerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInferenceScheduler', 'LookoutEquipment', result, false, true, mock)
  },
  describeInferenceSchedulerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInferenceScheduler', 'LookoutEquipment', result, true, false, mock)
  },
  describeModel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeModel', 'LookoutEquipment', result, true, true, mock)
  },
  describeModelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeModel', 'LookoutEquipment', result, false, true, mock)
  },
  describeModelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeModel', 'LookoutEquipment', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LookoutEquipment', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LookoutEquipment', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LookoutEquipment', result, true, false, mock)
  },
  listDataIngestionJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataIngestionJobs', 'LookoutEquipment', result, true, true, mock)
  },
  listDataIngestionJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataIngestionJobs', 'LookoutEquipment', result, false, true, mock)
  },
  listDataIngestionJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataIngestionJobs', 'LookoutEquipment', result, true, false, mock)
  },
  listDatasets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasets', 'LookoutEquipment', result, true, true, mock)
  },
  listDatasetsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasets', 'LookoutEquipment', result, false, true, mock)
  },
  listDatasetsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasets', 'LookoutEquipment', result, true, false, mock)
  },
  listInferenceExecutions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInferenceExecutions', 'LookoutEquipment', result, true, true, mock)
  },
  listInferenceExecutionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInferenceExecutions', 'LookoutEquipment', result, false, true, mock)
  },
  listInferenceExecutionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInferenceExecutions', 'LookoutEquipment', result, true, false, mock)
  },
  listInferenceSchedulers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInferenceSchedulers', 'LookoutEquipment', result, true, true, mock)
  },
  listInferenceSchedulersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInferenceSchedulers', 'LookoutEquipment', result, false, true, mock)
  },
  listInferenceSchedulersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInferenceSchedulers', 'LookoutEquipment', result, true, false, mock)
  },
  listModels: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listModels', 'LookoutEquipment', result, true, true, mock)
  },
  listModelsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listModels', 'LookoutEquipment', result, false, true, mock)
  },
  listModelsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listModels', 'LookoutEquipment', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'LookoutEquipment', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'LookoutEquipment', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'LookoutEquipment', result, true, false, mock)
  },
  startDataIngestionJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataIngestionJob', 'LookoutEquipment', result, true, true, mock)
  },
  startDataIngestionJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataIngestionJob', 'LookoutEquipment', result, false, true, mock)
  },
  startDataIngestionJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataIngestionJob', 'LookoutEquipment', result, true, false, mock)
  },
  startInferenceScheduler: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startInferenceScheduler', 'LookoutEquipment', result, true, true, mock)
  },
  startInferenceSchedulerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startInferenceScheduler', 'LookoutEquipment', result, false, true, mock)
  },
  startInferenceSchedulerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startInferenceScheduler', 'LookoutEquipment', result, true, false, mock)
  },
  stopInferenceScheduler: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopInferenceScheduler', 'LookoutEquipment', result, true, true, mock)
  },
  stopInferenceSchedulerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopInferenceScheduler', 'LookoutEquipment', result, false, true, mock)
  },
  stopInferenceSchedulerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopInferenceScheduler', 'LookoutEquipment', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'LookoutEquipment', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'LookoutEquipment', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'LookoutEquipment', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'LookoutEquipment', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'LookoutEquipment', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'LookoutEquipment', result, true, false, mock)
  },
  updateInferenceScheduler: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInferenceScheduler', 'LookoutEquipment', result, true, true, mock)
  },
  updateInferenceSchedulerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInferenceScheduler', 'LookoutEquipment', result, false, true, mock)
  },
  updateInferenceSchedulerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInferenceScheduler', 'LookoutEquipment', result, true, false, mock)
  },
}
