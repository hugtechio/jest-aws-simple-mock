
/**
* @description mocks_v2:mockMgn.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockMgn = {
  changeServerLifeCycleState: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('changeServerLifeCycleState', 'Mgn', result, true, true, mock)
  },
  changeServerLifeCycleStateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('changeServerLifeCycleState', 'Mgn', result, false, true, mock)
  },
  changeServerLifeCycleStateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('changeServerLifeCycleState', 'Mgn', result, true, false, mock)
  },
  createReplicationConfigurationTemplate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createReplicationConfigurationTemplate', 'Mgn', result, true, true, mock)
  },
  createReplicationConfigurationTemplateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createReplicationConfigurationTemplate', 'Mgn', result, false, true, mock)
  },
  createReplicationConfigurationTemplateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createReplicationConfigurationTemplate', 'Mgn', result, true, false, mock)
  },
  deleteJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteJob', 'Mgn', result, true, true, mock)
  },
  deleteJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteJob', 'Mgn', result, false, true, mock)
  },
  deleteJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteJob', 'Mgn', result, true, false, mock)
  },
  deleteReplicationConfigurationTemplate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReplicationConfigurationTemplate', 'Mgn', result, true, true, mock)
  },
  deleteReplicationConfigurationTemplateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReplicationConfigurationTemplate', 'Mgn', result, false, true, mock)
  },
  deleteReplicationConfigurationTemplateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReplicationConfigurationTemplate', 'Mgn', result, true, false, mock)
  },
  deleteSourceServer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSourceServer', 'Mgn', result, true, true, mock)
  },
  deleteSourceServerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSourceServer', 'Mgn', result, false, true, mock)
  },
  deleteSourceServerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSourceServer', 'Mgn', result, true, false, mock)
  },
  describeJobLogItems: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeJobLogItems', 'Mgn', result, true, true, mock)
  },
  describeJobLogItemsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeJobLogItems', 'Mgn', result, false, true, mock)
  },
  describeJobLogItemsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeJobLogItems', 'Mgn', result, true, false, mock)
  },
  describeJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeJobs', 'Mgn', result, true, true, mock)
  },
  describeJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeJobs', 'Mgn', result, false, true, mock)
  },
  describeJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeJobs', 'Mgn', result, true, false, mock)
  },
  describeReplicationConfigurationTemplates: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReplicationConfigurationTemplates', 'Mgn', result, true, true, mock)
  },
  describeReplicationConfigurationTemplatesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReplicationConfigurationTemplates', 'Mgn', result, false, true, mock)
  },
  describeReplicationConfigurationTemplatesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReplicationConfigurationTemplates', 'Mgn', result, true, false, mock)
  },
  describeSourceServers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSourceServers', 'Mgn', result, true, true, mock)
  },
  describeSourceServersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSourceServers', 'Mgn', result, false, true, mock)
  },
  describeSourceServersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSourceServers', 'Mgn', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Mgn', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Mgn', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Mgn', result, true, false, mock)
  },
  disconnectFromService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disconnectFromService', 'Mgn', result, true, true, mock)
  },
  disconnectFromServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disconnectFromService', 'Mgn', result, false, true, mock)
  },
  disconnectFromServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disconnectFromService', 'Mgn', result, true, false, mock)
  },
  finalizeCutover: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('finalizeCutover', 'Mgn', result, true, true, mock)
  },
  finalizeCutoverAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('finalizeCutover', 'Mgn', result, false, true, mock)
  },
  finalizeCutoverThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('finalizeCutover', 'Mgn', result, true, false, mock)
  },
  getLaunchConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLaunchConfiguration', 'Mgn', result, true, true, mock)
  },
  getLaunchConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLaunchConfiguration', 'Mgn', result, false, true, mock)
  },
  getLaunchConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLaunchConfiguration', 'Mgn', result, true, false, mock)
  },
  getReplicationConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getReplicationConfiguration', 'Mgn', result, true, true, mock)
  },
  getReplicationConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getReplicationConfiguration', 'Mgn', result, false, true, mock)
  },
  getReplicationConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getReplicationConfiguration', 'Mgn', result, true, false, mock)
  },
  initializeService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('initializeService', 'Mgn', result, true, true, mock)
  },
  initializeServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('initializeService', 'Mgn', result, false, true, mock)
  },
  initializeServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('initializeService', 'Mgn', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Mgn', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Mgn', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Mgn', result, true, false, mock)
  },
  markAsArchived: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('markAsArchived', 'Mgn', result, true, true, mock)
  },
  markAsArchivedAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('markAsArchived', 'Mgn', result, false, true, mock)
  },
  markAsArchivedThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('markAsArchived', 'Mgn', result, true, false, mock)
  },
  retryDataReplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retryDataReplication', 'Mgn', result, true, true, mock)
  },
  retryDataReplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retryDataReplication', 'Mgn', result, false, true, mock)
  },
  retryDataReplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retryDataReplication', 'Mgn', result, true, false, mock)
  },
  startCutover: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startCutover', 'Mgn', result, true, true, mock)
  },
  startCutoverAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startCutover', 'Mgn', result, false, true, mock)
  },
  startCutoverThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startCutover', 'Mgn', result, true, false, mock)
  },
  startTest: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTest', 'Mgn', result, true, true, mock)
  },
  startTestAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTest', 'Mgn', result, false, true, mock)
  },
  startTestThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTest', 'Mgn', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Mgn', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Mgn', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Mgn', result, true, false, mock)
  },
  terminateTargetInstances: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('terminateTargetInstances', 'Mgn', result, true, true, mock)
  },
  terminateTargetInstancesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('terminateTargetInstances', 'Mgn', result, false, true, mock)
  },
  terminateTargetInstancesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('terminateTargetInstances', 'Mgn', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Mgn', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Mgn', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Mgn', result, true, false, mock)
  },
  updateLaunchConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLaunchConfiguration', 'Mgn', result, true, true, mock)
  },
  updateLaunchConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLaunchConfiguration', 'Mgn', result, false, true, mock)
  },
  updateLaunchConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLaunchConfiguration', 'Mgn', result, true, false, mock)
  },
  updateReplicationConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationConfiguration', 'Mgn', result, true, true, mock)
  },
  updateReplicationConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationConfiguration', 'Mgn', result, false, true, mock)
  },
  updateReplicationConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationConfiguration', 'Mgn', result, true, false, mock)
  },
  updateReplicationConfigurationTemplate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationConfigurationTemplate', 'Mgn', result, true, true, mock)
  },
  updateReplicationConfigurationTemplateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationConfigurationTemplate', 'Mgn', result, false, true, mock)
  },
  updateReplicationConfigurationTemplateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationConfigurationTemplate', 'Mgn', result, true, false, mock)
  },
}
