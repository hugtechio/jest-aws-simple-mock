
/**
* @description mocks_v2:mockSigner.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockSigner = {
  addProfilePermission: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addProfilePermission', 'Signer', result, true, true, mock)
  },
  addProfilePermissionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addProfilePermission', 'Signer', result, false, true, mock)
  },
  addProfilePermissionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addProfilePermission', 'Signer', result, true, false, mock)
  },
  cancelSigningProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelSigningProfile', 'Signer', result, true, true, mock)
  },
  cancelSigningProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelSigningProfile', 'Signer', result, false, true, mock)
  },
  cancelSigningProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelSigningProfile', 'Signer', result, true, false, mock)
  },
  describeSigningJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSigningJob', 'Signer', result, true, true, mock)
  },
  describeSigningJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSigningJob', 'Signer', result, false, true, mock)
  },
  describeSigningJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSigningJob', 'Signer', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Signer', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Signer', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Signer', result, true, false, mock)
  },
  getSigningPlatform: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSigningPlatform', 'Signer', result, true, true, mock)
  },
  getSigningPlatformAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSigningPlatform', 'Signer', result, false, true, mock)
  },
  getSigningPlatformThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSigningPlatform', 'Signer', result, true, false, mock)
  },
  getSigningProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSigningProfile', 'Signer', result, true, true, mock)
  },
  getSigningProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSigningProfile', 'Signer', result, false, true, mock)
  },
  getSigningProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSigningProfile', 'Signer', result, true, false, mock)
  },
  listProfilePermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfilePermissions', 'Signer', result, true, true, mock)
  },
  listProfilePermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfilePermissions', 'Signer', result, false, true, mock)
  },
  listProfilePermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfilePermissions', 'Signer', result, true, false, mock)
  },
  listSigningJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningJobs', 'Signer', result, true, true, mock)
  },
  listSigningJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningJobs', 'Signer', result, false, true, mock)
  },
  listSigningJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningJobs', 'Signer', result, true, false, mock)
  },
  listSigningPlatforms: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningPlatforms', 'Signer', result, true, true, mock)
  },
  listSigningPlatformsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningPlatforms', 'Signer', result, false, true, mock)
  },
  listSigningPlatformsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningPlatforms', 'Signer', result, true, false, mock)
  },
  listSigningProfiles: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningProfiles', 'Signer', result, true, true, mock)
  },
  listSigningProfilesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningProfiles', 'Signer', result, false, true, mock)
  },
  listSigningProfilesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSigningProfiles', 'Signer', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Signer', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Signer', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Signer', result, true, false, mock)
  },
  putSigningProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putSigningProfile', 'Signer', result, true, true, mock)
  },
  putSigningProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putSigningProfile', 'Signer', result, false, true, mock)
  },
  putSigningProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putSigningProfile', 'Signer', result, true, false, mock)
  },
  removeProfilePermission: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeProfilePermission', 'Signer', result, true, true, mock)
  },
  removeProfilePermissionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeProfilePermission', 'Signer', result, false, true, mock)
  },
  removeProfilePermissionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeProfilePermission', 'Signer', result, true, false, mock)
  },
  revokeSignature: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeSignature', 'Signer', result, true, true, mock)
  },
  revokeSignatureAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeSignature', 'Signer', result, false, true, mock)
  },
  revokeSignatureThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeSignature', 'Signer', result, true, false, mock)
  },
  revokeSigningProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeSigningProfile', 'Signer', result, true, true, mock)
  },
  revokeSigningProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeSigningProfile', 'Signer', result, false, true, mock)
  },
  revokeSigningProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokeSigningProfile', 'Signer', result, true, false, mock)
  },
  startSigningJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSigningJob', 'Signer', result, true, true, mock)
  },
  startSigningJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSigningJob', 'Signer', result, false, true, mock)
  },
  startSigningJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSigningJob', 'Signer', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Signer', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Signer', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Signer', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Signer', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Signer', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Signer', result, true, false, mock)
  },
}
