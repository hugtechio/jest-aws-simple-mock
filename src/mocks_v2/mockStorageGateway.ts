
/**
* @description mocks_v2:mockStorageGateway.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockStorageGateway = {
  activateGateway: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activateGateway', 'StorageGateway', result, true, true, mock)
  },
  activateGatewayAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activateGateway', 'StorageGateway', result, false, true, mock)
  },
  activateGatewayThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activateGateway', 'StorageGateway', result, true, false, mock)
  },
  addCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addCache', 'StorageGateway', result, true, true, mock)
  },
  addCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addCache', 'StorageGateway', result, false, true, mock)
  },
  addCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addCache', 'StorageGateway', result, true, false, mock)
  },
  addTagsToResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTagsToResource', 'StorageGateway', result, true, true, mock)
  },
  addTagsToResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTagsToResource', 'StorageGateway', result, false, true, mock)
  },
  addTagsToResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTagsToResource', 'StorageGateway', result, true, false, mock)
  },
  addUploadBuffer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addUploadBuffer', 'StorageGateway', result, true, true, mock)
  },
  addUploadBufferAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addUploadBuffer', 'StorageGateway', result, false, true, mock)
  },
  addUploadBufferThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addUploadBuffer', 'StorageGateway', result, true, false, mock)
  },
  addWorkingStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addWorkingStorage', 'StorageGateway', result, true, true, mock)
  },
  addWorkingStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addWorkingStorage', 'StorageGateway', result, false, true, mock)
  },
  addWorkingStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addWorkingStorage', 'StorageGateway', result, true, false, mock)
  },
  assignTapePool: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('assignTapePool', 'StorageGateway', result, true, true, mock)
  },
  assignTapePoolAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('assignTapePool', 'StorageGateway', result, false, true, mock)
  },
  assignTapePoolThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('assignTapePool', 'StorageGateway', result, true, false, mock)
  },
  associateFileSystem: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateFileSystem', 'StorageGateway', result, true, true, mock)
  },
  associateFileSystemAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateFileSystem', 'StorageGateway', result, false, true, mock)
  },
  associateFileSystemThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateFileSystem', 'StorageGateway', result, true, false, mock)
  },
  attachVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('attachVolume', 'StorageGateway', result, true, true, mock)
  },
  attachVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('attachVolume', 'StorageGateway', result, false, true, mock)
  },
  attachVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('attachVolume', 'StorageGateway', result, true, false, mock)
  },
  cancelArchival: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelArchival', 'StorageGateway', result, true, true, mock)
  },
  cancelArchivalAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelArchival', 'StorageGateway', result, false, true, mock)
  },
  cancelArchivalThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelArchival', 'StorageGateway', result, true, false, mock)
  },
  cancelRetrieval: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelRetrieval', 'StorageGateway', result, true, true, mock)
  },
  cancelRetrievalAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelRetrieval', 'StorageGateway', result, false, true, mock)
  },
  cancelRetrievalThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelRetrieval', 'StorageGateway', result, true, false, mock)
  },
  createCachediSCSIVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCachediSCSIVolume', 'StorageGateway', result, true, true, mock)
  },
  createCachediSCSIVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCachediSCSIVolume', 'StorageGateway', result, false, true, mock)
  },
  createCachediSCSIVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCachediSCSIVolume', 'StorageGateway', result, true, false, mock)
  },
  createNFSFileShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNFSFileShare', 'StorageGateway', result, true, true, mock)
  },
  createNFSFileShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNFSFileShare', 'StorageGateway', result, false, true, mock)
  },
  createNFSFileShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createNFSFileShare', 'StorageGateway', result, true, false, mock)
  },
  createSMBFileShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSMBFileShare', 'StorageGateway', result, true, true, mock)
  },
  createSMBFileShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSMBFileShare', 'StorageGateway', result, false, true, mock)
  },
  createSMBFileShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSMBFileShare', 'StorageGateway', result, true, false, mock)
  },
  createSnapshot: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshot', 'StorageGateway', result, true, true, mock)
  },
  createSnapshotAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshot', 'StorageGateway', result, false, true, mock)
  },
  createSnapshotThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshot', 'StorageGateway', result, true, false, mock)
  },
  createSnapshotFromVolumeRecoveryPoint: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshotFromVolumeRecoveryPoint', 'StorageGateway', result, true, true, mock)
  },
  createSnapshotFromVolumeRecoveryPointAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshotFromVolumeRecoveryPoint', 'StorageGateway', result, false, true, mock)
  },
  createSnapshotFromVolumeRecoveryPointThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSnapshotFromVolumeRecoveryPoint', 'StorageGateway', result, true, false, mock)
  },
  createStorediSCSIVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createStorediSCSIVolume', 'StorageGateway', result, true, true, mock)
  },
  createStorediSCSIVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createStorediSCSIVolume', 'StorageGateway', result, false, true, mock)
  },
  createStorediSCSIVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createStorediSCSIVolume', 'StorageGateway', result, true, false, mock)
  },
  createTapePool: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapePool', 'StorageGateway', result, true, true, mock)
  },
  createTapePoolAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapePool', 'StorageGateway', result, false, true, mock)
  },
  createTapePoolThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapePool', 'StorageGateway', result, true, false, mock)
  },
  createTapeWithBarcode: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapeWithBarcode', 'StorageGateway', result, true, true, mock)
  },
  createTapeWithBarcodeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapeWithBarcode', 'StorageGateway', result, false, true, mock)
  },
  createTapeWithBarcodeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapeWithBarcode', 'StorageGateway', result, true, false, mock)
  },
  createTapes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapes', 'StorageGateway', result, true, true, mock)
  },
  createTapesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapes', 'StorageGateway', result, false, true, mock)
  },
  createTapesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTapes', 'StorageGateway', result, true, false, mock)
  },
  deleteAutomaticTapeCreationPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAutomaticTapeCreationPolicy', 'StorageGateway', result, true, true, mock)
  },
  deleteAutomaticTapeCreationPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAutomaticTapeCreationPolicy', 'StorageGateway', result, false, true, mock)
  },
  deleteAutomaticTapeCreationPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAutomaticTapeCreationPolicy', 'StorageGateway', result, true, false, mock)
  },
  deleteBandwidthRateLimit: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBandwidthRateLimit', 'StorageGateway', result, true, true, mock)
  },
  deleteBandwidthRateLimitAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBandwidthRateLimit', 'StorageGateway', result, false, true, mock)
  },
  deleteBandwidthRateLimitThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBandwidthRateLimit', 'StorageGateway', result, true, false, mock)
  },
  deleteChapCredentials: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChapCredentials', 'StorageGateway', result, true, true, mock)
  },
  deleteChapCredentialsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChapCredentials', 'StorageGateway', result, false, true, mock)
  },
  deleteChapCredentialsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChapCredentials', 'StorageGateway', result, true, false, mock)
  },
  deleteFileShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFileShare', 'StorageGateway', result, true, true, mock)
  },
  deleteFileShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFileShare', 'StorageGateway', result, false, true, mock)
  },
  deleteFileShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFileShare', 'StorageGateway', result, true, false, mock)
  },
  deleteGateway: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteGateway', 'StorageGateway', result, true, true, mock)
  },
  deleteGatewayAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteGateway', 'StorageGateway', result, false, true, mock)
  },
  deleteGatewayThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteGateway', 'StorageGateway', result, true, false, mock)
  },
  deleteSnapshotSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSnapshotSchedule', 'StorageGateway', result, true, true, mock)
  },
  deleteSnapshotScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSnapshotSchedule', 'StorageGateway', result, false, true, mock)
  },
  deleteSnapshotScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSnapshotSchedule', 'StorageGateway', result, true, false, mock)
  },
  deleteTape: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTape', 'StorageGateway', result, true, true, mock)
  },
  deleteTapeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTape', 'StorageGateway', result, false, true, mock)
  },
  deleteTapeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTape', 'StorageGateway', result, true, false, mock)
  },
  deleteTapeArchive: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTapeArchive', 'StorageGateway', result, true, true, mock)
  },
  deleteTapeArchiveAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTapeArchive', 'StorageGateway', result, false, true, mock)
  },
  deleteTapeArchiveThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTapeArchive', 'StorageGateway', result, true, false, mock)
  },
  deleteTapePool: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTapePool', 'StorageGateway', result, true, true, mock)
  },
  deleteTapePoolAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTapePool', 'StorageGateway', result, false, true, mock)
  },
  deleteTapePoolThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTapePool', 'StorageGateway', result, true, false, mock)
  },
  deleteVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVolume', 'StorageGateway', result, true, true, mock)
  },
  deleteVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVolume', 'StorageGateway', result, false, true, mock)
  },
  deleteVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVolume', 'StorageGateway', result, true, false, mock)
  },
  describeAvailabilityMonitorTest: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAvailabilityMonitorTest', 'StorageGateway', result, true, true, mock)
  },
  describeAvailabilityMonitorTestAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAvailabilityMonitorTest', 'StorageGateway', result, false, true, mock)
  },
  describeAvailabilityMonitorTestThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAvailabilityMonitorTest', 'StorageGateway', result, true, false, mock)
  },
  describeBandwidthRateLimit: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBandwidthRateLimit', 'StorageGateway', result, true, true, mock)
  },
  describeBandwidthRateLimitAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBandwidthRateLimit', 'StorageGateway', result, false, true, mock)
  },
  describeBandwidthRateLimitThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBandwidthRateLimit', 'StorageGateway', result, true, false, mock)
  },
  describeBandwidthRateLimitSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBandwidthRateLimitSchedule', 'StorageGateway', result, true, true, mock)
  },
  describeBandwidthRateLimitScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBandwidthRateLimitSchedule', 'StorageGateway', result, false, true, mock)
  },
  describeBandwidthRateLimitScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeBandwidthRateLimitSchedule', 'StorageGateway', result, true, false, mock)
  },
  describeCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCache', 'StorageGateway', result, true, true, mock)
  },
  describeCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCache', 'StorageGateway', result, false, true, mock)
  },
  describeCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCache', 'StorageGateway', result, true, false, mock)
  },
  describeCachediSCSIVolumes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCachediSCSIVolumes', 'StorageGateway', result, true, true, mock)
  },
  describeCachediSCSIVolumesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCachediSCSIVolumes', 'StorageGateway', result, false, true, mock)
  },
  describeCachediSCSIVolumesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCachediSCSIVolumes', 'StorageGateway', result, true, false, mock)
  },
  describeChapCredentials: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChapCredentials', 'StorageGateway', result, true, true, mock)
  },
  describeChapCredentialsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChapCredentials', 'StorageGateway', result, false, true, mock)
  },
  describeChapCredentialsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChapCredentials', 'StorageGateway', result, true, false, mock)
  },
  describeFileSystemAssociations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystemAssociations', 'StorageGateway', result, true, true, mock)
  },
  describeFileSystemAssociationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystemAssociations', 'StorageGateway', result, false, true, mock)
  },
  describeFileSystemAssociationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFileSystemAssociations', 'StorageGateway', result, true, false, mock)
  },
  describeGatewayInformation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeGatewayInformation', 'StorageGateway', result, true, true, mock)
  },
  describeGatewayInformationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeGatewayInformation', 'StorageGateway', result, false, true, mock)
  },
  describeGatewayInformationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeGatewayInformation', 'StorageGateway', result, true, false, mock)
  },
  describeMaintenanceStartTime: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMaintenanceStartTime', 'StorageGateway', result, true, true, mock)
  },
  describeMaintenanceStartTimeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMaintenanceStartTime', 'StorageGateway', result, false, true, mock)
  },
  describeMaintenanceStartTimeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMaintenanceStartTime', 'StorageGateway', result, true, false, mock)
  },
  describeNFSFileShares: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeNFSFileShares', 'StorageGateway', result, true, true, mock)
  },
  describeNFSFileSharesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeNFSFileShares', 'StorageGateway', result, false, true, mock)
  },
  describeNFSFileSharesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeNFSFileShares', 'StorageGateway', result, true, false, mock)
  },
  describeSMBFileShares: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSMBFileShares', 'StorageGateway', result, true, true, mock)
  },
  describeSMBFileSharesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSMBFileShares', 'StorageGateway', result, false, true, mock)
  },
  describeSMBFileSharesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSMBFileShares', 'StorageGateway', result, true, false, mock)
  },
  describeSMBSettings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSMBSettings', 'StorageGateway', result, true, true, mock)
  },
  describeSMBSettingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSMBSettings', 'StorageGateway', result, false, true, mock)
  },
  describeSMBSettingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSMBSettings', 'StorageGateway', result, true, false, mock)
  },
  describeSnapshotSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSnapshotSchedule', 'StorageGateway', result, true, true, mock)
  },
  describeSnapshotScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSnapshotSchedule', 'StorageGateway', result, false, true, mock)
  },
  describeSnapshotScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSnapshotSchedule', 'StorageGateway', result, true, false, mock)
  },
  describeStorediSCSIVolumes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeStorediSCSIVolumes', 'StorageGateway', result, true, true, mock)
  },
  describeStorediSCSIVolumesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeStorediSCSIVolumes', 'StorageGateway', result, false, true, mock)
  },
  describeStorediSCSIVolumesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeStorediSCSIVolumes', 'StorageGateway', result, true, false, mock)
  },
  describeTapeArchives: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapeArchives', 'StorageGateway', result, true, true, mock)
  },
  describeTapeArchivesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapeArchives', 'StorageGateway', result, false, true, mock)
  },
  describeTapeArchivesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapeArchives', 'StorageGateway', result, true, false, mock)
  },
  describeTapeRecoveryPoints: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapeRecoveryPoints', 'StorageGateway', result, true, true, mock)
  },
  describeTapeRecoveryPointsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapeRecoveryPoints', 'StorageGateway', result, false, true, mock)
  },
  describeTapeRecoveryPointsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapeRecoveryPoints', 'StorageGateway', result, true, false, mock)
  },
  describeTapes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapes', 'StorageGateway', result, true, true, mock)
  },
  describeTapesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapes', 'StorageGateway', result, false, true, mock)
  },
  describeTapesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTapes', 'StorageGateway', result, true, false, mock)
  },
  describeUploadBuffer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeUploadBuffer', 'StorageGateway', result, true, true, mock)
  },
  describeUploadBufferAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeUploadBuffer', 'StorageGateway', result, false, true, mock)
  },
  describeUploadBufferThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeUploadBuffer', 'StorageGateway', result, true, false, mock)
  },
  describeVTLDevices: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVTLDevices', 'StorageGateway', result, true, true, mock)
  },
  describeVTLDevicesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVTLDevices', 'StorageGateway', result, false, true, mock)
  },
  describeVTLDevicesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVTLDevices', 'StorageGateway', result, true, false, mock)
  },
  describeWorkingStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkingStorage', 'StorageGateway', result, true, true, mock)
  },
  describeWorkingStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkingStorage', 'StorageGateway', result, false, true, mock)
  },
  describeWorkingStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkingStorage', 'StorageGateway', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'StorageGateway', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'StorageGateway', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'StorageGateway', result, true, false, mock)
  },
  detachVolume: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('detachVolume', 'StorageGateway', result, true, true, mock)
  },
  detachVolumeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('detachVolume', 'StorageGateway', result, false, true, mock)
  },
  detachVolumeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('detachVolume', 'StorageGateway', result, true, false, mock)
  },
  disableGateway: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableGateway', 'StorageGateway', result, true, true, mock)
  },
  disableGatewayAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableGateway', 'StorageGateway', result, false, true, mock)
  },
  disableGatewayThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableGateway', 'StorageGateway', result, true, false, mock)
  },
  disassociateFileSystem: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateFileSystem', 'StorageGateway', result, true, true, mock)
  },
  disassociateFileSystemAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateFileSystem', 'StorageGateway', result, false, true, mock)
  },
  disassociateFileSystemThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateFileSystem', 'StorageGateway', result, true, false, mock)
  },
  joinDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('joinDomain', 'StorageGateway', result, true, true, mock)
  },
  joinDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('joinDomain', 'StorageGateway', result, false, true, mock)
  },
  joinDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('joinDomain', 'StorageGateway', result, true, false, mock)
  },
  listAutomaticTapeCreationPolicies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAutomaticTapeCreationPolicies', 'StorageGateway', result, true, true, mock)
  },
  listAutomaticTapeCreationPoliciesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAutomaticTapeCreationPolicies', 'StorageGateway', result, false, true, mock)
  },
  listAutomaticTapeCreationPoliciesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAutomaticTapeCreationPolicies', 'StorageGateway', result, true, false, mock)
  },
  listFileShares: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFileShares', 'StorageGateway', result, true, true, mock)
  },
  listFileSharesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFileShares', 'StorageGateway', result, false, true, mock)
  },
  listFileSharesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFileShares', 'StorageGateway', result, true, false, mock)
  },
  listFileSystemAssociations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFileSystemAssociations', 'StorageGateway', result, true, true, mock)
  },
  listFileSystemAssociationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFileSystemAssociations', 'StorageGateway', result, false, true, mock)
  },
  listFileSystemAssociationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFileSystemAssociations', 'StorageGateway', result, true, false, mock)
  },
  listGateways: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGateways', 'StorageGateway', result, true, true, mock)
  },
  listGatewaysAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGateways', 'StorageGateway', result, false, true, mock)
  },
  listGatewaysThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGateways', 'StorageGateway', result, true, false, mock)
  },
  listLocalDisks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLocalDisks', 'StorageGateway', result, true, true, mock)
  },
  listLocalDisksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLocalDisks', 'StorageGateway', result, false, true, mock)
  },
  listLocalDisksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLocalDisks', 'StorageGateway', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'StorageGateway', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'StorageGateway', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'StorageGateway', result, true, false, mock)
  },
  listTapePools: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTapePools', 'StorageGateway', result, true, true, mock)
  },
  listTapePoolsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTapePools', 'StorageGateway', result, false, true, mock)
  },
  listTapePoolsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTapePools', 'StorageGateway', result, true, false, mock)
  },
  listTapes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTapes', 'StorageGateway', result, true, true, mock)
  },
  listTapesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTapes', 'StorageGateway', result, false, true, mock)
  },
  listTapesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTapes', 'StorageGateway', result, true, false, mock)
  },
  listVolumeInitiators: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumeInitiators', 'StorageGateway', result, true, true, mock)
  },
  listVolumeInitiatorsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumeInitiators', 'StorageGateway', result, false, true, mock)
  },
  listVolumeInitiatorsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumeInitiators', 'StorageGateway', result, true, false, mock)
  },
  listVolumeRecoveryPoints: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumeRecoveryPoints', 'StorageGateway', result, true, true, mock)
  },
  listVolumeRecoveryPointsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumeRecoveryPoints', 'StorageGateway', result, false, true, mock)
  },
  listVolumeRecoveryPointsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumeRecoveryPoints', 'StorageGateway', result, true, false, mock)
  },
  listVolumes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumes', 'StorageGateway', result, true, true, mock)
  },
  listVolumesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumes', 'StorageGateway', result, false, true, mock)
  },
  listVolumesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVolumes', 'StorageGateway', result, true, false, mock)
  },
  notifyWhenUploaded: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('notifyWhenUploaded', 'StorageGateway', result, true, true, mock)
  },
  notifyWhenUploadedAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('notifyWhenUploaded', 'StorageGateway', result, false, true, mock)
  },
  notifyWhenUploadedThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('notifyWhenUploaded', 'StorageGateway', result, true, false, mock)
  },
  refreshCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('refreshCache', 'StorageGateway', result, true, true, mock)
  },
  refreshCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('refreshCache', 'StorageGateway', result, false, true, mock)
  },
  refreshCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('refreshCache', 'StorageGateway', result, true, false, mock)
  },
  removeTagsFromResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTagsFromResource', 'StorageGateway', result, true, true, mock)
  },
  removeTagsFromResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTagsFromResource', 'StorageGateway', result, false, true, mock)
  },
  removeTagsFromResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTagsFromResource', 'StorageGateway', result, true, false, mock)
  },
  resetCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resetCache', 'StorageGateway', result, true, true, mock)
  },
  resetCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resetCache', 'StorageGateway', result, false, true, mock)
  },
  resetCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resetCache', 'StorageGateway', result, true, false, mock)
  },
  retrieveTapeArchive: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retrieveTapeArchive', 'StorageGateway', result, true, true, mock)
  },
  retrieveTapeArchiveAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retrieveTapeArchive', 'StorageGateway', result, false, true, mock)
  },
  retrieveTapeArchiveThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retrieveTapeArchive', 'StorageGateway', result, true, false, mock)
  },
  retrieveTapeRecoveryPoint: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retrieveTapeRecoveryPoint', 'StorageGateway', result, true, true, mock)
  },
  retrieveTapeRecoveryPointAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retrieveTapeRecoveryPoint', 'StorageGateway', result, false, true, mock)
  },
  retrieveTapeRecoveryPointThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retrieveTapeRecoveryPoint', 'StorageGateway', result, true, false, mock)
  },
  setLocalConsolePassword: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setLocalConsolePassword', 'StorageGateway', result, true, true, mock)
  },
  setLocalConsolePasswordAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setLocalConsolePassword', 'StorageGateway', result, false, true, mock)
  },
  setLocalConsolePasswordThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setLocalConsolePassword', 'StorageGateway', result, true, false, mock)
  },
  setSMBGuestPassword: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setSMBGuestPassword', 'StorageGateway', result, true, true, mock)
  },
  setSMBGuestPasswordAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setSMBGuestPassword', 'StorageGateway', result, false, true, mock)
  },
  setSMBGuestPasswordThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('setSMBGuestPassword', 'StorageGateway', result, true, false, mock)
  },
  shutdownGateway: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('shutdownGateway', 'StorageGateway', result, true, true, mock)
  },
  shutdownGatewayAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('shutdownGateway', 'StorageGateway', result, false, true, mock)
  },
  shutdownGatewayThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('shutdownGateway', 'StorageGateway', result, true, false, mock)
  },
  startAvailabilityMonitorTest: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startAvailabilityMonitorTest', 'StorageGateway', result, true, true, mock)
  },
  startAvailabilityMonitorTestAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startAvailabilityMonitorTest', 'StorageGateway', result, false, true, mock)
  },
  startAvailabilityMonitorTestThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startAvailabilityMonitorTest', 'StorageGateway', result, true, false, mock)
  },
  startGateway: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startGateway', 'StorageGateway', result, true, true, mock)
  },
  startGatewayAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startGateway', 'StorageGateway', result, false, true, mock)
  },
  startGatewayThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startGateway', 'StorageGateway', result, true, false, mock)
  },
  updateAutomaticTapeCreationPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAutomaticTapeCreationPolicy', 'StorageGateway', result, true, true, mock)
  },
  updateAutomaticTapeCreationPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAutomaticTapeCreationPolicy', 'StorageGateway', result, false, true, mock)
  },
  updateAutomaticTapeCreationPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAutomaticTapeCreationPolicy', 'StorageGateway', result, true, false, mock)
  },
  updateBandwidthRateLimit: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBandwidthRateLimit', 'StorageGateway', result, true, true, mock)
  },
  updateBandwidthRateLimitAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBandwidthRateLimit', 'StorageGateway', result, false, true, mock)
  },
  updateBandwidthRateLimitThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBandwidthRateLimit', 'StorageGateway', result, true, false, mock)
  },
  updateBandwidthRateLimitSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBandwidthRateLimitSchedule', 'StorageGateway', result, true, true, mock)
  },
  updateBandwidthRateLimitScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBandwidthRateLimitSchedule', 'StorageGateway', result, false, true, mock)
  },
  updateBandwidthRateLimitScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBandwidthRateLimitSchedule', 'StorageGateway', result, true, false, mock)
  },
  updateChapCredentials: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChapCredentials', 'StorageGateway', result, true, true, mock)
  },
  updateChapCredentialsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChapCredentials', 'StorageGateway', result, false, true, mock)
  },
  updateChapCredentialsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChapCredentials', 'StorageGateway', result, true, false, mock)
  },
  updateFileSystemAssociation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFileSystemAssociation', 'StorageGateway', result, true, true, mock)
  },
  updateFileSystemAssociationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFileSystemAssociation', 'StorageGateway', result, false, true, mock)
  },
  updateFileSystemAssociationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFileSystemAssociation', 'StorageGateway', result, true, false, mock)
  },
  updateGatewayInformation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGatewayInformation', 'StorageGateway', result, true, true, mock)
  },
  updateGatewayInformationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGatewayInformation', 'StorageGateway', result, false, true, mock)
  },
  updateGatewayInformationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGatewayInformation', 'StorageGateway', result, true, false, mock)
  },
  updateGatewaySoftwareNow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGatewaySoftwareNow', 'StorageGateway', result, true, true, mock)
  },
  updateGatewaySoftwareNowAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGatewaySoftwareNow', 'StorageGateway', result, false, true, mock)
  },
  updateGatewaySoftwareNowThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGatewaySoftwareNow', 'StorageGateway', result, true, false, mock)
  },
  updateMaintenanceStartTime: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMaintenanceStartTime', 'StorageGateway', result, true, true, mock)
  },
  updateMaintenanceStartTimeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMaintenanceStartTime', 'StorageGateway', result, false, true, mock)
  },
  updateMaintenanceStartTimeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMaintenanceStartTime', 'StorageGateway', result, true, false, mock)
  },
  updateNFSFileShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNFSFileShare', 'StorageGateway', result, true, true, mock)
  },
  updateNFSFileShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNFSFileShare', 'StorageGateway', result, false, true, mock)
  },
  updateNFSFileShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateNFSFileShare', 'StorageGateway', result, true, false, mock)
  },
  updateSMBFileShare: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBFileShare', 'StorageGateway', result, true, true, mock)
  },
  updateSMBFileShareAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBFileShare', 'StorageGateway', result, false, true, mock)
  },
  updateSMBFileShareThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBFileShare', 'StorageGateway', result, true, false, mock)
  },
  updateSMBFileShareVisibility: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBFileShareVisibility', 'StorageGateway', result, true, true, mock)
  },
  updateSMBFileShareVisibilityAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBFileShareVisibility', 'StorageGateway', result, false, true, mock)
  },
  updateSMBFileShareVisibilityThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBFileShareVisibility', 'StorageGateway', result, true, false, mock)
  },
  updateSMBLocalGroups: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBLocalGroups', 'StorageGateway', result, true, true, mock)
  },
  updateSMBLocalGroupsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBLocalGroups', 'StorageGateway', result, false, true, mock)
  },
  updateSMBLocalGroupsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBLocalGroups', 'StorageGateway', result, true, false, mock)
  },
  updateSMBSecurityStrategy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBSecurityStrategy', 'StorageGateway', result, true, true, mock)
  },
  updateSMBSecurityStrategyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBSecurityStrategy', 'StorageGateway', result, false, true, mock)
  },
  updateSMBSecurityStrategyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSMBSecurityStrategy', 'StorageGateway', result, true, false, mock)
  },
  updateSnapshotSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSnapshotSchedule', 'StorageGateway', result, true, true, mock)
  },
  updateSnapshotScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSnapshotSchedule', 'StorageGateway', result, false, true, mock)
  },
  updateSnapshotScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSnapshotSchedule', 'StorageGateway', result, true, false, mock)
  },
  updateVTLDeviceType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVTLDeviceType', 'StorageGateway', result, true, true, mock)
  },
  updateVTLDeviceTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVTLDeviceType', 'StorageGateway', result, false, true, mock)
  },
  updateVTLDeviceTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVTLDeviceType', 'StorageGateway', result, true, false, mock)
  },
}
