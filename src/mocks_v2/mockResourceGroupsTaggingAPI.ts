
/**
* @description mocks_v2:mockResourceGroupsTaggingAPI.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockResourceGroupsTaggingAPI = {
  describeReportCreation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReportCreation', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  describeReportCreationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReportCreation', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  describeReportCreationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReportCreation', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  getComplianceSummary: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getComplianceSummary', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  getComplianceSummaryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getComplianceSummary', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  getComplianceSummaryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getComplianceSummary', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  getResources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResources', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  getResourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResources', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  getResourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResources', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  getTagKeys: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTagKeys', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  getTagKeysAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTagKeys', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  getTagKeysThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTagKeys', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  getTagValues: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTagValues', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  getTagValuesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTagValues', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  getTagValuesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTagValues', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  startReportCreation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startReportCreation', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  startReportCreationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startReportCreation', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  startReportCreationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startReportCreation', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  tagResources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResources', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  tagResourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResources', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  tagResourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResources', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
  untagResources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResources', 'ResourceGroupsTaggingAPI', result, true, true, mock)
  },
  untagResourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResources', 'ResourceGroupsTaggingAPI', result, false, true, mock)
  },
  untagResourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResources', 'ResourceGroupsTaggingAPI', result, true, false, mock)
  },
}
