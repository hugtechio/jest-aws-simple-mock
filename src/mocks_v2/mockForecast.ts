
/**
* @description mocks_v2:mockForecast.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockForecast = {
  createDataset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataset', 'Forecast', result, true, true, mock)
  },
  createDatasetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataset', 'Forecast', result, false, true, mock)
  },
  createDatasetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataset', 'Forecast', result, true, false, mock)
  },
  createDatasetGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDatasetGroup', 'Forecast', result, true, true, mock)
  },
  createDatasetGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDatasetGroup', 'Forecast', result, false, true, mock)
  },
  createDatasetGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDatasetGroup', 'Forecast', result, true, false, mock)
  },
  createDatasetImportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDatasetImportJob', 'Forecast', result, true, true, mock)
  },
  createDatasetImportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDatasetImportJob', 'Forecast', result, false, true, mock)
  },
  createDatasetImportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDatasetImportJob', 'Forecast', result, true, false, mock)
  },
  createForecast: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createForecast', 'Forecast', result, true, true, mock)
  },
  createForecastAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createForecast', 'Forecast', result, false, true, mock)
  },
  createForecastThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createForecast', 'Forecast', result, true, false, mock)
  },
  createForecastExportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createForecastExportJob', 'Forecast', result, true, true, mock)
  },
  createForecastExportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createForecastExportJob', 'Forecast', result, false, true, mock)
  },
  createForecastExportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createForecastExportJob', 'Forecast', result, true, false, mock)
  },
  createPredictor: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPredictor', 'Forecast', result, true, true, mock)
  },
  createPredictorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPredictor', 'Forecast', result, false, true, mock)
  },
  createPredictorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPredictor', 'Forecast', result, true, false, mock)
  },
  createPredictorBacktestExportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPredictorBacktestExportJob', 'Forecast', result, true, true, mock)
  },
  createPredictorBacktestExportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPredictorBacktestExportJob', 'Forecast', result, false, true, mock)
  },
  createPredictorBacktestExportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPredictorBacktestExportJob', 'Forecast', result, true, false, mock)
  },
  deleteDataset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataset', 'Forecast', result, true, true, mock)
  },
  deleteDatasetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataset', 'Forecast', result, false, true, mock)
  },
  deleteDatasetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataset', 'Forecast', result, true, false, mock)
  },
  deleteDatasetGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDatasetGroup', 'Forecast', result, true, true, mock)
  },
  deleteDatasetGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDatasetGroup', 'Forecast', result, false, true, mock)
  },
  deleteDatasetGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDatasetGroup', 'Forecast', result, true, false, mock)
  },
  deleteDatasetImportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDatasetImportJob', 'Forecast', result, true, true, mock)
  },
  deleteDatasetImportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDatasetImportJob', 'Forecast', result, false, true, mock)
  },
  deleteDatasetImportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDatasetImportJob', 'Forecast', result, true, false, mock)
  },
  deleteForecast: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteForecast', 'Forecast', result, true, true, mock)
  },
  deleteForecastAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteForecast', 'Forecast', result, false, true, mock)
  },
  deleteForecastThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteForecast', 'Forecast', result, true, false, mock)
  },
  deleteForecastExportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteForecastExportJob', 'Forecast', result, true, true, mock)
  },
  deleteForecastExportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteForecastExportJob', 'Forecast', result, false, true, mock)
  },
  deleteForecastExportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteForecastExportJob', 'Forecast', result, true, false, mock)
  },
  deletePredictor: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePredictor', 'Forecast', result, true, true, mock)
  },
  deletePredictorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePredictor', 'Forecast', result, false, true, mock)
  },
  deletePredictorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePredictor', 'Forecast', result, true, false, mock)
  },
  deletePredictorBacktestExportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePredictorBacktestExportJob', 'Forecast', result, true, true, mock)
  },
  deletePredictorBacktestExportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePredictorBacktestExportJob', 'Forecast', result, false, true, mock)
  },
  deletePredictorBacktestExportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePredictorBacktestExportJob', 'Forecast', result, true, false, mock)
  },
  deleteResourceTree: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourceTree', 'Forecast', result, true, true, mock)
  },
  deleteResourceTreeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourceTree', 'Forecast', result, false, true, mock)
  },
  deleteResourceTreeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourceTree', 'Forecast', result, true, false, mock)
  },
  describeDataset: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataset', 'Forecast', result, true, true, mock)
  },
  describeDatasetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataset', 'Forecast', result, false, true, mock)
  },
  describeDatasetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataset', 'Forecast', result, true, false, mock)
  },
  describeDatasetGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDatasetGroup', 'Forecast', result, true, true, mock)
  },
  describeDatasetGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDatasetGroup', 'Forecast', result, false, true, mock)
  },
  describeDatasetGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDatasetGroup', 'Forecast', result, true, false, mock)
  },
  describeDatasetImportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDatasetImportJob', 'Forecast', result, true, true, mock)
  },
  describeDatasetImportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDatasetImportJob', 'Forecast', result, false, true, mock)
  },
  describeDatasetImportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDatasetImportJob', 'Forecast', result, true, false, mock)
  },
  describeForecast: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeForecast', 'Forecast', result, true, true, mock)
  },
  describeForecastAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeForecast', 'Forecast', result, false, true, mock)
  },
  describeForecastThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeForecast', 'Forecast', result, true, false, mock)
  },
  describeForecastExportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeForecastExportJob', 'Forecast', result, true, true, mock)
  },
  describeForecastExportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeForecastExportJob', 'Forecast', result, false, true, mock)
  },
  describeForecastExportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeForecastExportJob', 'Forecast', result, true, false, mock)
  },
  describePredictor: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePredictor', 'Forecast', result, true, true, mock)
  },
  describePredictorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePredictor', 'Forecast', result, false, true, mock)
  },
  describePredictorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePredictor', 'Forecast', result, true, false, mock)
  },
  describePredictorBacktestExportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePredictorBacktestExportJob', 'Forecast', result, true, true, mock)
  },
  describePredictorBacktestExportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePredictorBacktestExportJob', 'Forecast', result, false, true, mock)
  },
  describePredictorBacktestExportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePredictorBacktestExportJob', 'Forecast', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Forecast', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Forecast', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Forecast', result, true, false, mock)
  },
  getAccuracyMetrics: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAccuracyMetrics', 'Forecast', result, true, true, mock)
  },
  getAccuracyMetricsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAccuracyMetrics', 'Forecast', result, false, true, mock)
  },
  getAccuracyMetricsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAccuracyMetrics', 'Forecast', result, true, false, mock)
  },
  listDatasetGroups: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasetGroups', 'Forecast', result, true, true, mock)
  },
  listDatasetGroupsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasetGroups', 'Forecast', result, false, true, mock)
  },
  listDatasetGroupsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasetGroups', 'Forecast', result, true, false, mock)
  },
  listDatasetImportJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasetImportJobs', 'Forecast', result, true, true, mock)
  },
  listDatasetImportJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasetImportJobs', 'Forecast', result, false, true, mock)
  },
  listDatasetImportJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasetImportJobs', 'Forecast', result, true, false, mock)
  },
  listDatasets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasets', 'Forecast', result, true, true, mock)
  },
  listDatasetsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasets', 'Forecast', result, false, true, mock)
  },
  listDatasetsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDatasets', 'Forecast', result, true, false, mock)
  },
  listForecastExportJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listForecastExportJobs', 'Forecast', result, true, true, mock)
  },
  listForecastExportJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listForecastExportJobs', 'Forecast', result, false, true, mock)
  },
  listForecastExportJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listForecastExportJobs', 'Forecast', result, true, false, mock)
  },
  listForecasts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listForecasts', 'Forecast', result, true, true, mock)
  },
  listForecastsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listForecasts', 'Forecast', result, false, true, mock)
  },
  listForecastsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listForecasts', 'Forecast', result, true, false, mock)
  },
  listPredictorBacktestExportJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPredictorBacktestExportJobs', 'Forecast', result, true, true, mock)
  },
  listPredictorBacktestExportJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPredictorBacktestExportJobs', 'Forecast', result, false, true, mock)
  },
  listPredictorBacktestExportJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPredictorBacktestExportJobs', 'Forecast', result, true, false, mock)
  },
  listPredictors: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPredictors', 'Forecast', result, true, true, mock)
  },
  listPredictorsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPredictors', 'Forecast', result, false, true, mock)
  },
  listPredictorsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPredictors', 'Forecast', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Forecast', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Forecast', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Forecast', result, true, false, mock)
  },
  stopResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopResource', 'Forecast', result, true, true, mock)
  },
  stopResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopResource', 'Forecast', result, false, true, mock)
  },
  stopResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopResource', 'Forecast', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Forecast', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Forecast', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Forecast', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Forecast', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Forecast', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Forecast', result, true, false, mock)
  },
  updateDatasetGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDatasetGroup', 'Forecast', result, true, true, mock)
  },
  updateDatasetGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDatasetGroup', 'Forecast', result, false, true, mock)
  },
  updateDatasetGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDatasetGroup', 'Forecast', result, true, false, mock)
  },
}
