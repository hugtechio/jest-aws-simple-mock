
/**
* @description mocks_v2:mockAppSync.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockAppSync = {
  createApiCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApiCache', 'AppSync', result, true, true, mock)
  },
  createApiCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApiCache', 'AppSync', result, false, true, mock)
  },
  createApiCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApiCache', 'AppSync', result, true, false, mock)
  },
  createApiKey: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApiKey', 'AppSync', result, true, true, mock)
  },
  createApiKeyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApiKey', 'AppSync', result, false, true, mock)
  },
  createApiKeyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApiKey', 'AppSync', result, true, false, mock)
  },
  createDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSource', 'AppSync', result, true, true, mock)
  },
  createDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSource', 'AppSync', result, false, true, mock)
  },
  createDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSource', 'AppSync', result, true, false, mock)
  },
  createFunction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFunction', 'AppSync', result, true, true, mock)
  },
  createFunctionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFunction', 'AppSync', result, false, true, mock)
  },
  createFunctionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFunction', 'AppSync', result, true, false, mock)
  },
  createGraphqlApi: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createGraphqlApi', 'AppSync', result, true, true, mock)
  },
  createGraphqlApiAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createGraphqlApi', 'AppSync', result, false, true, mock)
  },
  createGraphqlApiThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createGraphqlApi', 'AppSync', result, true, false, mock)
  },
  createResolver: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResolver', 'AppSync', result, true, true, mock)
  },
  createResolverAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResolver', 'AppSync', result, false, true, mock)
  },
  createResolverThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResolver', 'AppSync', result, true, false, mock)
  },
  createType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createType', 'AppSync', result, true, true, mock)
  },
  createTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createType', 'AppSync', result, false, true, mock)
  },
  createTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createType', 'AppSync', result, true, false, mock)
  },
  deleteApiCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApiCache', 'AppSync', result, true, true, mock)
  },
  deleteApiCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApiCache', 'AppSync', result, false, true, mock)
  },
  deleteApiCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApiCache', 'AppSync', result, true, false, mock)
  },
  deleteApiKey: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApiKey', 'AppSync', result, true, true, mock)
  },
  deleteApiKeyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApiKey', 'AppSync', result, false, true, mock)
  },
  deleteApiKeyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApiKey', 'AppSync', result, true, false, mock)
  },
  deleteDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSource', 'AppSync', result, true, true, mock)
  },
  deleteDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSource', 'AppSync', result, false, true, mock)
  },
  deleteDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSource', 'AppSync', result, true, false, mock)
  },
  deleteFunction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFunction', 'AppSync', result, true, true, mock)
  },
  deleteFunctionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFunction', 'AppSync', result, false, true, mock)
  },
  deleteFunctionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFunction', 'AppSync', result, true, false, mock)
  },
  deleteGraphqlApi: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteGraphqlApi', 'AppSync', result, true, true, mock)
  },
  deleteGraphqlApiAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteGraphqlApi', 'AppSync', result, false, true, mock)
  },
  deleteGraphqlApiThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteGraphqlApi', 'AppSync', result, true, false, mock)
  },
  deleteResolver: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResolver', 'AppSync', result, true, true, mock)
  },
  deleteResolverAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResolver', 'AppSync', result, false, true, mock)
  },
  deleteResolverThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResolver', 'AppSync', result, true, false, mock)
  },
  deleteType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteType', 'AppSync', result, true, true, mock)
  },
  deleteTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteType', 'AppSync', result, false, true, mock)
  },
  deleteTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteType', 'AppSync', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AppSync', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AppSync', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AppSync', result, true, false, mock)
  },
  flushApiCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('flushApiCache', 'AppSync', result, true, true, mock)
  },
  flushApiCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('flushApiCache', 'AppSync', result, false, true, mock)
  },
  flushApiCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('flushApiCache', 'AppSync', result, true, false, mock)
  },
  getApiCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApiCache', 'AppSync', result, true, true, mock)
  },
  getApiCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApiCache', 'AppSync', result, false, true, mock)
  },
  getApiCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApiCache', 'AppSync', result, true, false, mock)
  },
  getDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataSource', 'AppSync', result, true, true, mock)
  },
  getDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataSource', 'AppSync', result, false, true, mock)
  },
  getDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataSource', 'AppSync', result, true, false, mock)
  },
  getFunction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getFunction', 'AppSync', result, true, true, mock)
  },
  getFunctionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getFunction', 'AppSync', result, false, true, mock)
  },
  getFunctionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getFunction', 'AppSync', result, true, false, mock)
  },
  getGraphqlApi: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getGraphqlApi', 'AppSync', result, true, true, mock)
  },
  getGraphqlApiAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getGraphqlApi', 'AppSync', result, false, true, mock)
  },
  getGraphqlApiThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getGraphqlApi', 'AppSync', result, true, false, mock)
  },
  getIntrospectionSchema: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIntrospectionSchema', 'AppSync', result, true, true, mock)
  },
  getIntrospectionSchemaAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIntrospectionSchema', 'AppSync', result, false, true, mock)
  },
  getIntrospectionSchemaThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIntrospectionSchema', 'AppSync', result, true, false, mock)
  },
  getResolver: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResolver', 'AppSync', result, true, true, mock)
  },
  getResolverAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResolver', 'AppSync', result, false, true, mock)
  },
  getResolverThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResolver', 'AppSync', result, true, false, mock)
  },
  getSchemaCreationStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSchemaCreationStatus', 'AppSync', result, true, true, mock)
  },
  getSchemaCreationStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSchemaCreationStatus', 'AppSync', result, false, true, mock)
  },
  getSchemaCreationStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSchemaCreationStatus', 'AppSync', result, true, false, mock)
  },
  getType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getType', 'AppSync', result, true, true, mock)
  },
  getTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getType', 'AppSync', result, false, true, mock)
  },
  getTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getType', 'AppSync', result, true, false, mock)
  },
  listApiKeys: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApiKeys', 'AppSync', result, true, true, mock)
  },
  listApiKeysAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApiKeys', 'AppSync', result, false, true, mock)
  },
  listApiKeysThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApiKeys', 'AppSync', result, true, false, mock)
  },
  listDataSources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSources', 'AppSync', result, true, true, mock)
  },
  listDataSourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSources', 'AppSync', result, false, true, mock)
  },
  listDataSourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSources', 'AppSync', result, true, false, mock)
  },
  listFunctions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFunctions', 'AppSync', result, true, true, mock)
  },
  listFunctionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFunctions', 'AppSync', result, false, true, mock)
  },
  listFunctionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFunctions', 'AppSync', result, true, false, mock)
  },
  listGraphqlApis: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGraphqlApis', 'AppSync', result, true, true, mock)
  },
  listGraphqlApisAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGraphqlApis', 'AppSync', result, false, true, mock)
  },
  listGraphqlApisThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGraphqlApis', 'AppSync', result, true, false, mock)
  },
  listResolvers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResolvers', 'AppSync', result, true, true, mock)
  },
  listResolversAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResolvers', 'AppSync', result, false, true, mock)
  },
  listResolversThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResolvers', 'AppSync', result, true, false, mock)
  },
  listResolversByFunction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResolversByFunction', 'AppSync', result, true, true, mock)
  },
  listResolversByFunctionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResolversByFunction', 'AppSync', result, false, true, mock)
  },
  listResolversByFunctionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResolversByFunction', 'AppSync', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'AppSync', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'AppSync', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'AppSync', result, true, false, mock)
  },
  listTypes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTypes', 'AppSync', result, true, true, mock)
  },
  listTypesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTypes', 'AppSync', result, false, true, mock)
  },
  listTypesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTypes', 'AppSync', result, true, false, mock)
  },
  startSchemaCreation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSchemaCreation', 'AppSync', result, true, true, mock)
  },
  startSchemaCreationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSchemaCreation', 'AppSync', result, false, true, mock)
  },
  startSchemaCreationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSchemaCreation', 'AppSync', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'AppSync', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'AppSync', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'AppSync', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'AppSync', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'AppSync', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'AppSync', result, true, false, mock)
  },
  updateApiCache: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApiCache', 'AppSync', result, true, true, mock)
  },
  updateApiCacheAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApiCache', 'AppSync', result, false, true, mock)
  },
  updateApiCacheThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApiCache', 'AppSync', result, true, false, mock)
  },
  updateApiKey: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApiKey', 'AppSync', result, true, true, mock)
  },
  updateApiKeyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApiKey', 'AppSync', result, false, true, mock)
  },
  updateApiKeyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApiKey', 'AppSync', result, true, false, mock)
  },
  updateDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSource', 'AppSync', result, true, true, mock)
  },
  updateDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSource', 'AppSync', result, false, true, mock)
  },
  updateDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSource', 'AppSync', result, true, false, mock)
  },
  updateFunction: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFunction', 'AppSync', result, true, true, mock)
  },
  updateFunctionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFunction', 'AppSync', result, false, true, mock)
  },
  updateFunctionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateFunction', 'AppSync', result, true, false, mock)
  },
  updateGraphqlApi: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGraphqlApi', 'AppSync', result, true, true, mock)
  },
  updateGraphqlApiAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGraphqlApi', 'AppSync', result, false, true, mock)
  },
  updateGraphqlApiThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateGraphqlApi', 'AppSync', result, true, false, mock)
  },
  updateResolver: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResolver', 'AppSync', result, true, true, mock)
  },
  updateResolverAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResolver', 'AppSync', result, false, true, mock)
  },
  updateResolverThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResolver', 'AppSync', result, true, false, mock)
  },
  updateType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateType', 'AppSync', result, true, true, mock)
  },
  updateTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateType', 'AppSync', result, false, true, mock)
  },
  updateTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateType', 'AppSync', result, true, false, mock)
  },
}
