
/**
* @description mocks_v2:mockMarketplaceCatalog.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockMarketplaceCatalog = {
  cancelChangeSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelChangeSet', 'MarketplaceCatalog', result, true, true, mock)
  },
  cancelChangeSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelChangeSet', 'MarketplaceCatalog', result, false, true, mock)
  },
  cancelChangeSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelChangeSet', 'MarketplaceCatalog', result, true, false, mock)
  },
  describeChangeSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChangeSet', 'MarketplaceCatalog', result, true, true, mock)
  },
  describeChangeSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChangeSet', 'MarketplaceCatalog', result, false, true, mock)
  },
  describeChangeSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChangeSet', 'MarketplaceCatalog', result, true, false, mock)
  },
  describeEntity: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeEntity', 'MarketplaceCatalog', result, true, true, mock)
  },
  describeEntityAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeEntity', 'MarketplaceCatalog', result, false, true, mock)
  },
  describeEntityThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeEntity', 'MarketplaceCatalog', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MarketplaceCatalog', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MarketplaceCatalog', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MarketplaceCatalog', result, true, false, mock)
  },
  listChangeSets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChangeSets', 'MarketplaceCatalog', result, true, true, mock)
  },
  listChangeSetsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChangeSets', 'MarketplaceCatalog', result, false, true, mock)
  },
  listChangeSetsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChangeSets', 'MarketplaceCatalog', result, true, false, mock)
  },
  listEntities: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEntities', 'MarketplaceCatalog', result, true, true, mock)
  },
  listEntitiesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEntities', 'MarketplaceCatalog', result, false, true, mock)
  },
  listEntitiesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listEntities', 'MarketplaceCatalog', result, true, false, mock)
  },
  startChangeSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChangeSet', 'MarketplaceCatalog', result, true, true, mock)
  },
  startChangeSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChangeSet', 'MarketplaceCatalog', result, false, true, mock)
  },
  startChangeSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChangeSet', 'MarketplaceCatalog', result, true, false, mock)
  },
}
