
/**
* @description mocks_v2:mockOpenSearch.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockOpenSearch = {
  acceptInboundConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptInboundConnection', 'OpenSearch', result, true, true, mock)
  },
  acceptInboundConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptInboundConnection', 'OpenSearch', result, false, true, mock)
  },
  acceptInboundConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptInboundConnection', 'OpenSearch', result, true, false, mock)
  },
  addTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTags', 'OpenSearch', result, true, true, mock)
  },
  addTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTags', 'OpenSearch', result, false, true, mock)
  },
  addTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addTags', 'OpenSearch', result, true, false, mock)
  },
  associatePackage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associatePackage', 'OpenSearch', result, true, true, mock)
  },
  associatePackageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associatePackage', 'OpenSearch', result, false, true, mock)
  },
  associatePackageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associatePackage', 'OpenSearch', result, true, false, mock)
  },
  cancelServiceSoftwareUpdate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelServiceSoftwareUpdate', 'OpenSearch', result, true, true, mock)
  },
  cancelServiceSoftwareUpdateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelServiceSoftwareUpdate', 'OpenSearch', result, false, true, mock)
  },
  cancelServiceSoftwareUpdateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelServiceSoftwareUpdate', 'OpenSearch', result, true, false, mock)
  },
  createDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'OpenSearch', result, true, true, mock)
  },
  createDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'OpenSearch', result, false, true, mock)
  },
  createDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'OpenSearch', result, true, false, mock)
  },
  createOutboundConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOutboundConnection', 'OpenSearch', result, true, true, mock)
  },
  createOutboundConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOutboundConnection', 'OpenSearch', result, false, true, mock)
  },
  createOutboundConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createOutboundConnection', 'OpenSearch', result, true, false, mock)
  },
  createPackage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPackage', 'OpenSearch', result, true, true, mock)
  },
  createPackageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPackage', 'OpenSearch', result, false, true, mock)
  },
  createPackageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPackage', 'OpenSearch', result, true, false, mock)
  },
  deleteDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'OpenSearch', result, true, true, mock)
  },
  deleteDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'OpenSearch', result, false, true, mock)
  },
  deleteDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'OpenSearch', result, true, false, mock)
  },
  deleteInboundConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInboundConnection', 'OpenSearch', result, true, true, mock)
  },
  deleteInboundConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInboundConnection', 'OpenSearch', result, false, true, mock)
  },
  deleteInboundConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInboundConnection', 'OpenSearch', result, true, false, mock)
  },
  deleteOutboundConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOutboundConnection', 'OpenSearch', result, true, true, mock)
  },
  deleteOutboundConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOutboundConnection', 'OpenSearch', result, false, true, mock)
  },
  deleteOutboundConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteOutboundConnection', 'OpenSearch', result, true, false, mock)
  },
  deletePackage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePackage', 'OpenSearch', result, true, true, mock)
  },
  deletePackageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePackage', 'OpenSearch', result, false, true, mock)
  },
  deletePackageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePackage', 'OpenSearch', result, true, false, mock)
  },
  describeDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomain', 'OpenSearch', result, true, true, mock)
  },
  describeDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomain', 'OpenSearch', result, false, true, mock)
  },
  describeDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomain', 'OpenSearch', result, true, false, mock)
  },
  describeDomainAutoTunes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainAutoTunes', 'OpenSearch', result, true, true, mock)
  },
  describeDomainAutoTunesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainAutoTunes', 'OpenSearch', result, false, true, mock)
  },
  describeDomainAutoTunesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainAutoTunes', 'OpenSearch', result, true, false, mock)
  },
  describeDomainConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainConfig', 'OpenSearch', result, true, true, mock)
  },
  describeDomainConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainConfig', 'OpenSearch', result, false, true, mock)
  },
  describeDomainConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainConfig', 'OpenSearch', result, true, false, mock)
  },
  describeDomains: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomains', 'OpenSearch', result, true, true, mock)
  },
  describeDomainsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomains', 'OpenSearch', result, false, true, mock)
  },
  describeDomainsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomains', 'OpenSearch', result, true, false, mock)
  },
  describeInboundConnections: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInboundConnections', 'OpenSearch', result, true, true, mock)
  },
  describeInboundConnectionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInboundConnections', 'OpenSearch', result, false, true, mock)
  },
  describeInboundConnectionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInboundConnections', 'OpenSearch', result, true, false, mock)
  },
  describeInstanceTypeLimits: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInstanceTypeLimits', 'OpenSearch', result, true, true, mock)
  },
  describeInstanceTypeLimitsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInstanceTypeLimits', 'OpenSearch', result, false, true, mock)
  },
  describeInstanceTypeLimitsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInstanceTypeLimits', 'OpenSearch', result, true, false, mock)
  },
  describeOutboundConnections: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOutboundConnections', 'OpenSearch', result, true, true, mock)
  },
  describeOutboundConnectionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOutboundConnections', 'OpenSearch', result, false, true, mock)
  },
  describeOutboundConnectionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOutboundConnections', 'OpenSearch', result, true, false, mock)
  },
  describePackages: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePackages', 'OpenSearch', result, true, true, mock)
  },
  describePackagesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePackages', 'OpenSearch', result, false, true, mock)
  },
  describePackagesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePackages', 'OpenSearch', result, true, false, mock)
  },
  describeReservedInstanceOfferings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservedInstanceOfferings', 'OpenSearch', result, true, true, mock)
  },
  describeReservedInstanceOfferingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservedInstanceOfferings', 'OpenSearch', result, false, true, mock)
  },
  describeReservedInstanceOfferingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservedInstanceOfferings', 'OpenSearch', result, true, false, mock)
  },
  describeReservedInstances: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservedInstances', 'OpenSearch', result, true, true, mock)
  },
  describeReservedInstancesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservedInstances', 'OpenSearch', result, false, true, mock)
  },
  describeReservedInstancesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservedInstances', 'OpenSearch', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'OpenSearch', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'OpenSearch', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'OpenSearch', result, true, false, mock)
  },
  dissociatePackage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('dissociatePackage', 'OpenSearch', result, true, true, mock)
  },
  dissociatePackageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('dissociatePackage', 'OpenSearch', result, false, true, mock)
  },
  dissociatePackageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('dissociatePackage', 'OpenSearch', result, true, false, mock)
  },
  getCompatibleVersions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCompatibleVersions', 'OpenSearch', result, true, true, mock)
  },
  getCompatibleVersionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCompatibleVersions', 'OpenSearch', result, false, true, mock)
  },
  getCompatibleVersionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCompatibleVersions', 'OpenSearch', result, true, false, mock)
  },
  getPackageVersionHistory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPackageVersionHistory', 'OpenSearch', result, true, true, mock)
  },
  getPackageVersionHistoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPackageVersionHistory', 'OpenSearch', result, false, true, mock)
  },
  getPackageVersionHistoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPackageVersionHistory', 'OpenSearch', result, true, false, mock)
  },
  getUpgradeHistory: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getUpgradeHistory', 'OpenSearch', result, true, true, mock)
  },
  getUpgradeHistoryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getUpgradeHistory', 'OpenSearch', result, false, true, mock)
  },
  getUpgradeHistoryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getUpgradeHistory', 'OpenSearch', result, true, false, mock)
  },
  getUpgradeStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getUpgradeStatus', 'OpenSearch', result, true, true, mock)
  },
  getUpgradeStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getUpgradeStatus', 'OpenSearch', result, false, true, mock)
  },
  getUpgradeStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getUpgradeStatus', 'OpenSearch', result, true, false, mock)
  },
  listDomainNames: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainNames', 'OpenSearch', result, true, true, mock)
  },
  listDomainNamesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainNames', 'OpenSearch', result, false, true, mock)
  },
  listDomainNamesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainNames', 'OpenSearch', result, true, false, mock)
  },
  listDomainsForPackage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainsForPackage', 'OpenSearch', result, true, true, mock)
  },
  listDomainsForPackageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainsForPackage', 'OpenSearch', result, false, true, mock)
  },
  listDomainsForPackageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainsForPackage', 'OpenSearch', result, true, false, mock)
  },
  listInstanceTypeDetails: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInstanceTypeDetails', 'OpenSearch', result, true, true, mock)
  },
  listInstanceTypeDetailsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInstanceTypeDetails', 'OpenSearch', result, false, true, mock)
  },
  listInstanceTypeDetailsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInstanceTypeDetails', 'OpenSearch', result, true, false, mock)
  },
  listPackagesForDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPackagesForDomain', 'OpenSearch', result, true, true, mock)
  },
  listPackagesForDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPackagesForDomain', 'OpenSearch', result, false, true, mock)
  },
  listPackagesForDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPackagesForDomain', 'OpenSearch', result, true, false, mock)
  },
  listTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTags', 'OpenSearch', result, true, true, mock)
  },
  listTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTags', 'OpenSearch', result, false, true, mock)
  },
  listTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTags', 'OpenSearch', result, true, false, mock)
  },
  listVersions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVersions', 'OpenSearch', result, true, true, mock)
  },
  listVersionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVersions', 'OpenSearch', result, false, true, mock)
  },
  listVersionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVersions', 'OpenSearch', result, true, false, mock)
  },
  purchaseReservedInstanceOffering: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseReservedInstanceOffering', 'OpenSearch', result, true, true, mock)
  },
  purchaseReservedInstanceOfferingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseReservedInstanceOffering', 'OpenSearch', result, false, true, mock)
  },
  purchaseReservedInstanceOfferingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseReservedInstanceOffering', 'OpenSearch', result, true, false, mock)
  },
  rejectInboundConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInboundConnection', 'OpenSearch', result, true, true, mock)
  },
  rejectInboundConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInboundConnection', 'OpenSearch', result, false, true, mock)
  },
  rejectInboundConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInboundConnection', 'OpenSearch', result, true, false, mock)
  },
  removeTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTags', 'OpenSearch', result, true, true, mock)
  },
  removeTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTags', 'OpenSearch', result, false, true, mock)
  },
  removeTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeTags', 'OpenSearch', result, true, false, mock)
  },
  startServiceSoftwareUpdate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startServiceSoftwareUpdate', 'OpenSearch', result, true, true, mock)
  },
  startServiceSoftwareUpdateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startServiceSoftwareUpdate', 'OpenSearch', result, false, true, mock)
  },
  startServiceSoftwareUpdateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startServiceSoftwareUpdate', 'OpenSearch', result, true, false, mock)
  },
  updateDomainConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainConfig', 'OpenSearch', result, true, true, mock)
  },
  updateDomainConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainConfig', 'OpenSearch', result, false, true, mock)
  },
  updateDomainConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainConfig', 'OpenSearch', result, true, false, mock)
  },
  updatePackage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePackage', 'OpenSearch', result, true, true, mock)
  },
  updatePackageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePackage', 'OpenSearch', result, false, true, mock)
  },
  updatePackageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePackage', 'OpenSearch', result, true, false, mock)
  },
  upgradeDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('upgradeDomain', 'OpenSearch', result, true, true, mock)
  },
  upgradeDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('upgradeDomain', 'OpenSearch', result, false, true, mock)
  },
  upgradeDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('upgradeDomain', 'OpenSearch', result, true, false, mock)
  },
}
