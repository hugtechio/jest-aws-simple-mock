
/**
* @description mocks_v2:mockMediaLive.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockMediaLive = {
  acceptInputDeviceTransfer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptInputDeviceTransfer', 'MediaLive', result, true, true, mock)
  },
  acceptInputDeviceTransferAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptInputDeviceTransfer', 'MediaLive', result, false, true, mock)
  },
  acceptInputDeviceTransferThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acceptInputDeviceTransfer', 'MediaLive', result, true, false, mock)
  },
  batchDelete: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDelete', 'MediaLive', result, true, true, mock)
  },
  batchDeleteAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDelete', 'MediaLive', result, false, true, mock)
  },
  batchDeleteThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDelete', 'MediaLive', result, true, false, mock)
  },
  batchStart: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchStart', 'MediaLive', result, true, true, mock)
  },
  batchStartAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchStart', 'MediaLive', result, false, true, mock)
  },
  batchStartThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchStart', 'MediaLive', result, true, false, mock)
  },
  batchStop: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchStop', 'MediaLive', result, true, true, mock)
  },
  batchStopAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchStop', 'MediaLive', result, false, true, mock)
  },
  batchStopThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchStop', 'MediaLive', result, true, false, mock)
  },
  batchUpdateSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateSchedule', 'MediaLive', result, true, true, mock)
  },
  batchUpdateScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateSchedule', 'MediaLive', result, false, true, mock)
  },
  batchUpdateScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateSchedule', 'MediaLive', result, true, false, mock)
  },
  cancelInputDeviceTransfer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelInputDeviceTransfer', 'MediaLive', result, true, true, mock)
  },
  cancelInputDeviceTransferAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelInputDeviceTransfer', 'MediaLive', result, false, true, mock)
  },
  cancelInputDeviceTransferThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelInputDeviceTransfer', 'MediaLive', result, true, false, mock)
  },
  claimDevice: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('claimDevice', 'MediaLive', result, true, true, mock)
  },
  claimDeviceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('claimDevice', 'MediaLive', result, false, true, mock)
  },
  claimDeviceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('claimDevice', 'MediaLive', result, true, false, mock)
  },
  createChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaLive', result, true, true, mock)
  },
  createChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaLive', result, false, true, mock)
  },
  createChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaLive', result, true, false, mock)
  },
  createInput: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInput', 'MediaLive', result, true, true, mock)
  },
  createInputAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInput', 'MediaLive', result, false, true, mock)
  },
  createInputThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInput', 'MediaLive', result, true, false, mock)
  },
  createInputSecurityGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInputSecurityGroup', 'MediaLive', result, true, true, mock)
  },
  createInputSecurityGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInputSecurityGroup', 'MediaLive', result, false, true, mock)
  },
  createInputSecurityGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createInputSecurityGroup', 'MediaLive', result, true, false, mock)
  },
  createMultiplex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMultiplex', 'MediaLive', result, true, true, mock)
  },
  createMultiplexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMultiplex', 'MediaLive', result, false, true, mock)
  },
  createMultiplexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMultiplex', 'MediaLive', result, true, false, mock)
  },
  createMultiplexProgram: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMultiplexProgram', 'MediaLive', result, true, true, mock)
  },
  createMultiplexProgramAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMultiplexProgram', 'MediaLive', result, false, true, mock)
  },
  createMultiplexProgramThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMultiplexProgram', 'MediaLive', result, true, false, mock)
  },
  createPartnerInput: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPartnerInput', 'MediaLive', result, true, true, mock)
  },
  createPartnerInputAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPartnerInput', 'MediaLive', result, false, true, mock)
  },
  createPartnerInputThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPartnerInput', 'MediaLive', result, true, false, mock)
  },
  createTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTags', 'MediaLive', result, true, true, mock)
  },
  createTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTags', 'MediaLive', result, false, true, mock)
  },
  createTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTags', 'MediaLive', result, true, false, mock)
  },
  deleteChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaLive', result, true, true, mock)
  },
  deleteChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaLive', result, false, true, mock)
  },
  deleteChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaLive', result, true, false, mock)
  },
  deleteInput: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInput', 'MediaLive', result, true, true, mock)
  },
  deleteInputAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInput', 'MediaLive', result, false, true, mock)
  },
  deleteInputThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInput', 'MediaLive', result, true, false, mock)
  },
  deleteInputSecurityGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInputSecurityGroup', 'MediaLive', result, true, true, mock)
  },
  deleteInputSecurityGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInputSecurityGroup', 'MediaLive', result, false, true, mock)
  },
  deleteInputSecurityGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteInputSecurityGroup', 'MediaLive', result, true, false, mock)
  },
  deleteMultiplex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMultiplex', 'MediaLive', result, true, true, mock)
  },
  deleteMultiplexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMultiplex', 'MediaLive', result, false, true, mock)
  },
  deleteMultiplexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMultiplex', 'MediaLive', result, true, false, mock)
  },
  deleteMultiplexProgram: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMultiplexProgram', 'MediaLive', result, true, true, mock)
  },
  deleteMultiplexProgramAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMultiplexProgram', 'MediaLive', result, false, true, mock)
  },
  deleteMultiplexProgramThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteMultiplexProgram', 'MediaLive', result, true, false, mock)
  },
  deleteReservation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReservation', 'MediaLive', result, true, true, mock)
  },
  deleteReservationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReservation', 'MediaLive', result, false, true, mock)
  },
  deleteReservationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReservation', 'MediaLive', result, true, false, mock)
  },
  deleteSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchedule', 'MediaLive', result, true, true, mock)
  },
  deleteScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchedule', 'MediaLive', result, false, true, mock)
  },
  deleteScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchedule', 'MediaLive', result, true, false, mock)
  },
  deleteTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTags', 'MediaLive', result, true, true, mock)
  },
  deleteTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTags', 'MediaLive', result, false, true, mock)
  },
  deleteTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTags', 'MediaLive', result, true, false, mock)
  },
  describeChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaLive', result, true, true, mock)
  },
  describeChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaLive', result, false, true, mock)
  },
  describeChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaLive', result, true, false, mock)
  },
  describeInput: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInput', 'MediaLive', result, true, true, mock)
  },
  describeInputAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInput', 'MediaLive', result, false, true, mock)
  },
  describeInputThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInput', 'MediaLive', result, true, false, mock)
  },
  describeInputDevice: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputDevice', 'MediaLive', result, true, true, mock)
  },
  describeInputDeviceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputDevice', 'MediaLive', result, false, true, mock)
  },
  describeInputDeviceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputDevice', 'MediaLive', result, true, false, mock)
  },
  describeInputDeviceThumbnail: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputDeviceThumbnail', 'MediaLive', result, true, true, mock)
  },
  describeInputDeviceThumbnailAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputDeviceThumbnail', 'MediaLive', result, false, true, mock)
  },
  describeInputDeviceThumbnailThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputDeviceThumbnail', 'MediaLive', result, true, false, mock)
  },
  describeInputSecurityGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputSecurityGroup', 'MediaLive', result, true, true, mock)
  },
  describeInputSecurityGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputSecurityGroup', 'MediaLive', result, false, true, mock)
  },
  describeInputSecurityGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeInputSecurityGroup', 'MediaLive', result, true, false, mock)
  },
  describeMultiplex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMultiplex', 'MediaLive', result, true, true, mock)
  },
  describeMultiplexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMultiplex', 'MediaLive', result, false, true, mock)
  },
  describeMultiplexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMultiplex', 'MediaLive', result, true, false, mock)
  },
  describeMultiplexProgram: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMultiplexProgram', 'MediaLive', result, true, true, mock)
  },
  describeMultiplexProgramAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMultiplexProgram', 'MediaLive', result, false, true, mock)
  },
  describeMultiplexProgramThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMultiplexProgram', 'MediaLive', result, true, false, mock)
  },
  describeOffering: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOffering', 'MediaLive', result, true, true, mock)
  },
  describeOfferingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOffering', 'MediaLive', result, false, true, mock)
  },
  describeOfferingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeOffering', 'MediaLive', result, true, false, mock)
  },
  describeReservation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservation', 'MediaLive', result, true, true, mock)
  },
  describeReservationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservation', 'MediaLive', result, false, true, mock)
  },
  describeReservationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeReservation', 'MediaLive', result, true, false, mock)
  },
  describeSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSchedule', 'MediaLive', result, true, true, mock)
  },
  describeScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSchedule', 'MediaLive', result, false, true, mock)
  },
  describeScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSchedule', 'MediaLive', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaLive', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaLive', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaLive', result, true, false, mock)
  },
  listChannels: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaLive', result, true, true, mock)
  },
  listChannelsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaLive', result, false, true, mock)
  },
  listChannelsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaLive', result, true, false, mock)
  },
  listInputDeviceTransfers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputDeviceTransfers', 'MediaLive', result, true, true, mock)
  },
  listInputDeviceTransfersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputDeviceTransfers', 'MediaLive', result, false, true, mock)
  },
  listInputDeviceTransfersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputDeviceTransfers', 'MediaLive', result, true, false, mock)
  },
  listInputDevices: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputDevices', 'MediaLive', result, true, true, mock)
  },
  listInputDevicesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputDevices', 'MediaLive', result, false, true, mock)
  },
  listInputDevicesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputDevices', 'MediaLive', result, true, false, mock)
  },
  listInputSecurityGroups: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputSecurityGroups', 'MediaLive', result, true, true, mock)
  },
  listInputSecurityGroupsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputSecurityGroups', 'MediaLive', result, false, true, mock)
  },
  listInputSecurityGroupsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputSecurityGroups', 'MediaLive', result, true, false, mock)
  },
  listInputs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputs', 'MediaLive', result, true, true, mock)
  },
  listInputsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputs', 'MediaLive', result, false, true, mock)
  },
  listInputsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listInputs', 'MediaLive', result, true, false, mock)
  },
  listMultiplexPrograms: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMultiplexPrograms', 'MediaLive', result, true, true, mock)
  },
  listMultiplexProgramsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMultiplexPrograms', 'MediaLive', result, false, true, mock)
  },
  listMultiplexProgramsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMultiplexPrograms', 'MediaLive', result, true, false, mock)
  },
  listMultiplexes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMultiplexes', 'MediaLive', result, true, true, mock)
  },
  listMultiplexesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMultiplexes', 'MediaLive', result, false, true, mock)
  },
  listMultiplexesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMultiplexes', 'MediaLive', result, true, false, mock)
  },
  listOfferings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOfferings', 'MediaLive', result, true, true, mock)
  },
  listOfferingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOfferings', 'MediaLive', result, false, true, mock)
  },
  listOfferingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOfferings', 'MediaLive', result, true, false, mock)
  },
  listReservations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReservations', 'MediaLive', result, true, true, mock)
  },
  listReservationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReservations', 'MediaLive', result, false, true, mock)
  },
  listReservationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReservations', 'MediaLive', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaLive', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaLive', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaLive', result, true, false, mock)
  },
  purchaseOffering: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseOffering', 'MediaLive', result, true, true, mock)
  },
  purchaseOfferingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseOffering', 'MediaLive', result, false, true, mock)
  },
  purchaseOfferingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('purchaseOffering', 'MediaLive', result, true, false, mock)
  },
  rejectInputDeviceTransfer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInputDeviceTransfer', 'MediaLive', result, true, true, mock)
  },
  rejectInputDeviceTransferAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInputDeviceTransfer', 'MediaLive', result, false, true, mock)
  },
  rejectInputDeviceTransferThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rejectInputDeviceTransfer', 'MediaLive', result, true, false, mock)
  },
  startChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChannel', 'MediaLive', result, true, true, mock)
  },
  startChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChannel', 'MediaLive', result, false, true, mock)
  },
  startChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChannel', 'MediaLive', result, true, false, mock)
  },
  startMultiplex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startMultiplex', 'MediaLive', result, true, true, mock)
  },
  startMultiplexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startMultiplex', 'MediaLive', result, false, true, mock)
  },
  startMultiplexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startMultiplex', 'MediaLive', result, true, false, mock)
  },
  stopChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopChannel', 'MediaLive', result, true, true, mock)
  },
  stopChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopChannel', 'MediaLive', result, false, true, mock)
  },
  stopChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopChannel', 'MediaLive', result, true, false, mock)
  },
  stopMultiplex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopMultiplex', 'MediaLive', result, true, true, mock)
  },
  stopMultiplexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopMultiplex', 'MediaLive', result, false, true, mock)
  },
  stopMultiplexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopMultiplex', 'MediaLive', result, true, false, mock)
  },
  transferInputDevice: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('transferInputDevice', 'MediaLive', result, true, true, mock)
  },
  transferInputDeviceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('transferInputDevice', 'MediaLive', result, false, true, mock)
  },
  transferInputDeviceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('transferInputDevice', 'MediaLive', result, true, false, mock)
  },
  updateChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaLive', result, true, true, mock)
  },
  updateChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaLive', result, false, true, mock)
  },
  updateChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaLive', result, true, false, mock)
  },
  updateChannelClass: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannelClass', 'MediaLive', result, true, true, mock)
  },
  updateChannelClassAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannelClass', 'MediaLive', result, false, true, mock)
  },
  updateChannelClassThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannelClass', 'MediaLive', result, true, false, mock)
  },
  updateInput: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInput', 'MediaLive', result, true, true, mock)
  },
  updateInputAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInput', 'MediaLive', result, false, true, mock)
  },
  updateInputThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInput', 'MediaLive', result, true, false, mock)
  },
  updateInputDevice: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInputDevice', 'MediaLive', result, true, true, mock)
  },
  updateInputDeviceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInputDevice', 'MediaLive', result, false, true, mock)
  },
  updateInputDeviceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInputDevice', 'MediaLive', result, true, false, mock)
  },
  updateInputSecurityGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInputSecurityGroup', 'MediaLive', result, true, true, mock)
  },
  updateInputSecurityGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInputSecurityGroup', 'MediaLive', result, false, true, mock)
  },
  updateInputSecurityGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateInputSecurityGroup', 'MediaLive', result, true, false, mock)
  },
  updateMultiplex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMultiplex', 'MediaLive', result, true, true, mock)
  },
  updateMultiplexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMultiplex', 'MediaLive', result, false, true, mock)
  },
  updateMultiplexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMultiplex', 'MediaLive', result, true, false, mock)
  },
  updateMultiplexProgram: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMultiplexProgram', 'MediaLive', result, true, true, mock)
  },
  updateMultiplexProgramAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMultiplexProgram', 'MediaLive', result, false, true, mock)
  },
  updateMultiplexProgramThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMultiplexProgram', 'MediaLive', result, true, false, mock)
  },
  updateReservation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReservation', 'MediaLive', result, true, true, mock)
  },
  updateReservationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReservation', 'MediaLive', result, false, true, mock)
  },
  updateReservationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReservation', 'MediaLive', result, true, false, mock)
  },
}
