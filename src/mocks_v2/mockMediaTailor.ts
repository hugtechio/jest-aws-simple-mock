
/**
* @description mocks_v2:mockMediaTailor.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockMediaTailor = {
  configureLogsForPlaybackConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('configureLogsForPlaybackConfiguration', 'MediaTailor', result, true, true, mock)
  },
  configureLogsForPlaybackConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('configureLogsForPlaybackConfiguration', 'MediaTailor', result, false, true, mock)
  },
  configureLogsForPlaybackConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('configureLogsForPlaybackConfiguration', 'MediaTailor', result, true, false, mock)
  },
  createChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaTailor', result, true, true, mock)
  },
  createChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaTailor', result, false, true, mock)
  },
  createChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createChannel', 'MediaTailor', result, true, false, mock)
  },
  createPrefetchSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPrefetchSchedule', 'MediaTailor', result, true, true, mock)
  },
  createPrefetchScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPrefetchSchedule', 'MediaTailor', result, false, true, mock)
  },
  createPrefetchScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPrefetchSchedule', 'MediaTailor', result, true, false, mock)
  },
  createProgram: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProgram', 'MediaTailor', result, true, true, mock)
  },
  createProgramAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProgram', 'MediaTailor', result, false, true, mock)
  },
  createProgramThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProgram', 'MediaTailor', result, true, false, mock)
  },
  createSourceLocation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSourceLocation', 'MediaTailor', result, true, true, mock)
  },
  createSourceLocationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSourceLocation', 'MediaTailor', result, false, true, mock)
  },
  createSourceLocationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSourceLocation', 'MediaTailor', result, true, false, mock)
  },
  createVodSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVodSource', 'MediaTailor', result, true, true, mock)
  },
  createVodSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVodSource', 'MediaTailor', result, false, true, mock)
  },
  createVodSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createVodSource', 'MediaTailor', result, true, false, mock)
  },
  deleteChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaTailor', result, true, true, mock)
  },
  deleteChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaTailor', result, false, true, mock)
  },
  deleteChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannel', 'MediaTailor', result, true, false, mock)
  },
  deleteChannelPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannelPolicy', 'MediaTailor', result, true, true, mock)
  },
  deleteChannelPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannelPolicy', 'MediaTailor', result, false, true, mock)
  },
  deleteChannelPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteChannelPolicy', 'MediaTailor', result, true, false, mock)
  },
  deletePlaybackConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePlaybackConfiguration', 'MediaTailor', result, true, true, mock)
  },
  deletePlaybackConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePlaybackConfiguration', 'MediaTailor', result, false, true, mock)
  },
  deletePlaybackConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePlaybackConfiguration', 'MediaTailor', result, true, false, mock)
  },
  deletePrefetchSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePrefetchSchedule', 'MediaTailor', result, true, true, mock)
  },
  deletePrefetchScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePrefetchSchedule', 'MediaTailor', result, false, true, mock)
  },
  deletePrefetchScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePrefetchSchedule', 'MediaTailor', result, true, false, mock)
  },
  deleteProgram: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProgram', 'MediaTailor', result, true, true, mock)
  },
  deleteProgramAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProgram', 'MediaTailor', result, false, true, mock)
  },
  deleteProgramThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProgram', 'MediaTailor', result, true, false, mock)
  },
  deleteSourceLocation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSourceLocation', 'MediaTailor', result, true, true, mock)
  },
  deleteSourceLocationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSourceLocation', 'MediaTailor', result, false, true, mock)
  },
  deleteSourceLocationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSourceLocation', 'MediaTailor', result, true, false, mock)
  },
  deleteVodSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVodSource', 'MediaTailor', result, true, true, mock)
  },
  deleteVodSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVodSource', 'MediaTailor', result, false, true, mock)
  },
  deleteVodSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteVodSource', 'MediaTailor', result, true, false, mock)
  },
  describeChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaTailor', result, true, true, mock)
  },
  describeChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaTailor', result, false, true, mock)
  },
  describeChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeChannel', 'MediaTailor', result, true, false, mock)
  },
  describeProgram: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeProgram', 'MediaTailor', result, true, true, mock)
  },
  describeProgramAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeProgram', 'MediaTailor', result, false, true, mock)
  },
  describeProgramThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeProgram', 'MediaTailor', result, true, false, mock)
  },
  describeSourceLocation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSourceLocation', 'MediaTailor', result, true, true, mock)
  },
  describeSourceLocationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSourceLocation', 'MediaTailor', result, false, true, mock)
  },
  describeSourceLocationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSourceLocation', 'MediaTailor', result, true, false, mock)
  },
  describeVodSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVodSource', 'MediaTailor', result, true, true, mock)
  },
  describeVodSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVodSource', 'MediaTailor', result, false, true, mock)
  },
  describeVodSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVodSource', 'MediaTailor', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaTailor', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaTailor', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'MediaTailor', result, true, false, mock)
  },
  getChannelPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getChannelPolicy', 'MediaTailor', result, true, true, mock)
  },
  getChannelPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getChannelPolicy', 'MediaTailor', result, false, true, mock)
  },
  getChannelPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getChannelPolicy', 'MediaTailor', result, true, false, mock)
  },
  getChannelSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getChannelSchedule', 'MediaTailor', result, true, true, mock)
  },
  getChannelScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getChannelSchedule', 'MediaTailor', result, false, true, mock)
  },
  getChannelScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getChannelSchedule', 'MediaTailor', result, true, false, mock)
  },
  getPlaybackConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPlaybackConfiguration', 'MediaTailor', result, true, true, mock)
  },
  getPlaybackConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPlaybackConfiguration', 'MediaTailor', result, false, true, mock)
  },
  getPlaybackConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPlaybackConfiguration', 'MediaTailor', result, true, false, mock)
  },
  getPrefetchSchedule: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPrefetchSchedule', 'MediaTailor', result, true, true, mock)
  },
  getPrefetchScheduleAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPrefetchSchedule', 'MediaTailor', result, false, true, mock)
  },
  getPrefetchScheduleThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPrefetchSchedule', 'MediaTailor', result, true, false, mock)
  },
  listAlerts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlerts', 'MediaTailor', result, true, true, mock)
  },
  listAlertsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlerts', 'MediaTailor', result, false, true, mock)
  },
  listAlertsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlerts', 'MediaTailor', result, true, false, mock)
  },
  listChannels: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaTailor', result, true, true, mock)
  },
  listChannelsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaTailor', result, false, true, mock)
  },
  listChannelsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listChannels', 'MediaTailor', result, true, false, mock)
  },
  listPlaybackConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPlaybackConfigurations', 'MediaTailor', result, true, true, mock)
  },
  listPlaybackConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPlaybackConfigurations', 'MediaTailor', result, false, true, mock)
  },
  listPlaybackConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPlaybackConfigurations', 'MediaTailor', result, true, false, mock)
  },
  listPrefetchSchedules: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPrefetchSchedules', 'MediaTailor', result, true, true, mock)
  },
  listPrefetchSchedulesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPrefetchSchedules', 'MediaTailor', result, false, true, mock)
  },
  listPrefetchSchedulesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPrefetchSchedules', 'MediaTailor', result, true, false, mock)
  },
  listSourceLocations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSourceLocations', 'MediaTailor', result, true, true, mock)
  },
  listSourceLocationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSourceLocations', 'MediaTailor', result, false, true, mock)
  },
  listSourceLocationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSourceLocations', 'MediaTailor', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaTailor', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaTailor', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'MediaTailor', result, true, false, mock)
  },
  listVodSources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVodSources', 'MediaTailor', result, true, true, mock)
  },
  listVodSourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVodSources', 'MediaTailor', result, false, true, mock)
  },
  listVodSourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listVodSources', 'MediaTailor', result, true, false, mock)
  },
  putChannelPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putChannelPolicy', 'MediaTailor', result, true, true, mock)
  },
  putChannelPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putChannelPolicy', 'MediaTailor', result, false, true, mock)
  },
  putChannelPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putChannelPolicy', 'MediaTailor', result, true, false, mock)
  },
  putPlaybackConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPlaybackConfiguration', 'MediaTailor', result, true, true, mock)
  },
  putPlaybackConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPlaybackConfiguration', 'MediaTailor', result, false, true, mock)
  },
  putPlaybackConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPlaybackConfiguration', 'MediaTailor', result, true, false, mock)
  },
  startChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChannel', 'MediaTailor', result, true, true, mock)
  },
  startChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChannel', 'MediaTailor', result, false, true, mock)
  },
  startChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startChannel', 'MediaTailor', result, true, false, mock)
  },
  stopChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopChannel', 'MediaTailor', result, true, true, mock)
  },
  stopChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopChannel', 'MediaTailor', result, false, true, mock)
  },
  stopChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopChannel', 'MediaTailor', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaTailor', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaTailor', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'MediaTailor', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaTailor', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaTailor', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'MediaTailor', result, true, false, mock)
  },
  updateChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaTailor', result, true, true, mock)
  },
  updateChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaTailor', result, false, true, mock)
  },
  updateChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateChannel', 'MediaTailor', result, true, false, mock)
  },
  updateSourceLocation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSourceLocation', 'MediaTailor', result, true, true, mock)
  },
  updateSourceLocationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSourceLocation', 'MediaTailor', result, false, true, mock)
  },
  updateSourceLocationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSourceLocation', 'MediaTailor', result, true, false, mock)
  },
  updateVodSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVodSource', 'MediaTailor', result, true, true, mock)
  },
  updateVodSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVodSource', 'MediaTailor', result, false, true, mock)
  },
  updateVodSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateVodSource', 'MediaTailor', result, true, false, mock)
  },
}
