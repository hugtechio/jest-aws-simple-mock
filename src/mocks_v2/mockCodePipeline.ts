
/**
* @description mocks_v2:mockCodePipeline.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockCodePipeline = {
  acknowledgeJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acknowledgeJob', 'CodePipeline', result, true, true, mock)
  },
  acknowledgeJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acknowledgeJob', 'CodePipeline', result, false, true, mock)
  },
  acknowledgeJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acknowledgeJob', 'CodePipeline', result, true, false, mock)
  },
  acknowledgeThirdPartyJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acknowledgeThirdPartyJob', 'CodePipeline', result, true, true, mock)
  },
  acknowledgeThirdPartyJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acknowledgeThirdPartyJob', 'CodePipeline', result, false, true, mock)
  },
  acknowledgeThirdPartyJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('acknowledgeThirdPartyJob', 'CodePipeline', result, true, false, mock)
  },
  createCustomActionType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCustomActionType', 'CodePipeline', result, true, true, mock)
  },
  createCustomActionTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCustomActionType', 'CodePipeline', result, false, true, mock)
  },
  createCustomActionTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCustomActionType', 'CodePipeline', result, true, false, mock)
  },
  createPipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPipeline', 'CodePipeline', result, true, true, mock)
  },
  createPipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPipeline', 'CodePipeline', result, false, true, mock)
  },
  createPipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createPipeline', 'CodePipeline', result, true, false, mock)
  },
  deleteCustomActionType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCustomActionType', 'CodePipeline', result, true, true, mock)
  },
  deleteCustomActionTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCustomActionType', 'CodePipeline', result, false, true, mock)
  },
  deleteCustomActionTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCustomActionType', 'CodePipeline', result, true, false, mock)
  },
  deletePipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePipeline', 'CodePipeline', result, true, true, mock)
  },
  deletePipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePipeline', 'CodePipeline', result, false, true, mock)
  },
  deletePipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePipeline', 'CodePipeline', result, true, false, mock)
  },
  deleteWebhook: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWebhook', 'CodePipeline', result, true, true, mock)
  },
  deleteWebhookAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWebhook', 'CodePipeline', result, false, true, mock)
  },
  deleteWebhookThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWebhook', 'CodePipeline', result, true, false, mock)
  },
  deregisterWebhookWithThirdParty: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterWebhookWithThirdParty', 'CodePipeline', result, true, true, mock)
  },
  deregisterWebhookWithThirdPartyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterWebhookWithThirdParty', 'CodePipeline', result, false, true, mock)
  },
  deregisterWebhookWithThirdPartyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterWebhookWithThirdParty', 'CodePipeline', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CodePipeline', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CodePipeline', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CodePipeline', result, true, false, mock)
  },
  disableStageTransition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableStageTransition', 'CodePipeline', result, true, true, mock)
  },
  disableStageTransitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableStageTransition', 'CodePipeline', result, false, true, mock)
  },
  disableStageTransitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disableStageTransition', 'CodePipeline', result, true, false, mock)
  },
  enableStageTransition: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableStageTransition', 'CodePipeline', result, true, true, mock)
  },
  enableStageTransitionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableStageTransition', 'CodePipeline', result, false, true, mock)
  },
  enableStageTransitionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('enableStageTransition', 'CodePipeline', result, true, false, mock)
  },
  getActionType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getActionType', 'CodePipeline', result, true, true, mock)
  },
  getActionTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getActionType', 'CodePipeline', result, false, true, mock)
  },
  getActionTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getActionType', 'CodePipeline', result, true, false, mock)
  },
  getJobDetails: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJobDetails', 'CodePipeline', result, true, true, mock)
  },
  getJobDetailsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJobDetails', 'CodePipeline', result, false, true, mock)
  },
  getJobDetailsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJobDetails', 'CodePipeline', result, true, false, mock)
  },
  getPipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipeline', 'CodePipeline', result, true, true, mock)
  },
  getPipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipeline', 'CodePipeline', result, false, true, mock)
  },
  getPipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipeline', 'CodePipeline', result, true, false, mock)
  },
  getPipelineExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineExecution', 'CodePipeline', result, true, true, mock)
  },
  getPipelineExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineExecution', 'CodePipeline', result, false, true, mock)
  },
  getPipelineExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineExecution', 'CodePipeline', result, true, false, mock)
  },
  getPipelineState: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineState', 'CodePipeline', result, true, true, mock)
  },
  getPipelineStateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineState', 'CodePipeline', result, false, true, mock)
  },
  getPipelineStateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPipelineState', 'CodePipeline', result, true, false, mock)
  },
  getThirdPartyJobDetails: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getThirdPartyJobDetails', 'CodePipeline', result, true, true, mock)
  },
  getThirdPartyJobDetailsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getThirdPartyJobDetails', 'CodePipeline', result, false, true, mock)
  },
  getThirdPartyJobDetailsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getThirdPartyJobDetails', 'CodePipeline', result, true, false, mock)
  },
  listActionExecutions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listActionExecutions', 'CodePipeline', result, true, true, mock)
  },
  listActionExecutionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listActionExecutions', 'CodePipeline', result, false, true, mock)
  },
  listActionExecutionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listActionExecutions', 'CodePipeline', result, true, false, mock)
  },
  listActionTypes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listActionTypes', 'CodePipeline', result, true, true, mock)
  },
  listActionTypesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listActionTypes', 'CodePipeline', result, false, true, mock)
  },
  listActionTypesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listActionTypes', 'CodePipeline', result, true, false, mock)
  },
  listPipelineExecutions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelineExecutions', 'CodePipeline', result, true, true, mock)
  },
  listPipelineExecutionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelineExecutions', 'CodePipeline', result, false, true, mock)
  },
  listPipelineExecutionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelineExecutions', 'CodePipeline', result, true, false, mock)
  },
  listPipelines: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelines', 'CodePipeline', result, true, true, mock)
  },
  listPipelinesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelines', 'CodePipeline', result, false, true, mock)
  },
  listPipelinesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPipelines', 'CodePipeline', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'CodePipeline', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'CodePipeline', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'CodePipeline', result, true, false, mock)
  },
  listWebhooks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWebhooks', 'CodePipeline', result, true, true, mock)
  },
  listWebhooksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWebhooks', 'CodePipeline', result, false, true, mock)
  },
  listWebhooksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWebhooks', 'CodePipeline', result, true, false, mock)
  },
  pollForJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForJobs', 'CodePipeline', result, true, true, mock)
  },
  pollForJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForJobs', 'CodePipeline', result, false, true, mock)
  },
  pollForJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForJobs', 'CodePipeline', result, true, false, mock)
  },
  pollForThirdPartyJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForThirdPartyJobs', 'CodePipeline', result, true, true, mock)
  },
  pollForThirdPartyJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForThirdPartyJobs', 'CodePipeline', result, false, true, mock)
  },
  pollForThirdPartyJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pollForThirdPartyJobs', 'CodePipeline', result, true, false, mock)
  },
  putActionRevision: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putActionRevision', 'CodePipeline', result, true, true, mock)
  },
  putActionRevisionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putActionRevision', 'CodePipeline', result, false, true, mock)
  },
  putActionRevisionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putActionRevision', 'CodePipeline', result, true, false, mock)
  },
  putApprovalResult: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putApprovalResult', 'CodePipeline', result, true, true, mock)
  },
  putApprovalResultAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putApprovalResult', 'CodePipeline', result, false, true, mock)
  },
  putApprovalResultThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putApprovalResult', 'CodePipeline', result, true, false, mock)
  },
  putJobFailureResult: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putJobFailureResult', 'CodePipeline', result, true, true, mock)
  },
  putJobFailureResultAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putJobFailureResult', 'CodePipeline', result, false, true, mock)
  },
  putJobFailureResultThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putJobFailureResult', 'CodePipeline', result, true, false, mock)
  },
  putJobSuccessResult: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putJobSuccessResult', 'CodePipeline', result, true, true, mock)
  },
  putJobSuccessResultAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putJobSuccessResult', 'CodePipeline', result, false, true, mock)
  },
  putJobSuccessResultThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putJobSuccessResult', 'CodePipeline', result, true, false, mock)
  },
  putThirdPartyJobFailureResult: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putThirdPartyJobFailureResult', 'CodePipeline', result, true, true, mock)
  },
  putThirdPartyJobFailureResultAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putThirdPartyJobFailureResult', 'CodePipeline', result, false, true, mock)
  },
  putThirdPartyJobFailureResultThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putThirdPartyJobFailureResult', 'CodePipeline', result, true, false, mock)
  },
  putThirdPartyJobSuccessResult: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putThirdPartyJobSuccessResult', 'CodePipeline', result, true, true, mock)
  },
  putThirdPartyJobSuccessResultAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putThirdPartyJobSuccessResult', 'CodePipeline', result, false, true, mock)
  },
  putThirdPartyJobSuccessResultThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putThirdPartyJobSuccessResult', 'CodePipeline', result, true, false, mock)
  },
  putWebhook: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putWebhook', 'CodePipeline', result, true, true, mock)
  },
  putWebhookAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putWebhook', 'CodePipeline', result, false, true, mock)
  },
  putWebhookThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putWebhook', 'CodePipeline', result, true, false, mock)
  },
  registerWebhookWithThirdParty: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerWebhookWithThirdParty', 'CodePipeline', result, true, true, mock)
  },
  registerWebhookWithThirdPartyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerWebhookWithThirdParty', 'CodePipeline', result, false, true, mock)
  },
  registerWebhookWithThirdPartyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerWebhookWithThirdParty', 'CodePipeline', result, true, false, mock)
  },
  retryStageExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retryStageExecution', 'CodePipeline', result, true, true, mock)
  },
  retryStageExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retryStageExecution', 'CodePipeline', result, false, true, mock)
  },
  retryStageExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('retryStageExecution', 'CodePipeline', result, true, false, mock)
  },
  startPipelineExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startPipelineExecution', 'CodePipeline', result, true, true, mock)
  },
  startPipelineExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startPipelineExecution', 'CodePipeline', result, false, true, mock)
  },
  startPipelineExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startPipelineExecution', 'CodePipeline', result, true, false, mock)
  },
  stopPipelineExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopPipelineExecution', 'CodePipeline', result, true, true, mock)
  },
  stopPipelineExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopPipelineExecution', 'CodePipeline', result, false, true, mock)
  },
  stopPipelineExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopPipelineExecution', 'CodePipeline', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'CodePipeline', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'CodePipeline', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'CodePipeline', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'CodePipeline', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'CodePipeline', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'CodePipeline', result, true, false, mock)
  },
  updateActionType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateActionType', 'CodePipeline', result, true, true, mock)
  },
  updateActionTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateActionType', 'CodePipeline', result, false, true, mock)
  },
  updateActionTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateActionType', 'CodePipeline', result, true, false, mock)
  },
  updatePipeline: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePipeline', 'CodePipeline', result, true, true, mock)
  },
  updatePipelineAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePipeline', 'CodePipeline', result, false, true, mock)
  },
  updatePipelineThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePipeline', 'CodePipeline', result, true, false, mock)
  },
}
