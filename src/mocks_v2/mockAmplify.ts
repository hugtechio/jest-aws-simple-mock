
/**
* @description mocks_v2:mockAmplify.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockAmplify = {
  createApp: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApp', 'Amplify', result, true, true, mock)
  },
  createAppAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApp', 'Amplify', result, false, true, mock)
  },
  createAppThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApp', 'Amplify', result, true, false, mock)
  },
  createBackendEnvironment: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendEnvironment', 'Amplify', result, true, true, mock)
  },
  createBackendEnvironmentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendEnvironment', 'Amplify', result, false, true, mock)
  },
  createBackendEnvironmentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBackendEnvironment', 'Amplify', result, true, false, mock)
  },
  createBranch: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBranch', 'Amplify', result, true, true, mock)
  },
  createBranchAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBranch', 'Amplify', result, false, true, mock)
  },
  createBranchThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createBranch', 'Amplify', result, true, false, mock)
  },
  createDeployment: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDeployment', 'Amplify', result, true, true, mock)
  },
  createDeploymentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDeployment', 'Amplify', result, false, true, mock)
  },
  createDeploymentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDeployment', 'Amplify', result, true, false, mock)
  },
  createDomainAssociation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomainAssociation', 'Amplify', result, true, true, mock)
  },
  createDomainAssociationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomainAssociation', 'Amplify', result, false, true, mock)
  },
  createDomainAssociationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomainAssociation', 'Amplify', result, true, false, mock)
  },
  createWebhook: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWebhook', 'Amplify', result, true, true, mock)
  },
  createWebhookAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWebhook', 'Amplify', result, false, true, mock)
  },
  createWebhookThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWebhook', 'Amplify', result, true, false, mock)
  },
  deleteApp: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApp', 'Amplify', result, true, true, mock)
  },
  deleteAppAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApp', 'Amplify', result, false, true, mock)
  },
  deleteAppThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApp', 'Amplify', result, true, false, mock)
  },
  deleteBackendEnvironment: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendEnvironment', 'Amplify', result, true, true, mock)
  },
  deleteBackendEnvironmentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendEnvironment', 'Amplify', result, false, true, mock)
  },
  deleteBackendEnvironmentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBackendEnvironment', 'Amplify', result, true, false, mock)
  },
  deleteBranch: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBranch', 'Amplify', result, true, true, mock)
  },
  deleteBranchAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBranch', 'Amplify', result, false, true, mock)
  },
  deleteBranchThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteBranch', 'Amplify', result, true, false, mock)
  },
  deleteDomainAssociation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomainAssociation', 'Amplify', result, true, true, mock)
  },
  deleteDomainAssociationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomainAssociation', 'Amplify', result, false, true, mock)
  },
  deleteDomainAssociationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomainAssociation', 'Amplify', result, true, false, mock)
  },
  deleteJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteJob', 'Amplify', result, true, true, mock)
  },
  deleteJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteJob', 'Amplify', result, false, true, mock)
  },
  deleteJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteJob', 'Amplify', result, true, false, mock)
  },
  deleteWebhook: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWebhook', 'Amplify', result, true, true, mock)
  },
  deleteWebhookAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWebhook', 'Amplify', result, false, true, mock)
  },
  deleteWebhookThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWebhook', 'Amplify', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Amplify', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Amplify', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Amplify', result, true, false, mock)
  },
  generateAccessLogs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('generateAccessLogs', 'Amplify', result, true, true, mock)
  },
  generateAccessLogsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('generateAccessLogs', 'Amplify', result, false, true, mock)
  },
  generateAccessLogsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('generateAccessLogs', 'Amplify', result, true, false, mock)
  },
  getApp: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApp', 'Amplify', result, true, true, mock)
  },
  getAppAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApp', 'Amplify', result, false, true, mock)
  },
  getAppThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApp', 'Amplify', result, true, false, mock)
  },
  getArtifactUrl: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getArtifactUrl', 'Amplify', result, true, true, mock)
  },
  getArtifactUrlAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getArtifactUrl', 'Amplify', result, false, true, mock)
  },
  getArtifactUrlThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getArtifactUrl', 'Amplify', result, true, false, mock)
  },
  getBackendEnvironment: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendEnvironment', 'Amplify', result, true, true, mock)
  },
  getBackendEnvironmentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendEnvironment', 'Amplify', result, false, true, mock)
  },
  getBackendEnvironmentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBackendEnvironment', 'Amplify', result, true, false, mock)
  },
  getBranch: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBranch', 'Amplify', result, true, true, mock)
  },
  getBranchAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBranch', 'Amplify', result, false, true, mock)
  },
  getBranchThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBranch', 'Amplify', result, true, false, mock)
  },
  getDomainAssociation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDomainAssociation', 'Amplify', result, true, true, mock)
  },
  getDomainAssociationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDomainAssociation', 'Amplify', result, false, true, mock)
  },
  getDomainAssociationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDomainAssociation', 'Amplify', result, true, false, mock)
  },
  getJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJob', 'Amplify', result, true, true, mock)
  },
  getJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJob', 'Amplify', result, false, true, mock)
  },
  getJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getJob', 'Amplify', result, true, false, mock)
  },
  getWebhook: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getWebhook', 'Amplify', result, true, true, mock)
  },
  getWebhookAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getWebhook', 'Amplify', result, false, true, mock)
  },
  getWebhookThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getWebhook', 'Amplify', result, true, false, mock)
  },
  listApps: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApps', 'Amplify', result, true, true, mock)
  },
  listAppsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApps', 'Amplify', result, false, true, mock)
  },
  listAppsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApps', 'Amplify', result, true, false, mock)
  },
  listArtifacts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listArtifacts', 'Amplify', result, true, true, mock)
  },
  listArtifactsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listArtifacts', 'Amplify', result, false, true, mock)
  },
  listArtifactsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listArtifacts', 'Amplify', result, true, false, mock)
  },
  listBackendEnvironments: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBackendEnvironments', 'Amplify', result, true, true, mock)
  },
  listBackendEnvironmentsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBackendEnvironments', 'Amplify', result, false, true, mock)
  },
  listBackendEnvironmentsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBackendEnvironments', 'Amplify', result, true, false, mock)
  },
  listBranches: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBranches', 'Amplify', result, true, true, mock)
  },
  listBranchesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBranches', 'Amplify', result, false, true, mock)
  },
  listBranchesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listBranches', 'Amplify', result, true, false, mock)
  },
  listDomainAssociations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainAssociations', 'Amplify', result, true, true, mock)
  },
  listDomainAssociationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainAssociations', 'Amplify', result, false, true, mock)
  },
  listDomainAssociationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainAssociations', 'Amplify', result, true, false, mock)
  },
  listJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listJobs', 'Amplify', result, true, true, mock)
  },
  listJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listJobs', 'Amplify', result, false, true, mock)
  },
  listJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listJobs', 'Amplify', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Amplify', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Amplify', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Amplify', result, true, false, mock)
  },
  listWebhooks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWebhooks', 'Amplify', result, true, true, mock)
  },
  listWebhooksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWebhooks', 'Amplify', result, false, true, mock)
  },
  listWebhooksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWebhooks', 'Amplify', result, true, false, mock)
  },
  startDeployment: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDeployment', 'Amplify', result, true, true, mock)
  },
  startDeploymentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDeployment', 'Amplify', result, false, true, mock)
  },
  startDeploymentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDeployment', 'Amplify', result, true, false, mock)
  },
  startJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startJob', 'Amplify', result, true, true, mock)
  },
  startJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startJob', 'Amplify', result, false, true, mock)
  },
  startJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startJob', 'Amplify', result, true, false, mock)
  },
  stopJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopJob', 'Amplify', result, true, true, mock)
  },
  stopJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopJob', 'Amplify', result, false, true, mock)
  },
  stopJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopJob', 'Amplify', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Amplify', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Amplify', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Amplify', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Amplify', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Amplify', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Amplify', result, true, false, mock)
  },
  updateApp: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApp', 'Amplify', result, true, true, mock)
  },
  updateAppAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApp', 'Amplify', result, false, true, mock)
  },
  updateAppThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApp', 'Amplify', result, true, false, mock)
  },
  updateBranch: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBranch', 'Amplify', result, true, true, mock)
  },
  updateBranchAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBranch', 'Amplify', result, false, true, mock)
  },
  updateBranchThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBranch', 'Amplify', result, true, false, mock)
  },
  updateDomainAssociation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainAssociation', 'Amplify', result, true, true, mock)
  },
  updateDomainAssociationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainAssociation', 'Amplify', result, false, true, mock)
  },
  updateDomainAssociationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainAssociation', 'Amplify', result, true, false, mock)
  },
  updateWebhook: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWebhook', 'Amplify', result, true, true, mock)
  },
  updateWebhookAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWebhook', 'Amplify', result, false, true, mock)
  },
  updateWebhookThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWebhook', 'Amplify', result, true, false, mock)
  },
}
