
/**
* @description mocks_v2:mockCustomerProfiles.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockCustomerProfiles = {
  addProfileKey: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addProfileKey', 'CustomerProfiles', result, true, true, mock)
  },
  addProfileKeyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addProfileKey', 'CustomerProfiles', result, false, true, mock)
  },
  addProfileKeyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addProfileKey', 'CustomerProfiles', result, true, false, mock)
  },
  createDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'CustomerProfiles', result, true, true, mock)
  },
  createDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'CustomerProfiles', result, false, true, mock)
  },
  createDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'CustomerProfiles', result, true, false, mock)
  },
  createProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProfile', 'CustomerProfiles', result, true, true, mock)
  },
  createProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProfile', 'CustomerProfiles', result, false, true, mock)
  },
  createProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createProfile', 'CustomerProfiles', result, true, false, mock)
  },
  deleteDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'CustomerProfiles', result, true, true, mock)
  },
  deleteDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'CustomerProfiles', result, false, true, mock)
  },
  deleteDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'CustomerProfiles', result, true, false, mock)
  },
  deleteIntegration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIntegration', 'CustomerProfiles', result, true, true, mock)
  },
  deleteIntegrationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIntegration', 'CustomerProfiles', result, false, true, mock)
  },
  deleteIntegrationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIntegration', 'CustomerProfiles', result, true, false, mock)
  },
  deleteProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfile', 'CustomerProfiles', result, true, true, mock)
  },
  deleteProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfile', 'CustomerProfiles', result, false, true, mock)
  },
  deleteProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfile', 'CustomerProfiles', result, true, false, mock)
  },
  deleteProfileKey: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileKey', 'CustomerProfiles', result, true, true, mock)
  },
  deleteProfileKeyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileKey', 'CustomerProfiles', result, false, true, mock)
  },
  deleteProfileKeyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileKey', 'CustomerProfiles', result, true, false, mock)
  },
  deleteProfileObject: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileObject', 'CustomerProfiles', result, true, true, mock)
  },
  deleteProfileObjectAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileObject', 'CustomerProfiles', result, false, true, mock)
  },
  deleteProfileObjectThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileObject', 'CustomerProfiles', result, true, false, mock)
  },
  deleteProfileObjectType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileObjectType', 'CustomerProfiles', result, true, true, mock)
  },
  deleteProfileObjectTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileObjectType', 'CustomerProfiles', result, false, true, mock)
  },
  deleteProfileObjectTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProfileObjectType', 'CustomerProfiles', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CustomerProfiles', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CustomerProfiles', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CustomerProfiles', result, true, false, mock)
  },
  getDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDomain', 'CustomerProfiles', result, true, true, mock)
  },
  getDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDomain', 'CustomerProfiles', result, false, true, mock)
  },
  getDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDomain', 'CustomerProfiles', result, true, false, mock)
  },
  getIntegration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIntegration', 'CustomerProfiles', result, true, true, mock)
  },
  getIntegrationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIntegration', 'CustomerProfiles', result, false, true, mock)
  },
  getIntegrationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIntegration', 'CustomerProfiles', result, true, false, mock)
  },
  getMatches: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMatches', 'CustomerProfiles', result, true, true, mock)
  },
  getMatchesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMatches', 'CustomerProfiles', result, false, true, mock)
  },
  getMatchesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getMatches', 'CustomerProfiles', result, true, false, mock)
  },
  getProfileObjectType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProfileObjectType', 'CustomerProfiles', result, true, true, mock)
  },
  getProfileObjectTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProfileObjectType', 'CustomerProfiles', result, false, true, mock)
  },
  getProfileObjectTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProfileObjectType', 'CustomerProfiles', result, true, false, mock)
  },
  getProfileObjectTypeTemplate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProfileObjectTypeTemplate', 'CustomerProfiles', result, true, true, mock)
  },
  getProfileObjectTypeTemplateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProfileObjectTypeTemplate', 'CustomerProfiles', result, false, true, mock)
  },
  getProfileObjectTypeTemplateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProfileObjectTypeTemplate', 'CustomerProfiles', result, true, false, mock)
  },
  listAccountIntegrations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAccountIntegrations', 'CustomerProfiles', result, true, true, mock)
  },
  listAccountIntegrationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAccountIntegrations', 'CustomerProfiles', result, false, true, mock)
  },
  listAccountIntegrationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAccountIntegrations', 'CustomerProfiles', result, true, false, mock)
  },
  listDomains: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomains', 'CustomerProfiles', result, true, true, mock)
  },
  listDomainsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomains', 'CustomerProfiles', result, false, true, mock)
  },
  listDomainsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomains', 'CustomerProfiles', result, true, false, mock)
  },
  listIntegrations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIntegrations', 'CustomerProfiles', result, true, true, mock)
  },
  listIntegrationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIntegrations', 'CustomerProfiles', result, false, true, mock)
  },
  listIntegrationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIntegrations', 'CustomerProfiles', result, true, false, mock)
  },
  listProfileObjectTypeTemplates: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjectTypeTemplates', 'CustomerProfiles', result, true, true, mock)
  },
  listProfileObjectTypeTemplatesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjectTypeTemplates', 'CustomerProfiles', result, false, true, mock)
  },
  listProfileObjectTypeTemplatesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjectTypeTemplates', 'CustomerProfiles', result, true, false, mock)
  },
  listProfileObjectTypes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjectTypes', 'CustomerProfiles', result, true, true, mock)
  },
  listProfileObjectTypesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjectTypes', 'CustomerProfiles', result, false, true, mock)
  },
  listProfileObjectTypesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjectTypes', 'CustomerProfiles', result, true, false, mock)
  },
  listProfileObjects: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjects', 'CustomerProfiles', result, true, true, mock)
  },
  listProfileObjectsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjects', 'CustomerProfiles', result, false, true, mock)
  },
  listProfileObjectsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProfileObjects', 'CustomerProfiles', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'CustomerProfiles', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'CustomerProfiles', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'CustomerProfiles', result, true, false, mock)
  },
  mergeProfiles: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('mergeProfiles', 'CustomerProfiles', result, true, true, mock)
  },
  mergeProfilesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('mergeProfiles', 'CustomerProfiles', result, false, true, mock)
  },
  mergeProfilesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('mergeProfiles', 'CustomerProfiles', result, true, false, mock)
  },
  putIntegration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putIntegration', 'CustomerProfiles', result, true, true, mock)
  },
  putIntegrationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putIntegration', 'CustomerProfiles', result, false, true, mock)
  },
  putIntegrationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putIntegration', 'CustomerProfiles', result, true, false, mock)
  },
  putProfileObject: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProfileObject', 'CustomerProfiles', result, true, true, mock)
  },
  putProfileObjectAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProfileObject', 'CustomerProfiles', result, false, true, mock)
  },
  putProfileObjectThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProfileObject', 'CustomerProfiles', result, true, false, mock)
  },
  putProfileObjectType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProfileObjectType', 'CustomerProfiles', result, true, true, mock)
  },
  putProfileObjectTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProfileObjectType', 'CustomerProfiles', result, false, true, mock)
  },
  putProfileObjectTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProfileObjectType', 'CustomerProfiles', result, true, false, mock)
  },
  searchProfiles: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchProfiles', 'CustomerProfiles', result, true, true, mock)
  },
  searchProfilesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchProfiles', 'CustomerProfiles', result, false, true, mock)
  },
  searchProfilesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchProfiles', 'CustomerProfiles', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'CustomerProfiles', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'CustomerProfiles', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'CustomerProfiles', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'CustomerProfiles', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'CustomerProfiles', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'CustomerProfiles', result, true, false, mock)
  },
  updateDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomain', 'CustomerProfiles', result, true, true, mock)
  },
  updateDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomain', 'CustomerProfiles', result, false, true, mock)
  },
  updateDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomain', 'CustomerProfiles', result, true, false, mock)
  },
  updateProfile: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateProfile', 'CustomerProfiles', result, true, true, mock)
  },
  updateProfileAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateProfile', 'CustomerProfiles', result, false, true, mock)
  },
  updateProfileThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateProfile', 'CustomerProfiles', result, true, false, mock)
  },
}
