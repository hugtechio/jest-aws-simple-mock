
/**
* @description mocks_v2:mockKafka.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockKafka = {
  batchAssociateScramSecret: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchAssociateScramSecret', 'Kafka', result, true, true, mock)
  },
  batchAssociateScramSecretAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchAssociateScramSecret', 'Kafka', result, false, true, mock)
  },
  batchAssociateScramSecretThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchAssociateScramSecret', 'Kafka', result, true, false, mock)
  },
  batchDisassociateScramSecret: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDisassociateScramSecret', 'Kafka', result, true, true, mock)
  },
  batchDisassociateScramSecretAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDisassociateScramSecret', 'Kafka', result, false, true, mock)
  },
  batchDisassociateScramSecretThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDisassociateScramSecret', 'Kafka', result, true, false, mock)
  },
  createCluster: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCluster', 'Kafka', result, true, true, mock)
  },
  createClusterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCluster', 'Kafka', result, false, true, mock)
  },
  createClusterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCluster', 'Kafka', result, true, false, mock)
  },
  createConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConfiguration', 'Kafka', result, true, true, mock)
  },
  createConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConfiguration', 'Kafka', result, false, true, mock)
  },
  createConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConfiguration', 'Kafka', result, true, false, mock)
  },
  deleteCluster: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCluster', 'Kafka', result, true, true, mock)
  },
  deleteClusterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCluster', 'Kafka', result, false, true, mock)
  },
  deleteClusterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteCluster', 'Kafka', result, true, false, mock)
  },
  deleteConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConfiguration', 'Kafka', result, true, true, mock)
  },
  deleteConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConfiguration', 'Kafka', result, false, true, mock)
  },
  deleteConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConfiguration', 'Kafka', result, true, false, mock)
  },
  describeCluster: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCluster', 'Kafka', result, true, true, mock)
  },
  describeClusterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCluster', 'Kafka', result, false, true, mock)
  },
  describeClusterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCluster', 'Kafka', result, true, false, mock)
  },
  describeClusterOperation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeClusterOperation', 'Kafka', result, true, true, mock)
  },
  describeClusterOperationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeClusterOperation', 'Kafka', result, false, true, mock)
  },
  describeClusterOperationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeClusterOperation', 'Kafka', result, true, false, mock)
  },
  describeConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfiguration', 'Kafka', result, true, true, mock)
  },
  describeConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfiguration', 'Kafka', result, false, true, mock)
  },
  describeConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfiguration', 'Kafka', result, true, false, mock)
  },
  describeConfigurationRevision: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfigurationRevision', 'Kafka', result, true, true, mock)
  },
  describeConfigurationRevisionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfigurationRevision', 'Kafka', result, false, true, mock)
  },
  describeConfigurationRevisionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConfigurationRevision', 'Kafka', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Kafka', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Kafka', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Kafka', result, true, false, mock)
  },
  getBootstrapBrokers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBootstrapBrokers', 'Kafka', result, true, true, mock)
  },
  getBootstrapBrokersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBootstrapBrokers', 'Kafka', result, false, true, mock)
  },
  getBootstrapBrokersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getBootstrapBrokers', 'Kafka', result, true, false, mock)
  },
  getCompatibleKafkaVersions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCompatibleKafkaVersions', 'Kafka', result, true, true, mock)
  },
  getCompatibleKafkaVersionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCompatibleKafkaVersions', 'Kafka', result, false, true, mock)
  },
  getCompatibleKafkaVersionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCompatibleKafkaVersions', 'Kafka', result, true, false, mock)
  },
  listClusterOperations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listClusterOperations', 'Kafka', result, true, true, mock)
  },
  listClusterOperationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listClusterOperations', 'Kafka', result, false, true, mock)
  },
  listClusterOperationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listClusterOperations', 'Kafka', result, true, false, mock)
  },
  listClusters: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listClusters', 'Kafka', result, true, true, mock)
  },
  listClustersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listClusters', 'Kafka', result, false, true, mock)
  },
  listClustersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listClusters', 'Kafka', result, true, false, mock)
  },
  listConfigurationRevisions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurationRevisions', 'Kafka', result, true, true, mock)
  },
  listConfigurationRevisionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurationRevisions', 'Kafka', result, false, true, mock)
  },
  listConfigurationRevisionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurationRevisions', 'Kafka', result, true, false, mock)
  },
  listConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurations', 'Kafka', result, true, true, mock)
  },
  listConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurations', 'Kafka', result, false, true, mock)
  },
  listConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConfigurations', 'Kafka', result, true, false, mock)
  },
  listKafkaVersions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listKafkaVersions', 'Kafka', result, true, true, mock)
  },
  listKafkaVersionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listKafkaVersions', 'Kafka', result, false, true, mock)
  },
  listKafkaVersionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listKafkaVersions', 'Kafka', result, true, false, mock)
  },
  listNodes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNodes', 'Kafka', result, true, true, mock)
  },
  listNodesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNodes', 'Kafka', result, false, true, mock)
  },
  listNodesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listNodes', 'Kafka', result, true, false, mock)
  },
  listScramSecrets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listScramSecrets', 'Kafka', result, true, true, mock)
  },
  listScramSecretsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listScramSecrets', 'Kafka', result, false, true, mock)
  },
  listScramSecretsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listScramSecrets', 'Kafka', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Kafka', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Kafka', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Kafka', result, true, false, mock)
  },
  rebootBroker: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rebootBroker', 'Kafka', result, true, true, mock)
  },
  rebootBrokerAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rebootBroker', 'Kafka', result, false, true, mock)
  },
  rebootBrokerThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('rebootBroker', 'Kafka', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Kafka', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Kafka', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Kafka', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Kafka', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Kafka', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Kafka', result, true, false, mock)
  },
  updateBrokerCount: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerCount', 'Kafka', result, true, true, mock)
  },
  updateBrokerCountAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerCount', 'Kafka', result, false, true, mock)
  },
  updateBrokerCountThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerCount', 'Kafka', result, true, false, mock)
  },
  updateBrokerStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerStorage', 'Kafka', result, true, true, mock)
  },
  updateBrokerStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerStorage', 'Kafka', result, false, true, mock)
  },
  updateBrokerStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerStorage', 'Kafka', result, true, false, mock)
  },
  updateBrokerType: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerType', 'Kafka', result, true, true, mock)
  },
  updateBrokerTypeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerType', 'Kafka', result, false, true, mock)
  },
  updateBrokerTypeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateBrokerType', 'Kafka', result, true, false, mock)
  },
  updateClusterConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateClusterConfiguration', 'Kafka', result, true, true, mock)
  },
  updateClusterConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateClusterConfiguration', 'Kafka', result, false, true, mock)
  },
  updateClusterConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateClusterConfiguration', 'Kafka', result, true, false, mock)
  },
  updateClusterKafkaVersion: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateClusterKafkaVersion', 'Kafka', result, true, true, mock)
  },
  updateClusterKafkaVersionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateClusterKafkaVersion', 'Kafka', result, false, true, mock)
  },
  updateClusterKafkaVersionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateClusterKafkaVersion', 'Kafka', result, true, false, mock)
  },
  updateConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConfiguration', 'Kafka', result, true, true, mock)
  },
  updateConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConfiguration', 'Kafka', result, false, true, mock)
  },
  updateConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConfiguration', 'Kafka', result, true, false, mock)
  },
  updateMonitoring: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMonitoring', 'Kafka', result, true, true, mock)
  },
  updateMonitoringAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMonitoring', 'Kafka', result, false, true, mock)
  },
  updateMonitoringThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMonitoring', 'Kafka', result, true, false, mock)
  },
  updateSecurity: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSecurity', 'Kafka', result, true, true, mock)
  },
  updateSecurityAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSecurity', 'Kafka', result, false, true, mock)
  },
  updateSecurityThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSecurity', 'Kafka', result, true, false, mock)
  },
}
