
/**
* @description mocks_v2:mockLookoutMetrics.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockLookoutMetrics = {
  activateAnomalyDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activateAnomalyDetector', 'LookoutMetrics', result, true, true, mock)
  },
  activateAnomalyDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activateAnomalyDetector', 'LookoutMetrics', result, false, true, mock)
  },
  activateAnomalyDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('activateAnomalyDetector', 'LookoutMetrics', result, true, false, mock)
  },
  backTestAnomalyDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('backTestAnomalyDetector', 'LookoutMetrics', result, true, true, mock)
  },
  backTestAnomalyDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('backTestAnomalyDetector', 'LookoutMetrics', result, false, true, mock)
  },
  backTestAnomalyDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('backTestAnomalyDetector', 'LookoutMetrics', result, true, false, mock)
  },
  createAlert: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAlert', 'LookoutMetrics', result, true, true, mock)
  },
  createAlertAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAlert', 'LookoutMetrics', result, false, true, mock)
  },
  createAlertThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAlert', 'LookoutMetrics', result, true, false, mock)
  },
  createAnomalyDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAnomalyDetector', 'LookoutMetrics', result, true, true, mock)
  },
  createAnomalyDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAnomalyDetector', 'LookoutMetrics', result, false, true, mock)
  },
  createAnomalyDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAnomalyDetector', 'LookoutMetrics', result, true, false, mock)
  },
  createMetricSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMetricSet', 'LookoutMetrics', result, true, true, mock)
  },
  createMetricSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMetricSet', 'LookoutMetrics', result, false, true, mock)
  },
  createMetricSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createMetricSet', 'LookoutMetrics', result, true, false, mock)
  },
  deleteAlert: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAlert', 'LookoutMetrics', result, true, true, mock)
  },
  deleteAlertAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAlert', 'LookoutMetrics', result, false, true, mock)
  },
  deleteAlertThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAlert', 'LookoutMetrics', result, true, false, mock)
  },
  deleteAnomalyDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAnomalyDetector', 'LookoutMetrics', result, true, true, mock)
  },
  deleteAnomalyDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAnomalyDetector', 'LookoutMetrics', result, false, true, mock)
  },
  deleteAnomalyDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAnomalyDetector', 'LookoutMetrics', result, true, false, mock)
  },
  describeAlert: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAlert', 'LookoutMetrics', result, true, true, mock)
  },
  describeAlertAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAlert', 'LookoutMetrics', result, false, true, mock)
  },
  describeAlertThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAlert', 'LookoutMetrics', result, true, false, mock)
  },
  describeAnomalyDetectionExecutions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnomalyDetectionExecutions', 'LookoutMetrics', result, true, true, mock)
  },
  describeAnomalyDetectionExecutionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnomalyDetectionExecutions', 'LookoutMetrics', result, false, true, mock)
  },
  describeAnomalyDetectionExecutionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnomalyDetectionExecutions', 'LookoutMetrics', result, true, false, mock)
  },
  describeAnomalyDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnomalyDetector', 'LookoutMetrics', result, true, true, mock)
  },
  describeAnomalyDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnomalyDetector', 'LookoutMetrics', result, false, true, mock)
  },
  describeAnomalyDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnomalyDetector', 'LookoutMetrics', result, true, false, mock)
  },
  describeMetricSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMetricSet', 'LookoutMetrics', result, true, true, mock)
  },
  describeMetricSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMetricSet', 'LookoutMetrics', result, false, true, mock)
  },
  describeMetricSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeMetricSet', 'LookoutMetrics', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LookoutMetrics', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LookoutMetrics', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LookoutMetrics', result, true, false, mock)
  },
  getAnomalyGroup: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAnomalyGroup', 'LookoutMetrics', result, true, true, mock)
  },
  getAnomalyGroupAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAnomalyGroup', 'LookoutMetrics', result, false, true, mock)
  },
  getAnomalyGroupThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAnomalyGroup', 'LookoutMetrics', result, true, false, mock)
  },
  getFeedback: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getFeedback', 'LookoutMetrics', result, true, true, mock)
  },
  getFeedbackAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getFeedback', 'LookoutMetrics', result, false, true, mock)
  },
  getFeedbackThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getFeedback', 'LookoutMetrics', result, true, false, mock)
  },
  getSampleData: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSampleData', 'LookoutMetrics', result, true, true, mock)
  },
  getSampleDataAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSampleData', 'LookoutMetrics', result, false, true, mock)
  },
  getSampleDataThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSampleData', 'LookoutMetrics', result, true, false, mock)
  },
  listAlerts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlerts', 'LookoutMetrics', result, true, true, mock)
  },
  listAlertsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlerts', 'LookoutMetrics', result, false, true, mock)
  },
  listAlertsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlerts', 'LookoutMetrics', result, true, false, mock)
  },
  listAnomalyDetectors: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyDetectors', 'LookoutMetrics', result, true, true, mock)
  },
  listAnomalyDetectorsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyDetectors', 'LookoutMetrics', result, false, true, mock)
  },
  listAnomalyDetectorsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyDetectors', 'LookoutMetrics', result, true, false, mock)
  },
  listAnomalyGroupSummaries: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyGroupSummaries', 'LookoutMetrics', result, true, true, mock)
  },
  listAnomalyGroupSummariesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyGroupSummaries', 'LookoutMetrics', result, false, true, mock)
  },
  listAnomalyGroupSummariesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyGroupSummaries', 'LookoutMetrics', result, true, false, mock)
  },
  listAnomalyGroupTimeSeries: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyGroupTimeSeries', 'LookoutMetrics', result, true, true, mock)
  },
  listAnomalyGroupTimeSeriesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyGroupTimeSeries', 'LookoutMetrics', result, false, true, mock)
  },
  listAnomalyGroupTimeSeriesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAnomalyGroupTimeSeries', 'LookoutMetrics', result, true, false, mock)
  },
  listMetricSets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMetricSets', 'LookoutMetrics', result, true, true, mock)
  },
  listMetricSetsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMetricSets', 'LookoutMetrics', result, false, true, mock)
  },
  listMetricSetsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMetricSets', 'LookoutMetrics', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'LookoutMetrics', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'LookoutMetrics', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'LookoutMetrics', result, true, false, mock)
  },
  putFeedback: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putFeedback', 'LookoutMetrics', result, true, true, mock)
  },
  putFeedbackAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putFeedback', 'LookoutMetrics', result, false, true, mock)
  },
  putFeedbackThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putFeedback', 'LookoutMetrics', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'LookoutMetrics', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'LookoutMetrics', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'LookoutMetrics', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'LookoutMetrics', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'LookoutMetrics', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'LookoutMetrics', result, true, false, mock)
  },
  updateAnomalyDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAnomalyDetector', 'LookoutMetrics', result, true, true, mock)
  },
  updateAnomalyDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAnomalyDetector', 'LookoutMetrics', result, false, true, mock)
  },
  updateAnomalyDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAnomalyDetector', 'LookoutMetrics', result, true, false, mock)
  },
  updateMetricSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMetricSet', 'LookoutMetrics', result, true, true, mock)
  },
  updateMetricSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMetricSet', 'LookoutMetrics', result, false, true, mock)
  },
  updateMetricSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateMetricSet', 'LookoutMetrics', result, true, false, mock)
  },
}
