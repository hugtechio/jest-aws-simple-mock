
/**
* @description mocks_v2:mockPolly.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockPolly = {
  deleteLexicon: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLexicon', 'Polly', result, true, true, mock)
  },
  deleteLexiconAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLexicon', 'Polly', result, false, true, mock)
  },
  deleteLexiconThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLexicon', 'Polly', result, true, false, mock)
  },
  describeVoices: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVoices', 'Polly', result, true, true, mock)
  },
  describeVoicesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVoices', 'Polly', result, false, true, mock)
  },
  describeVoicesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeVoices', 'Polly', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Polly', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Polly', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Polly', result, true, false, mock)
  },
  getLexicon: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLexicon', 'Polly', result, true, true, mock)
  },
  getLexiconAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLexicon', 'Polly', result, false, true, mock)
  },
  getLexiconThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLexicon', 'Polly', result, true, false, mock)
  },
  getSpeechSynthesisTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSpeechSynthesisTask', 'Polly', result, true, true, mock)
  },
  getSpeechSynthesisTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSpeechSynthesisTask', 'Polly', result, false, true, mock)
  },
  getSpeechSynthesisTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getSpeechSynthesisTask', 'Polly', result, true, false, mock)
  },
  listLexicons: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLexicons', 'Polly', result, true, true, mock)
  },
  listLexiconsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLexicons', 'Polly', result, false, true, mock)
  },
  listLexiconsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLexicons', 'Polly', result, true, false, mock)
  },
  listSpeechSynthesisTasks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSpeechSynthesisTasks', 'Polly', result, true, true, mock)
  },
  listSpeechSynthesisTasksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSpeechSynthesisTasks', 'Polly', result, false, true, mock)
  },
  listSpeechSynthesisTasksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSpeechSynthesisTasks', 'Polly', result, true, false, mock)
  },
  putLexicon: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putLexicon', 'Polly', result, true, true, mock)
  },
  putLexiconAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putLexicon', 'Polly', result, false, true, mock)
  },
  putLexiconThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putLexicon', 'Polly', result, true, false, mock)
  },
  startSpeechSynthesisTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSpeechSynthesisTask', 'Polly', result, true, true, mock)
  },
  startSpeechSynthesisTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSpeechSynthesisTask', 'Polly', result, false, true, mock)
  },
  startSpeechSynthesisTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startSpeechSynthesisTask', 'Polly', result, true, false, mock)
  },
  synthesizeSpeech: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('synthesizeSpeech', 'Polly', result, true, true, mock)
  },
  synthesizeSpeechAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('synthesizeSpeech', 'Polly', result, false, true, mock)
  },
  synthesizeSpeechThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('synthesizeSpeech', 'Polly', result, true, false, mock)
  },
}
