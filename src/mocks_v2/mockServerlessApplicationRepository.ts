
/**
* @description mocks_v2:mockServerlessApplicationRepository.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockServerlessApplicationRepository = {
  createApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplication', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  createApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplication', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  createApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplication', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  createApplicationVersion: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplicationVersion', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  createApplicationVersionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplicationVersion', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  createApplicationVersionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createApplicationVersion', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  createCloudFormationChangeSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCloudFormationChangeSet', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  createCloudFormationChangeSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCloudFormationChangeSet', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  createCloudFormationChangeSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCloudFormationChangeSet', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  createCloudFormationTemplate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCloudFormationTemplate', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  createCloudFormationTemplateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCloudFormationTemplate', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  createCloudFormationTemplateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCloudFormationTemplate', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  deleteApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApplication', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  deleteApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApplication', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  deleteApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteApplication', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  getApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApplication', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  getApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApplication', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  getApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApplication', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  getApplicationPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApplicationPolicy', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  getApplicationPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApplicationPolicy', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  getApplicationPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getApplicationPolicy', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  getCloudFormationTemplate: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCloudFormationTemplate', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  getCloudFormationTemplateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCloudFormationTemplate', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  getCloudFormationTemplateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCloudFormationTemplate', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  listApplicationDependencies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplicationDependencies', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  listApplicationDependenciesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplicationDependencies', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  listApplicationDependenciesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplicationDependencies', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  listApplicationVersions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplicationVersions', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  listApplicationVersionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplicationVersions', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  listApplicationVersionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplicationVersions', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  listApplications: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplications', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  listApplicationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplications', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  listApplicationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listApplications', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  putApplicationPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putApplicationPolicy', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  putApplicationPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putApplicationPolicy', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  putApplicationPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putApplicationPolicy', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  unshareApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unshareApplication', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  unshareApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unshareApplication', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  unshareApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unshareApplication', 'ServerlessApplicationRepository', result, true, false, mock)
  },
  updateApplication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApplication', 'ServerlessApplicationRepository', result, true, true, mock)
  },
  updateApplicationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApplication', 'ServerlessApplicationRepository', result, false, true, mock)
  },
  updateApplicationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateApplication', 'ServerlessApplicationRepository', result, true, false, mock)
  },
}
