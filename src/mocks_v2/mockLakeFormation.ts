
/**
* @description mocks_v2:mockLakeFormation.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockLakeFormation = {
  addLFTagsToResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addLFTagsToResource', 'LakeFormation', result, true, true, mock)
  },
  addLFTagsToResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addLFTagsToResource', 'LakeFormation', result, false, true, mock)
  },
  addLFTagsToResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('addLFTagsToResource', 'LakeFormation', result, true, false, mock)
  },
  batchGrantPermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchGrantPermissions', 'LakeFormation', result, true, true, mock)
  },
  batchGrantPermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchGrantPermissions', 'LakeFormation', result, false, true, mock)
  },
  batchGrantPermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchGrantPermissions', 'LakeFormation', result, true, false, mock)
  },
  batchRevokePermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchRevokePermissions', 'LakeFormation', result, true, true, mock)
  },
  batchRevokePermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchRevokePermissions', 'LakeFormation', result, false, true, mock)
  },
  batchRevokePermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchRevokePermissions', 'LakeFormation', result, true, false, mock)
  },
  createLFTag: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLFTag', 'LakeFormation', result, true, true, mock)
  },
  createLFTagAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLFTag', 'LakeFormation', result, false, true, mock)
  },
  createLFTagThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLFTag', 'LakeFormation', result, true, false, mock)
  },
  deleteLFTag: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLFTag', 'LakeFormation', result, true, true, mock)
  },
  deleteLFTagAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLFTag', 'LakeFormation', result, false, true, mock)
  },
  deleteLFTagThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLFTag', 'LakeFormation', result, true, false, mock)
  },
  deregisterResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterResource', 'LakeFormation', result, true, true, mock)
  },
  deregisterResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterResource', 'LakeFormation', result, false, true, mock)
  },
  deregisterResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deregisterResource', 'LakeFormation', result, true, false, mock)
  },
  describeResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeResource', 'LakeFormation', result, true, true, mock)
  },
  describeResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeResource', 'LakeFormation', result, false, true, mock)
  },
  describeResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeResource', 'LakeFormation', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LakeFormation', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LakeFormation', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'LakeFormation', result, true, false, mock)
  },
  getDataLakeSettings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataLakeSettings', 'LakeFormation', result, true, true, mock)
  },
  getDataLakeSettingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataLakeSettings', 'LakeFormation', result, false, true, mock)
  },
  getDataLakeSettingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDataLakeSettings', 'LakeFormation', result, true, false, mock)
  },
  getEffectivePermissionsForPath: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getEffectivePermissionsForPath', 'LakeFormation', result, true, true, mock)
  },
  getEffectivePermissionsForPathAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getEffectivePermissionsForPath', 'LakeFormation', result, false, true, mock)
  },
  getEffectivePermissionsForPathThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getEffectivePermissionsForPath', 'LakeFormation', result, true, false, mock)
  },
  getLFTag: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLFTag', 'LakeFormation', result, true, true, mock)
  },
  getLFTagAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLFTag', 'LakeFormation', result, false, true, mock)
  },
  getLFTagThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getLFTag', 'LakeFormation', result, true, false, mock)
  },
  getResourceLFTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourceLFTags', 'LakeFormation', result, true, true, mock)
  },
  getResourceLFTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourceLFTags', 'LakeFormation', result, false, true, mock)
  },
  getResourceLFTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourceLFTags', 'LakeFormation', result, true, false, mock)
  },
  grantPermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('grantPermissions', 'LakeFormation', result, true, true, mock)
  },
  grantPermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('grantPermissions', 'LakeFormation', result, false, true, mock)
  },
  grantPermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('grantPermissions', 'LakeFormation', result, true, false, mock)
  },
  listLFTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLFTags', 'LakeFormation', result, true, true, mock)
  },
  listLFTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLFTags', 'LakeFormation', result, false, true, mock)
  },
  listLFTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLFTags', 'LakeFormation', result, true, false, mock)
  },
  listPermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPermissions', 'LakeFormation', result, true, true, mock)
  },
  listPermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPermissions', 'LakeFormation', result, false, true, mock)
  },
  listPermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPermissions', 'LakeFormation', result, true, false, mock)
  },
  listResources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResources', 'LakeFormation', result, true, true, mock)
  },
  listResourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResources', 'LakeFormation', result, false, true, mock)
  },
  listResourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResources', 'LakeFormation', result, true, false, mock)
  },
  putDataLakeSettings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putDataLakeSettings', 'LakeFormation', result, true, true, mock)
  },
  putDataLakeSettingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putDataLakeSettings', 'LakeFormation', result, false, true, mock)
  },
  putDataLakeSettingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putDataLakeSettings', 'LakeFormation', result, true, false, mock)
  },
  registerResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerResource', 'LakeFormation', result, true, true, mock)
  },
  registerResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerResource', 'LakeFormation', result, false, true, mock)
  },
  registerResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('registerResource', 'LakeFormation', result, true, false, mock)
  },
  removeLFTagsFromResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeLFTagsFromResource', 'LakeFormation', result, true, true, mock)
  },
  removeLFTagsFromResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeLFTagsFromResource', 'LakeFormation', result, false, true, mock)
  },
  removeLFTagsFromResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('removeLFTagsFromResource', 'LakeFormation', result, true, false, mock)
  },
  revokePermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokePermissions', 'LakeFormation', result, true, true, mock)
  },
  revokePermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokePermissions', 'LakeFormation', result, false, true, mock)
  },
  revokePermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('revokePermissions', 'LakeFormation', result, true, false, mock)
  },
  searchDatabasesByLFTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchDatabasesByLFTags', 'LakeFormation', result, true, true, mock)
  },
  searchDatabasesByLFTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchDatabasesByLFTags', 'LakeFormation', result, false, true, mock)
  },
  searchDatabasesByLFTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchDatabasesByLFTags', 'LakeFormation', result, true, false, mock)
  },
  searchTablesByLFTags: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchTablesByLFTags', 'LakeFormation', result, true, true, mock)
  },
  searchTablesByLFTagsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchTablesByLFTags', 'LakeFormation', result, false, true, mock)
  },
  searchTablesByLFTagsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchTablesByLFTags', 'LakeFormation', result, true, false, mock)
  },
  updateLFTag: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLFTag', 'LakeFormation', result, true, true, mock)
  },
  updateLFTagAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLFTag', 'LakeFormation', result, false, true, mock)
  },
  updateLFTagThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLFTag', 'LakeFormation', result, true, false, mock)
  },
  updateResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResource', 'LakeFormation', result, true, true, mock)
  },
  updateResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResource', 'LakeFormation', result, false, true, mock)
  },
  updateResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResource', 'LakeFormation', result, true, false, mock)
  },
}
