
/**
* @description mocks_v2:mockSavingsplans.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockSavingsplans = {
  createSavingsPlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSavingsPlan', 'Savingsplans', result, true, true, mock)
  },
  createSavingsPlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSavingsPlan', 'Savingsplans', result, false, true, mock)
  },
  createSavingsPlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSavingsPlan', 'Savingsplans', result, true, false, mock)
  },
  deleteQueuedSavingsPlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteQueuedSavingsPlan', 'Savingsplans', result, true, true, mock)
  },
  deleteQueuedSavingsPlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteQueuedSavingsPlan', 'Savingsplans', result, false, true, mock)
  },
  deleteQueuedSavingsPlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteQueuedSavingsPlan', 'Savingsplans', result, true, false, mock)
  },
  describeSavingsPlanRates: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlanRates', 'Savingsplans', result, true, true, mock)
  },
  describeSavingsPlanRatesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlanRates', 'Savingsplans', result, false, true, mock)
  },
  describeSavingsPlanRatesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlanRates', 'Savingsplans', result, true, false, mock)
  },
  describeSavingsPlans: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlans', 'Savingsplans', result, true, true, mock)
  },
  describeSavingsPlansAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlans', 'Savingsplans', result, false, true, mock)
  },
  describeSavingsPlansThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlans', 'Savingsplans', result, true, false, mock)
  },
  describeSavingsPlansOfferingRates: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlansOfferingRates', 'Savingsplans', result, true, true, mock)
  },
  describeSavingsPlansOfferingRatesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlansOfferingRates', 'Savingsplans', result, false, true, mock)
  },
  describeSavingsPlansOfferingRatesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlansOfferingRates', 'Savingsplans', result, true, false, mock)
  },
  describeSavingsPlansOfferings: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlansOfferings', 'Savingsplans', result, true, true, mock)
  },
  describeSavingsPlansOfferingsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlansOfferings', 'Savingsplans', result, false, true, mock)
  },
  describeSavingsPlansOfferingsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSavingsPlansOfferings', 'Savingsplans', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Savingsplans', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Savingsplans', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Savingsplans', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Savingsplans', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Savingsplans', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Savingsplans', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Savingsplans', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Savingsplans', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Savingsplans', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Savingsplans', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Savingsplans', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Savingsplans', result, true, false, mock)
  },
}
