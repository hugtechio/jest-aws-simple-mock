
/**
* @description mocks_v2:mockHoneycode.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockHoneycode = {
  batchCreateTableRows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchCreateTableRows', 'Honeycode', result, true, true, mock)
  },
  batchCreateTableRowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchCreateTableRows', 'Honeycode', result, false, true, mock)
  },
  batchCreateTableRowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchCreateTableRows', 'Honeycode', result, true, false, mock)
  },
  batchDeleteTableRows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteTableRows', 'Honeycode', result, true, true, mock)
  },
  batchDeleteTableRowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteTableRows', 'Honeycode', result, false, true, mock)
  },
  batchDeleteTableRowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteTableRows', 'Honeycode', result, true, false, mock)
  },
  batchUpdateTableRows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateTableRows', 'Honeycode', result, true, true, mock)
  },
  batchUpdateTableRowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateTableRows', 'Honeycode', result, false, true, mock)
  },
  batchUpdateTableRowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateTableRows', 'Honeycode', result, true, false, mock)
  },
  batchUpsertTableRows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpsertTableRows', 'Honeycode', result, true, true, mock)
  },
  batchUpsertTableRowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpsertTableRows', 'Honeycode', result, false, true, mock)
  },
  batchUpsertTableRowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpsertTableRows', 'Honeycode', result, true, false, mock)
  },
  describeTableDataImportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTableDataImportJob', 'Honeycode', result, true, true, mock)
  },
  describeTableDataImportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTableDataImportJob', 'Honeycode', result, false, true, mock)
  },
  describeTableDataImportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTableDataImportJob', 'Honeycode', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Honeycode', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Honeycode', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Honeycode', result, true, false, mock)
  },
  getScreenData: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getScreenData', 'Honeycode', result, true, true, mock)
  },
  getScreenDataAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getScreenData', 'Honeycode', result, false, true, mock)
  },
  getScreenDataThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getScreenData', 'Honeycode', result, true, false, mock)
  },
  invokeScreenAutomation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('invokeScreenAutomation', 'Honeycode', result, true, true, mock)
  },
  invokeScreenAutomationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('invokeScreenAutomation', 'Honeycode', result, false, true, mock)
  },
  invokeScreenAutomationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('invokeScreenAutomation', 'Honeycode', result, true, false, mock)
  },
  listTableColumns: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTableColumns', 'Honeycode', result, true, true, mock)
  },
  listTableColumnsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTableColumns', 'Honeycode', result, false, true, mock)
  },
  listTableColumnsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTableColumns', 'Honeycode', result, true, false, mock)
  },
  listTableRows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTableRows', 'Honeycode', result, true, true, mock)
  },
  listTableRowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTableRows', 'Honeycode', result, false, true, mock)
  },
  listTableRowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTableRows', 'Honeycode', result, true, false, mock)
  },
  listTables: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTables', 'Honeycode', result, true, true, mock)
  },
  listTablesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTables', 'Honeycode', result, false, true, mock)
  },
  listTablesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTables', 'Honeycode', result, true, false, mock)
  },
  queryTableRows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('queryTableRows', 'Honeycode', result, true, true, mock)
  },
  queryTableRowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('queryTableRows', 'Honeycode', result, false, true, mock)
  },
  queryTableRowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('queryTableRows', 'Honeycode', result, true, false, mock)
  },
  startTableDataImportJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTableDataImportJob', 'Honeycode', result, true, true, mock)
  },
  startTableDataImportJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTableDataImportJob', 'Honeycode', result, false, true, mock)
  },
  startTableDataImportJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTableDataImportJob', 'Honeycode', result, true, false, mock)
  },
}
