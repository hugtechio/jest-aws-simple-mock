
/**
* @description mocks_v2:mockAppRunner.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockAppRunner = {
  associateCustomDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateCustomDomain', 'AppRunner', result, true, true, mock)
  },
  associateCustomDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateCustomDomain', 'AppRunner', result, false, true, mock)
  },
  associateCustomDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateCustomDomain', 'AppRunner', result, true, false, mock)
  },
  createAutoScalingConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAutoScalingConfiguration', 'AppRunner', result, true, true, mock)
  },
  createAutoScalingConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAutoScalingConfiguration', 'AppRunner', result, false, true, mock)
  },
  createAutoScalingConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAutoScalingConfiguration', 'AppRunner', result, true, false, mock)
  },
  createConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConnection', 'AppRunner', result, true, true, mock)
  },
  createConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConnection', 'AppRunner', result, false, true, mock)
  },
  createConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConnection', 'AppRunner', result, true, false, mock)
  },
  createService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createService', 'AppRunner', result, true, true, mock)
  },
  createServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createService', 'AppRunner', result, false, true, mock)
  },
  createServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createService', 'AppRunner', result, true, false, mock)
  },
  deleteAutoScalingConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAutoScalingConfiguration', 'AppRunner', result, true, true, mock)
  },
  deleteAutoScalingConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAutoScalingConfiguration', 'AppRunner', result, false, true, mock)
  },
  deleteAutoScalingConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAutoScalingConfiguration', 'AppRunner', result, true, false, mock)
  },
  deleteConnection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConnection', 'AppRunner', result, true, true, mock)
  },
  deleteConnectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConnection', 'AppRunner', result, false, true, mock)
  },
  deleteConnectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConnection', 'AppRunner', result, true, false, mock)
  },
  deleteService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteService', 'AppRunner', result, true, true, mock)
  },
  deleteServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteService', 'AppRunner', result, false, true, mock)
  },
  deleteServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteService', 'AppRunner', result, true, false, mock)
  },
  describeAutoScalingConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAutoScalingConfiguration', 'AppRunner', result, true, true, mock)
  },
  describeAutoScalingConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAutoScalingConfiguration', 'AppRunner', result, false, true, mock)
  },
  describeAutoScalingConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAutoScalingConfiguration', 'AppRunner', result, true, false, mock)
  },
  describeCustomDomains: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCustomDomains', 'AppRunner', result, true, true, mock)
  },
  describeCustomDomainsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCustomDomains', 'AppRunner', result, false, true, mock)
  },
  describeCustomDomainsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCustomDomains', 'AppRunner', result, true, false, mock)
  },
  describeService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeService', 'AppRunner', result, true, true, mock)
  },
  describeServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeService', 'AppRunner', result, false, true, mock)
  },
  describeServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeService', 'AppRunner', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AppRunner', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AppRunner', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AppRunner', result, true, false, mock)
  },
  disassociateCustomDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateCustomDomain', 'AppRunner', result, true, true, mock)
  },
  disassociateCustomDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateCustomDomain', 'AppRunner', result, false, true, mock)
  },
  disassociateCustomDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateCustomDomain', 'AppRunner', result, true, false, mock)
  },
  listAutoScalingConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAutoScalingConfigurations', 'AppRunner', result, true, true, mock)
  },
  listAutoScalingConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAutoScalingConfigurations', 'AppRunner', result, false, true, mock)
  },
  listAutoScalingConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAutoScalingConfigurations', 'AppRunner', result, true, false, mock)
  },
  listConnections: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConnections', 'AppRunner', result, true, true, mock)
  },
  listConnectionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConnections', 'AppRunner', result, false, true, mock)
  },
  listConnectionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConnections', 'AppRunner', result, true, false, mock)
  },
  listOperations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOperations', 'AppRunner', result, true, true, mock)
  },
  listOperationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOperations', 'AppRunner', result, false, true, mock)
  },
  listOperationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listOperations', 'AppRunner', result, true, false, mock)
  },
  listServices: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listServices', 'AppRunner', result, true, true, mock)
  },
  listServicesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listServices', 'AppRunner', result, false, true, mock)
  },
  listServicesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listServices', 'AppRunner', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'AppRunner', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'AppRunner', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'AppRunner', result, true, false, mock)
  },
  pauseService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pauseService', 'AppRunner', result, true, true, mock)
  },
  pauseServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pauseService', 'AppRunner', result, false, true, mock)
  },
  pauseServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('pauseService', 'AppRunner', result, true, false, mock)
  },
  resumeService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resumeService', 'AppRunner', result, true, true, mock)
  },
  resumeServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resumeService', 'AppRunner', result, false, true, mock)
  },
  resumeServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('resumeService', 'AppRunner', result, true, false, mock)
  },
  startDeployment: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDeployment', 'AppRunner', result, true, true, mock)
  },
  startDeploymentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDeployment', 'AppRunner', result, false, true, mock)
  },
  startDeploymentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDeployment', 'AppRunner', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'AppRunner', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'AppRunner', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'AppRunner', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'AppRunner', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'AppRunner', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'AppRunner', result, true, false, mock)
  },
  updateService: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateService', 'AppRunner', result, true, true, mock)
  },
  updateServiceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateService', 'AppRunner', result, false, true, mock)
  },
  updateServiceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateService', 'AppRunner', result, true, false, mock)
  },
}
