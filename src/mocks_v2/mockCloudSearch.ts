
/**
* @description mocks_v2:mockCloudSearch.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockCloudSearch = {
  buildSuggesters: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('buildSuggesters', 'CloudSearch', result, true, true, mock)
  },
  buildSuggestersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('buildSuggesters', 'CloudSearch', result, false, true, mock)
  },
  buildSuggestersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('buildSuggesters', 'CloudSearch', result, true, false, mock)
  },
  createDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'CloudSearch', result, true, true, mock)
  },
  createDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'CloudSearch', result, false, true, mock)
  },
  createDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDomain', 'CloudSearch', result, true, false, mock)
  },
  defineAnalysisScheme: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineAnalysisScheme', 'CloudSearch', result, true, true, mock)
  },
  defineAnalysisSchemeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineAnalysisScheme', 'CloudSearch', result, false, true, mock)
  },
  defineAnalysisSchemeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineAnalysisScheme', 'CloudSearch', result, true, false, mock)
  },
  defineExpression: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineExpression', 'CloudSearch', result, true, true, mock)
  },
  defineExpressionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineExpression', 'CloudSearch', result, false, true, mock)
  },
  defineExpressionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineExpression', 'CloudSearch', result, true, false, mock)
  },
  defineIndexField: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineIndexField', 'CloudSearch', result, true, true, mock)
  },
  defineIndexFieldAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineIndexField', 'CloudSearch', result, false, true, mock)
  },
  defineIndexFieldThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineIndexField', 'CloudSearch', result, true, false, mock)
  },
  defineSuggester: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineSuggester', 'CloudSearch', result, true, true, mock)
  },
  defineSuggesterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineSuggester', 'CloudSearch', result, false, true, mock)
  },
  defineSuggesterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('defineSuggester', 'CloudSearch', result, true, false, mock)
  },
  deleteAnalysisScheme: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAnalysisScheme', 'CloudSearch', result, true, true, mock)
  },
  deleteAnalysisSchemeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAnalysisScheme', 'CloudSearch', result, false, true, mock)
  },
  deleteAnalysisSchemeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAnalysisScheme', 'CloudSearch', result, true, false, mock)
  },
  deleteDomain: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'CloudSearch', result, true, true, mock)
  },
  deleteDomainAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'CloudSearch', result, false, true, mock)
  },
  deleteDomainThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDomain', 'CloudSearch', result, true, false, mock)
  },
  deleteExpression: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteExpression', 'CloudSearch', result, true, true, mock)
  },
  deleteExpressionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteExpression', 'CloudSearch', result, false, true, mock)
  },
  deleteExpressionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteExpression', 'CloudSearch', result, true, false, mock)
  },
  deleteIndexField: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIndexField', 'CloudSearch', result, true, true, mock)
  },
  deleteIndexFieldAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIndexField', 'CloudSearch', result, false, true, mock)
  },
  deleteIndexFieldThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIndexField', 'CloudSearch', result, true, false, mock)
  },
  deleteSuggester: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSuggester', 'CloudSearch', result, true, true, mock)
  },
  deleteSuggesterAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSuggester', 'CloudSearch', result, false, true, mock)
  },
  deleteSuggesterThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSuggester', 'CloudSearch', result, true, false, mock)
  },
  describeAnalysisSchemes: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnalysisSchemes', 'CloudSearch', result, true, true, mock)
  },
  describeAnalysisSchemesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnalysisSchemes', 'CloudSearch', result, false, true, mock)
  },
  describeAnalysisSchemesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAnalysisSchemes', 'CloudSearch', result, true, false, mock)
  },
  describeAvailabilityOptions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAvailabilityOptions', 'CloudSearch', result, true, true, mock)
  },
  describeAvailabilityOptionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAvailabilityOptions', 'CloudSearch', result, false, true, mock)
  },
  describeAvailabilityOptionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAvailabilityOptions', 'CloudSearch', result, true, false, mock)
  },
  describeDomainEndpointOptions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainEndpointOptions', 'CloudSearch', result, true, true, mock)
  },
  describeDomainEndpointOptionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainEndpointOptions', 'CloudSearch', result, false, true, mock)
  },
  describeDomainEndpointOptionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomainEndpointOptions', 'CloudSearch', result, true, false, mock)
  },
  describeDomains: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomains', 'CloudSearch', result, true, true, mock)
  },
  describeDomainsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomains', 'CloudSearch', result, false, true, mock)
  },
  describeDomainsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDomains', 'CloudSearch', result, true, false, mock)
  },
  describeExpressions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExpressions', 'CloudSearch', result, true, true, mock)
  },
  describeExpressionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExpressions', 'CloudSearch', result, false, true, mock)
  },
  describeExpressionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeExpressions', 'CloudSearch', result, true, false, mock)
  },
  describeIndexFields: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeIndexFields', 'CloudSearch', result, true, true, mock)
  },
  describeIndexFieldsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeIndexFields', 'CloudSearch', result, false, true, mock)
  },
  describeIndexFieldsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeIndexFields', 'CloudSearch', result, true, false, mock)
  },
  describeScalingParameters: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingParameters', 'CloudSearch', result, true, true, mock)
  },
  describeScalingParametersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingParameters', 'CloudSearch', result, false, true, mock)
  },
  describeScalingParametersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingParameters', 'CloudSearch', result, true, false, mock)
  },
  describeServiceAccessPolicies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeServiceAccessPolicies', 'CloudSearch', result, true, true, mock)
  },
  describeServiceAccessPoliciesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeServiceAccessPolicies', 'CloudSearch', result, false, true, mock)
  },
  describeServiceAccessPoliciesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeServiceAccessPolicies', 'CloudSearch', result, true, false, mock)
  },
  describeSuggesters: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSuggesters', 'CloudSearch', result, true, true, mock)
  },
  describeSuggestersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSuggesters', 'CloudSearch', result, false, true, mock)
  },
  describeSuggestersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSuggesters', 'CloudSearch', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CloudSearch', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CloudSearch', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'CloudSearch', result, true, false, mock)
  },
  indexDocuments: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('indexDocuments', 'CloudSearch', result, true, true, mock)
  },
  indexDocumentsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('indexDocuments', 'CloudSearch', result, false, true, mock)
  },
  indexDocumentsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('indexDocuments', 'CloudSearch', result, true, false, mock)
  },
  listDomainNames: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainNames', 'CloudSearch', result, true, true, mock)
  },
  listDomainNamesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainNames', 'CloudSearch', result, false, true, mock)
  },
  listDomainNamesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDomainNames', 'CloudSearch', result, true, false, mock)
  },
  updateAvailabilityOptions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAvailabilityOptions', 'CloudSearch', result, true, true, mock)
  },
  updateAvailabilityOptionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAvailabilityOptions', 'CloudSearch', result, false, true, mock)
  },
  updateAvailabilityOptionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAvailabilityOptions', 'CloudSearch', result, true, false, mock)
  },
  updateDomainEndpointOptions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainEndpointOptions', 'CloudSearch', result, true, true, mock)
  },
  updateDomainEndpointOptionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainEndpointOptions', 'CloudSearch', result, false, true, mock)
  },
  updateDomainEndpointOptionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDomainEndpointOptions', 'CloudSearch', result, true, false, mock)
  },
  updateScalingParameters: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateScalingParameters', 'CloudSearch', result, true, true, mock)
  },
  updateScalingParametersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateScalingParameters', 'CloudSearch', result, false, true, mock)
  },
  updateScalingParametersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateScalingParameters', 'CloudSearch', result, true, false, mock)
  },
  updateServiceAccessPolicies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateServiceAccessPolicies', 'CloudSearch', result, true, true, mock)
  },
  updateServiceAccessPoliciesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateServiceAccessPolicies', 'CloudSearch', result, false, true, mock)
  },
  updateServiceAccessPoliciesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateServiceAccessPolicies', 'CloudSearch', result, true, false, mock)
  },
}
