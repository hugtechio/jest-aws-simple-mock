
/**
* @description mocks_v2:mockSSMIncidents.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockSSMIncidents = {
  createReplicationSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createReplicationSet', 'SSMIncidents', result, true, true, mock)
  },
  createReplicationSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createReplicationSet', 'SSMIncidents', result, false, true, mock)
  },
  createReplicationSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createReplicationSet', 'SSMIncidents', result, true, false, mock)
  },
  createResponsePlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResponsePlan', 'SSMIncidents', result, true, true, mock)
  },
  createResponsePlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResponsePlan', 'SSMIncidents', result, false, true, mock)
  },
  createResponsePlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createResponsePlan', 'SSMIncidents', result, true, false, mock)
  },
  createTimelineEvent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTimelineEvent', 'SSMIncidents', result, true, true, mock)
  },
  createTimelineEventAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTimelineEvent', 'SSMIncidents', result, false, true, mock)
  },
  createTimelineEventThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTimelineEvent', 'SSMIncidents', result, true, false, mock)
  },
  deleteIncidentRecord: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIncidentRecord', 'SSMIncidents', result, true, true, mock)
  },
  deleteIncidentRecordAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIncidentRecord', 'SSMIncidents', result, false, true, mock)
  },
  deleteIncidentRecordThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIncidentRecord', 'SSMIncidents', result, true, false, mock)
  },
  deleteReplicationSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReplicationSet', 'SSMIncidents', result, true, true, mock)
  },
  deleteReplicationSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReplicationSet', 'SSMIncidents', result, false, true, mock)
  },
  deleteReplicationSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteReplicationSet', 'SSMIncidents', result, true, false, mock)
  },
  deleteResourcePolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourcePolicy', 'SSMIncidents', result, true, true, mock)
  },
  deleteResourcePolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourcePolicy', 'SSMIncidents', result, false, true, mock)
  },
  deleteResourcePolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourcePolicy', 'SSMIncidents', result, true, false, mock)
  },
  deleteResponsePlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResponsePlan', 'SSMIncidents', result, true, true, mock)
  },
  deleteResponsePlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResponsePlan', 'SSMIncidents', result, false, true, mock)
  },
  deleteResponsePlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResponsePlan', 'SSMIncidents', result, true, false, mock)
  },
  deleteTimelineEvent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTimelineEvent', 'SSMIncidents', result, true, true, mock)
  },
  deleteTimelineEventAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTimelineEvent', 'SSMIncidents', result, false, true, mock)
  },
  deleteTimelineEventThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTimelineEvent', 'SSMIncidents', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'SSMIncidents', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'SSMIncidents', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'SSMIncidents', result, true, false, mock)
  },
  getIncidentRecord: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIncidentRecord', 'SSMIncidents', result, true, true, mock)
  },
  getIncidentRecordAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIncidentRecord', 'SSMIncidents', result, false, true, mock)
  },
  getIncidentRecordThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getIncidentRecord', 'SSMIncidents', result, true, false, mock)
  },
  getReplicationSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getReplicationSet', 'SSMIncidents', result, true, true, mock)
  },
  getReplicationSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getReplicationSet', 'SSMIncidents', result, false, true, mock)
  },
  getReplicationSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getReplicationSet', 'SSMIncidents', result, true, false, mock)
  },
  getResourcePolicies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourcePolicies', 'SSMIncidents', result, true, true, mock)
  },
  getResourcePoliciesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourcePolicies', 'SSMIncidents', result, false, true, mock)
  },
  getResourcePoliciesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourcePolicies', 'SSMIncidents', result, true, false, mock)
  },
  getResponsePlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResponsePlan', 'SSMIncidents', result, true, true, mock)
  },
  getResponsePlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResponsePlan', 'SSMIncidents', result, false, true, mock)
  },
  getResponsePlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResponsePlan', 'SSMIncidents', result, true, false, mock)
  },
  getTimelineEvent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTimelineEvent', 'SSMIncidents', result, true, true, mock)
  },
  getTimelineEventAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTimelineEvent', 'SSMIncidents', result, false, true, mock)
  },
  getTimelineEventThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getTimelineEvent', 'SSMIncidents', result, true, false, mock)
  },
  listIncidentRecords: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIncidentRecords', 'SSMIncidents', result, true, true, mock)
  },
  listIncidentRecordsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIncidentRecords', 'SSMIncidents', result, false, true, mock)
  },
  listIncidentRecordsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIncidentRecords', 'SSMIncidents', result, true, false, mock)
  },
  listRelatedItems: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRelatedItems', 'SSMIncidents', result, true, true, mock)
  },
  listRelatedItemsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRelatedItems', 'SSMIncidents', result, false, true, mock)
  },
  listRelatedItemsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRelatedItems', 'SSMIncidents', result, true, false, mock)
  },
  listReplicationSets: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReplicationSets', 'SSMIncidents', result, true, true, mock)
  },
  listReplicationSetsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReplicationSets', 'SSMIncidents', result, false, true, mock)
  },
  listReplicationSetsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listReplicationSets', 'SSMIncidents', result, true, false, mock)
  },
  listResponsePlans: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResponsePlans', 'SSMIncidents', result, true, true, mock)
  },
  listResponsePlansAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResponsePlans', 'SSMIncidents', result, false, true, mock)
  },
  listResponsePlansThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listResponsePlans', 'SSMIncidents', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'SSMIncidents', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'SSMIncidents', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'SSMIncidents', result, true, false, mock)
  },
  listTimelineEvents: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTimelineEvents', 'SSMIncidents', result, true, true, mock)
  },
  listTimelineEventsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTimelineEvents', 'SSMIncidents', result, false, true, mock)
  },
  listTimelineEventsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTimelineEvents', 'SSMIncidents', result, true, false, mock)
  },
  putResourcePolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putResourcePolicy', 'SSMIncidents', result, true, true, mock)
  },
  putResourcePolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putResourcePolicy', 'SSMIncidents', result, false, true, mock)
  },
  putResourcePolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putResourcePolicy', 'SSMIncidents', result, true, false, mock)
  },
  startIncident: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startIncident', 'SSMIncidents', result, true, true, mock)
  },
  startIncidentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startIncident', 'SSMIncidents', result, false, true, mock)
  },
  startIncidentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startIncident', 'SSMIncidents', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'SSMIncidents', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'SSMIncidents', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'SSMIncidents', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'SSMIncidents', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'SSMIncidents', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'SSMIncidents', result, true, false, mock)
  },
  updateDeletionProtection: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDeletionProtection', 'SSMIncidents', result, true, true, mock)
  },
  updateDeletionProtectionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDeletionProtection', 'SSMIncidents', result, false, true, mock)
  },
  updateDeletionProtectionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDeletionProtection', 'SSMIncidents', result, true, false, mock)
  },
  updateIncidentRecord: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateIncidentRecord', 'SSMIncidents', result, true, true, mock)
  },
  updateIncidentRecordAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateIncidentRecord', 'SSMIncidents', result, false, true, mock)
  },
  updateIncidentRecordThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateIncidentRecord', 'SSMIncidents', result, true, false, mock)
  },
  updateRelatedItems: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRelatedItems', 'SSMIncidents', result, true, true, mock)
  },
  updateRelatedItemsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRelatedItems', 'SSMIncidents', result, false, true, mock)
  },
  updateRelatedItemsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRelatedItems', 'SSMIncidents', result, true, false, mock)
  },
  updateReplicationSet: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationSet', 'SSMIncidents', result, true, true, mock)
  },
  updateReplicationSetAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationSet', 'SSMIncidents', result, false, true, mock)
  },
  updateReplicationSetThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateReplicationSet', 'SSMIncidents', result, true, false, mock)
  },
  updateResponsePlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResponsePlan', 'SSMIncidents', result, true, true, mock)
  },
  updateResponsePlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResponsePlan', 'SSMIncidents', result, false, true, mock)
  },
  updateResponsePlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateResponsePlan', 'SSMIncidents', result, true, false, mock)
  },
  updateTimelineEvent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTimelineEvent', 'SSMIncidents', result, true, true, mock)
  },
  updateTimelineEventAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTimelineEvent', 'SSMIncidents', result, false, true, mock)
  },
  updateTimelineEventThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTimelineEvent', 'SSMIncidents', result, true, false, mock)
  },
}
