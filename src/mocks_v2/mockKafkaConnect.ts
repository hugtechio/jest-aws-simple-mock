
/**
* @description mocks_v2:mockKafkaConnect.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockKafkaConnect = {
  createConnector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConnector', 'KafkaConnect', result, true, true, mock)
  },
  createConnectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConnector', 'KafkaConnect', result, false, true, mock)
  },
  createConnectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createConnector', 'KafkaConnect', result, true, false, mock)
  },
  createCustomPlugin: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCustomPlugin', 'KafkaConnect', result, true, true, mock)
  },
  createCustomPluginAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCustomPlugin', 'KafkaConnect', result, false, true, mock)
  },
  createCustomPluginThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createCustomPlugin', 'KafkaConnect', result, true, false, mock)
  },
  createWorkerConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkerConfiguration', 'KafkaConnect', result, true, true, mock)
  },
  createWorkerConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkerConfiguration', 'KafkaConnect', result, false, true, mock)
  },
  createWorkerConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkerConfiguration', 'KafkaConnect', result, true, false, mock)
  },
  deleteConnector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConnector', 'KafkaConnect', result, true, true, mock)
  },
  deleteConnectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConnector', 'KafkaConnect', result, false, true, mock)
  },
  deleteConnectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteConnector', 'KafkaConnect', result, true, false, mock)
  },
  describeConnector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConnector', 'KafkaConnect', result, true, true, mock)
  },
  describeConnectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConnector', 'KafkaConnect', result, false, true, mock)
  },
  describeConnectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeConnector', 'KafkaConnect', result, true, false, mock)
  },
  describeCustomPlugin: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCustomPlugin', 'KafkaConnect', result, true, true, mock)
  },
  describeCustomPluginAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCustomPlugin', 'KafkaConnect', result, false, true, mock)
  },
  describeCustomPluginThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCustomPlugin', 'KafkaConnect', result, true, false, mock)
  },
  describeWorkerConfiguration: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkerConfiguration', 'KafkaConnect', result, true, true, mock)
  },
  describeWorkerConfigurationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkerConfiguration', 'KafkaConnect', result, false, true, mock)
  },
  describeWorkerConfigurationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkerConfiguration', 'KafkaConnect', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'KafkaConnect', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'KafkaConnect', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'KafkaConnect', result, true, false, mock)
  },
  listConnectors: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConnectors', 'KafkaConnect', result, true, true, mock)
  },
  listConnectorsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConnectors', 'KafkaConnect', result, false, true, mock)
  },
  listConnectorsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listConnectors', 'KafkaConnect', result, true, false, mock)
  },
  listCustomPlugins: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCustomPlugins', 'KafkaConnect', result, true, true, mock)
  },
  listCustomPluginsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCustomPlugins', 'KafkaConnect', result, false, true, mock)
  },
  listCustomPluginsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listCustomPlugins', 'KafkaConnect', result, true, false, mock)
  },
  listWorkerConfigurations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkerConfigurations', 'KafkaConnect', result, true, true, mock)
  },
  listWorkerConfigurationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkerConfigurations', 'KafkaConnect', result, false, true, mock)
  },
  listWorkerConfigurationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkerConfigurations', 'KafkaConnect', result, true, false, mock)
  },
  updateConnector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConnector', 'KafkaConnect', result, true, true, mock)
  },
  updateConnectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConnector', 'KafkaConnect', result, false, true, mock)
  },
  updateConnectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateConnector', 'KafkaConnect', result, true, false, mock)
  },
}
