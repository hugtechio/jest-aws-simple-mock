
/**
* @description mocks_v2:mockFMS.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockFMS = {
  associateAdminAccount: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateAdminAccount', 'FMS', result, true, true, mock)
  },
  associateAdminAccountAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateAdminAccount', 'FMS', result, false, true, mock)
  },
  associateAdminAccountThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateAdminAccount', 'FMS', result, true, false, mock)
  },
  deleteAppsList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAppsList', 'FMS', result, true, true, mock)
  },
  deleteAppsListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAppsList', 'FMS', result, false, true, mock)
  },
  deleteAppsListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAppsList', 'FMS', result, true, false, mock)
  },
  deleteNotificationChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteNotificationChannel', 'FMS', result, true, true, mock)
  },
  deleteNotificationChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteNotificationChannel', 'FMS', result, false, true, mock)
  },
  deleteNotificationChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteNotificationChannel', 'FMS', result, true, false, mock)
  },
  deletePolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePolicy', 'FMS', result, true, true, mock)
  },
  deletePolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePolicy', 'FMS', result, false, true, mock)
  },
  deletePolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePolicy', 'FMS', result, true, false, mock)
  },
  deleteProtocolsList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProtocolsList', 'FMS', result, true, true, mock)
  },
  deleteProtocolsListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProtocolsList', 'FMS', result, false, true, mock)
  },
  deleteProtocolsListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteProtocolsList', 'FMS', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'FMS', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'FMS', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'FMS', result, true, false, mock)
  },
  disassociateAdminAccount: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateAdminAccount', 'FMS', result, true, true, mock)
  },
  disassociateAdminAccountAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateAdminAccount', 'FMS', result, false, true, mock)
  },
  disassociateAdminAccountThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateAdminAccount', 'FMS', result, true, false, mock)
  },
  getAdminAccount: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAdminAccount', 'FMS', result, true, true, mock)
  },
  getAdminAccountAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAdminAccount', 'FMS', result, false, true, mock)
  },
  getAdminAccountThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAdminAccount', 'FMS', result, true, false, mock)
  },
  getAppsList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAppsList', 'FMS', result, true, true, mock)
  },
  getAppsListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAppsList', 'FMS', result, false, true, mock)
  },
  getAppsListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getAppsList', 'FMS', result, true, false, mock)
  },
  getComplianceDetail: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getComplianceDetail', 'FMS', result, true, true, mock)
  },
  getComplianceDetailAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getComplianceDetail', 'FMS', result, false, true, mock)
  },
  getComplianceDetailThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getComplianceDetail', 'FMS', result, true, false, mock)
  },
  getNotificationChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNotificationChannel', 'FMS', result, true, true, mock)
  },
  getNotificationChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNotificationChannel', 'FMS', result, false, true, mock)
  },
  getNotificationChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getNotificationChannel', 'FMS', result, true, false, mock)
  },
  getPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPolicy', 'FMS', result, true, true, mock)
  },
  getPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPolicy', 'FMS', result, false, true, mock)
  },
  getPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getPolicy', 'FMS', result, true, false, mock)
  },
  getProtectionStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProtectionStatus', 'FMS', result, true, true, mock)
  },
  getProtectionStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProtectionStatus', 'FMS', result, false, true, mock)
  },
  getProtectionStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProtectionStatus', 'FMS', result, true, false, mock)
  },
  getProtocolsList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProtocolsList', 'FMS', result, true, true, mock)
  },
  getProtocolsListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProtocolsList', 'FMS', result, false, true, mock)
  },
  getProtocolsListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getProtocolsList', 'FMS', result, true, false, mock)
  },
  getViolationDetails: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getViolationDetails', 'FMS', result, true, true, mock)
  },
  getViolationDetailsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getViolationDetails', 'FMS', result, false, true, mock)
  },
  getViolationDetailsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getViolationDetails', 'FMS', result, true, false, mock)
  },
  listAppsLists: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAppsLists', 'FMS', result, true, true, mock)
  },
  listAppsListsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAppsLists', 'FMS', result, false, true, mock)
  },
  listAppsListsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAppsLists', 'FMS', result, true, false, mock)
  },
  listComplianceStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listComplianceStatus', 'FMS', result, true, true, mock)
  },
  listComplianceStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listComplianceStatus', 'FMS', result, false, true, mock)
  },
  listComplianceStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listComplianceStatus', 'FMS', result, true, false, mock)
  },
  listMemberAccounts: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMemberAccounts', 'FMS', result, true, true, mock)
  },
  listMemberAccountsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMemberAccounts', 'FMS', result, false, true, mock)
  },
  listMemberAccountsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listMemberAccounts', 'FMS', result, true, false, mock)
  },
  listPolicies: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPolicies', 'FMS', result, true, true, mock)
  },
  listPoliciesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPolicies', 'FMS', result, false, true, mock)
  },
  listPoliciesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPolicies', 'FMS', result, true, false, mock)
  },
  listProtocolsLists: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProtocolsLists', 'FMS', result, true, true, mock)
  },
  listProtocolsListsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProtocolsLists', 'FMS', result, false, true, mock)
  },
  listProtocolsListsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listProtocolsLists', 'FMS', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'FMS', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'FMS', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'FMS', result, true, false, mock)
  },
  putAppsList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putAppsList', 'FMS', result, true, true, mock)
  },
  putAppsListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putAppsList', 'FMS', result, false, true, mock)
  },
  putAppsListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putAppsList', 'FMS', result, true, false, mock)
  },
  putNotificationChannel: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putNotificationChannel', 'FMS', result, true, true, mock)
  },
  putNotificationChannelAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putNotificationChannel', 'FMS', result, false, true, mock)
  },
  putNotificationChannelThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putNotificationChannel', 'FMS', result, true, false, mock)
  },
  putPolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPolicy', 'FMS', result, true, true, mock)
  },
  putPolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPolicy', 'FMS', result, false, true, mock)
  },
  putPolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPolicy', 'FMS', result, true, false, mock)
  },
  putProtocolsList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProtocolsList', 'FMS', result, true, true, mock)
  },
  putProtocolsListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProtocolsList', 'FMS', result, false, true, mock)
  },
  putProtocolsListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putProtocolsList', 'FMS', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'FMS', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'FMS', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'FMS', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'FMS', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'FMS', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'FMS', result, true, false, mock)
  },
}
