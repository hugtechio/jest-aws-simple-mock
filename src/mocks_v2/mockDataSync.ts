
/**
* @description mocks_v2:mockDataSync.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockDataSync = {
  cancelTaskExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelTaskExecution', 'DataSync', result, true, true, mock)
  },
  cancelTaskExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelTaskExecution', 'DataSync', result, false, true, mock)
  },
  cancelTaskExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('cancelTaskExecution', 'DataSync', result, true, false, mock)
  },
  createAgent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAgent', 'DataSync', result, true, true, mock)
  },
  createAgentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAgent', 'DataSync', result, false, true, mock)
  },
  createAgentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createAgent', 'DataSync', result, true, false, mock)
  },
  createLocationEfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationEfs', 'DataSync', result, true, true, mock)
  },
  createLocationEfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationEfs', 'DataSync', result, false, true, mock)
  },
  createLocationEfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationEfs', 'DataSync', result, true, false, mock)
  },
  createLocationFsxWindows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationFsxWindows', 'DataSync', result, true, true, mock)
  },
  createLocationFsxWindowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationFsxWindows', 'DataSync', result, false, true, mock)
  },
  createLocationFsxWindowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationFsxWindows', 'DataSync', result, true, false, mock)
  },
  createLocationHdfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationHdfs', 'DataSync', result, true, true, mock)
  },
  createLocationHdfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationHdfs', 'DataSync', result, false, true, mock)
  },
  createLocationHdfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationHdfs', 'DataSync', result, true, false, mock)
  },
  createLocationNfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationNfs', 'DataSync', result, true, true, mock)
  },
  createLocationNfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationNfs', 'DataSync', result, false, true, mock)
  },
  createLocationNfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationNfs', 'DataSync', result, true, false, mock)
  },
  createLocationObjectStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationObjectStorage', 'DataSync', result, true, true, mock)
  },
  createLocationObjectStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationObjectStorage', 'DataSync', result, false, true, mock)
  },
  createLocationObjectStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationObjectStorage', 'DataSync', result, true, false, mock)
  },
  createLocationS3: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationS3', 'DataSync', result, true, true, mock)
  },
  createLocationS3All: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationS3', 'DataSync', result, false, true, mock)
  },
  createLocationS3Throw: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationS3', 'DataSync', result, true, false, mock)
  },
  createLocationSmb: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationSmb', 'DataSync', result, true, true, mock)
  },
  createLocationSmbAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationSmb', 'DataSync', result, false, true, mock)
  },
  createLocationSmbThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createLocationSmb', 'DataSync', result, true, false, mock)
  },
  createTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTask', 'DataSync', result, true, true, mock)
  },
  createTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTask', 'DataSync', result, false, true, mock)
  },
  createTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createTask', 'DataSync', result, true, false, mock)
  },
  deleteAgent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAgent', 'DataSync', result, true, true, mock)
  },
  deleteAgentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAgent', 'DataSync', result, false, true, mock)
  },
  deleteAgentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteAgent', 'DataSync', result, true, false, mock)
  },
  deleteLocation: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLocation', 'DataSync', result, true, true, mock)
  },
  deleteLocationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLocation', 'DataSync', result, false, true, mock)
  },
  deleteLocationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteLocation', 'DataSync', result, true, false, mock)
  },
  deleteTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTask', 'DataSync', result, true, true, mock)
  },
  deleteTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTask', 'DataSync', result, false, true, mock)
  },
  deleteTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteTask', 'DataSync', result, true, false, mock)
  },
  describeAgent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAgent', 'DataSync', result, true, true, mock)
  },
  describeAgentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAgent', 'DataSync', result, false, true, mock)
  },
  describeAgentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAgent', 'DataSync', result, true, false, mock)
  },
  describeLocationEfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationEfs', 'DataSync', result, true, true, mock)
  },
  describeLocationEfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationEfs', 'DataSync', result, false, true, mock)
  },
  describeLocationEfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationEfs', 'DataSync', result, true, false, mock)
  },
  describeLocationFsxWindows: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationFsxWindows', 'DataSync', result, true, true, mock)
  },
  describeLocationFsxWindowsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationFsxWindows', 'DataSync', result, false, true, mock)
  },
  describeLocationFsxWindowsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationFsxWindows', 'DataSync', result, true, false, mock)
  },
  describeLocationHdfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationHdfs', 'DataSync', result, true, true, mock)
  },
  describeLocationHdfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationHdfs', 'DataSync', result, false, true, mock)
  },
  describeLocationHdfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationHdfs', 'DataSync', result, true, false, mock)
  },
  describeLocationNfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationNfs', 'DataSync', result, true, true, mock)
  },
  describeLocationNfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationNfs', 'DataSync', result, false, true, mock)
  },
  describeLocationNfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationNfs', 'DataSync', result, true, false, mock)
  },
  describeLocationObjectStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationObjectStorage', 'DataSync', result, true, true, mock)
  },
  describeLocationObjectStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationObjectStorage', 'DataSync', result, false, true, mock)
  },
  describeLocationObjectStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationObjectStorage', 'DataSync', result, true, false, mock)
  },
  describeLocationS3: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationS3', 'DataSync', result, true, true, mock)
  },
  describeLocationS3All: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationS3', 'DataSync', result, false, true, mock)
  },
  describeLocationS3Throw: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationS3', 'DataSync', result, true, false, mock)
  },
  describeLocationSmb: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationSmb', 'DataSync', result, true, true, mock)
  },
  describeLocationSmbAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationSmb', 'DataSync', result, false, true, mock)
  },
  describeLocationSmbThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeLocationSmb', 'DataSync', result, true, false, mock)
  },
  describeTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTask', 'DataSync', result, true, true, mock)
  },
  describeTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTask', 'DataSync', result, false, true, mock)
  },
  describeTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTask', 'DataSync', result, true, false, mock)
  },
  describeTaskExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTaskExecution', 'DataSync', result, true, true, mock)
  },
  describeTaskExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTaskExecution', 'DataSync', result, false, true, mock)
  },
  describeTaskExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeTaskExecution', 'DataSync', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataSync', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataSync', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'DataSync', result, true, false, mock)
  },
  listAgents: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAgents', 'DataSync', result, true, true, mock)
  },
  listAgentsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAgents', 'DataSync', result, false, true, mock)
  },
  listAgentsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAgents', 'DataSync', result, true, false, mock)
  },
  listLocations: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLocations', 'DataSync', result, true, true, mock)
  },
  listLocationsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLocations', 'DataSync', result, false, true, mock)
  },
  listLocationsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listLocations', 'DataSync', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DataSync', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DataSync', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'DataSync', result, true, false, mock)
  },
  listTaskExecutions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTaskExecutions', 'DataSync', result, true, true, mock)
  },
  listTaskExecutionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTaskExecutions', 'DataSync', result, false, true, mock)
  },
  listTaskExecutionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTaskExecutions', 'DataSync', result, true, false, mock)
  },
  listTasks: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTasks', 'DataSync', result, true, true, mock)
  },
  listTasksAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTasks', 'DataSync', result, false, true, mock)
  },
  listTasksThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTasks', 'DataSync', result, true, false, mock)
  },
  startTaskExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTaskExecution', 'DataSync', result, true, true, mock)
  },
  startTaskExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTaskExecution', 'DataSync', result, false, true, mock)
  },
  startTaskExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startTaskExecution', 'DataSync', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'DataSync', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'DataSync', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'DataSync', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'DataSync', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'DataSync', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'DataSync', result, true, false, mock)
  },
  updateAgent: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAgent', 'DataSync', result, true, true, mock)
  },
  updateAgentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAgent', 'DataSync', result, false, true, mock)
  },
  updateAgentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateAgent', 'DataSync', result, true, false, mock)
  },
  updateLocationHdfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationHdfs', 'DataSync', result, true, true, mock)
  },
  updateLocationHdfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationHdfs', 'DataSync', result, false, true, mock)
  },
  updateLocationHdfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationHdfs', 'DataSync', result, true, false, mock)
  },
  updateLocationNfs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationNfs', 'DataSync', result, true, true, mock)
  },
  updateLocationNfsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationNfs', 'DataSync', result, false, true, mock)
  },
  updateLocationNfsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationNfs', 'DataSync', result, true, false, mock)
  },
  updateLocationObjectStorage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationObjectStorage', 'DataSync', result, true, true, mock)
  },
  updateLocationObjectStorageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationObjectStorage', 'DataSync', result, false, true, mock)
  },
  updateLocationObjectStorageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationObjectStorage', 'DataSync', result, true, false, mock)
  },
  updateLocationSmb: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationSmb', 'DataSync', result, true, true, mock)
  },
  updateLocationSmbAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationSmb', 'DataSync', result, false, true, mock)
  },
  updateLocationSmbThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateLocationSmb', 'DataSync', result, true, false, mock)
  },
  updateTask: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTask', 'DataSync', result, true, true, mock)
  },
  updateTaskAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTask', 'DataSync', result, false, true, mock)
  },
  updateTaskThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTask', 'DataSync', result, true, false, mock)
  },
  updateTaskExecution: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTaskExecution', 'DataSync', result, true, true, mock)
  },
  updateTaskExecutionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTaskExecution', 'DataSync', result, false, true, mock)
  },
  updateTaskExecutionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateTaskExecution', 'DataSync', result, true, false, mock)
  },
}
