
/**
* @description mocks_v2:mockAutoScalingPlans.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockAutoScalingPlans = {
  createScalingPlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createScalingPlan', 'AutoScalingPlans', result, true, true, mock)
  },
  createScalingPlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createScalingPlan', 'AutoScalingPlans', result, false, true, mock)
  },
  createScalingPlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createScalingPlan', 'AutoScalingPlans', result, true, false, mock)
  },
  deleteScalingPlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteScalingPlan', 'AutoScalingPlans', result, true, true, mock)
  },
  deleteScalingPlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteScalingPlan', 'AutoScalingPlans', result, false, true, mock)
  },
  deleteScalingPlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteScalingPlan', 'AutoScalingPlans', result, true, false, mock)
  },
  describeScalingPlanResources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingPlanResources', 'AutoScalingPlans', result, true, true, mock)
  },
  describeScalingPlanResourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingPlanResources', 'AutoScalingPlans', result, false, true, mock)
  },
  describeScalingPlanResourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingPlanResources', 'AutoScalingPlans', result, true, false, mock)
  },
  describeScalingPlans: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingPlans', 'AutoScalingPlans', result, true, true, mock)
  },
  describeScalingPlansAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingPlans', 'AutoScalingPlans', result, false, true, mock)
  },
  describeScalingPlansThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeScalingPlans', 'AutoScalingPlans', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AutoScalingPlans', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AutoScalingPlans', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'AutoScalingPlans', result, true, false, mock)
  },
  getScalingPlanResourceForecastData: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getScalingPlanResourceForecastData', 'AutoScalingPlans', result, true, true, mock)
  },
  getScalingPlanResourceForecastDataAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getScalingPlanResourceForecastData', 'AutoScalingPlans', result, false, true, mock)
  },
  getScalingPlanResourceForecastDataThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getScalingPlanResourceForecastData', 'AutoScalingPlans', result, true, false, mock)
  },
  updateScalingPlan: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateScalingPlan', 'AutoScalingPlans', result, true, true, mock)
  },
  updateScalingPlanAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateScalingPlan', 'AutoScalingPlans', result, false, true, mock)
  },
  updateScalingPlanThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateScalingPlan', 'AutoScalingPlans', result, true, false, mock)
  },
}
