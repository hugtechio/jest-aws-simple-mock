
/**
* @description mocks_v2:mockIoT1ClickDevicesService.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockIoT1ClickDevicesService = {
  claimDevicesByClaimCode: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('claimDevicesByClaimCode', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  claimDevicesByClaimCodeAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('claimDevicesByClaimCode', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  claimDevicesByClaimCodeThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('claimDevicesByClaimCode', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  describeDevice: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDevice', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  describeDeviceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDevice', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  describeDeviceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDevice', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  finalizeDeviceClaim: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('finalizeDeviceClaim', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  finalizeDeviceClaimAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('finalizeDeviceClaim', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  finalizeDeviceClaimThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('finalizeDeviceClaim', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  getDeviceMethods: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDeviceMethods', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  getDeviceMethodsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDeviceMethods', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  getDeviceMethodsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDeviceMethods', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  initiateDeviceClaim: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('initiateDeviceClaim', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  initiateDeviceClaimAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('initiateDeviceClaim', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  initiateDeviceClaimThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('initiateDeviceClaim', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  invokeDeviceMethod: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('invokeDeviceMethod', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  invokeDeviceMethodAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('invokeDeviceMethod', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  invokeDeviceMethodThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('invokeDeviceMethod', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  listDeviceEvents: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDeviceEvents', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  listDeviceEventsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDeviceEvents', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  listDeviceEventsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDeviceEvents', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  listDevices: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDevices', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  listDevicesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDevices', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  listDevicesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDevices', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  unclaimDevice: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unclaimDevice', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  unclaimDeviceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unclaimDevice', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  unclaimDeviceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('unclaimDevice', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'IoT1ClickDevicesService', result, true, false, mock)
  },
  updateDeviceState: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDeviceState', 'IoT1ClickDevicesService', result, true, true, mock)
  },
  updateDeviceStateAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDeviceState', 'IoT1ClickDevicesService', result, false, true, mock)
  },
  updateDeviceStateThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDeviceState', 'IoT1ClickDevicesService', result, true, false, mock)
  },
}
