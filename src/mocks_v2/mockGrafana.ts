
/**
* @description mocks_v2:mockGrafana.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockGrafana = {
  associateLicense: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateLicense', 'Grafana', result, true, true, mock)
  },
  associateLicenseAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateLicense', 'Grafana', result, false, true, mock)
  },
  associateLicenseThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('associateLicense', 'Grafana', result, true, false, mock)
  },
  createWorkspace: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkspace', 'Grafana', result, true, true, mock)
  },
  createWorkspaceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkspace', 'Grafana', result, false, true, mock)
  },
  createWorkspaceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createWorkspace', 'Grafana', result, true, false, mock)
  },
  deleteWorkspace: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkspace', 'Grafana', result, true, true, mock)
  },
  deleteWorkspaceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkspace', 'Grafana', result, false, true, mock)
  },
  deleteWorkspaceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteWorkspace', 'Grafana', result, true, false, mock)
  },
  describeWorkspace: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkspace', 'Grafana', result, true, true, mock)
  },
  describeWorkspaceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkspace', 'Grafana', result, false, true, mock)
  },
  describeWorkspaceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkspace', 'Grafana', result, true, false, mock)
  },
  describeWorkspaceAuthentication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkspaceAuthentication', 'Grafana', result, true, true, mock)
  },
  describeWorkspaceAuthenticationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkspaceAuthentication', 'Grafana', result, false, true, mock)
  },
  describeWorkspaceAuthenticationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeWorkspaceAuthentication', 'Grafana', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Grafana', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Grafana', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Grafana', result, true, false, mock)
  },
  disassociateLicense: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateLicense', 'Grafana', result, true, true, mock)
  },
  disassociateLicenseAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateLicense', 'Grafana', result, false, true, mock)
  },
  disassociateLicenseThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('disassociateLicense', 'Grafana', result, true, false, mock)
  },
  listPermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPermissions', 'Grafana', result, true, true, mock)
  },
  listPermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPermissions', 'Grafana', result, false, true, mock)
  },
  listPermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listPermissions', 'Grafana', result, true, false, mock)
  },
  listWorkspaces: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkspaces', 'Grafana', result, true, true, mock)
  },
  listWorkspacesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkspaces', 'Grafana', result, false, true, mock)
  },
  listWorkspacesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listWorkspaces', 'Grafana', result, true, false, mock)
  },
  updatePermissions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePermissions', 'Grafana', result, true, true, mock)
  },
  updatePermissionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePermissions', 'Grafana', result, false, true, mock)
  },
  updatePermissionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updatePermissions', 'Grafana', result, true, false, mock)
  },
  updateWorkspace: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkspace', 'Grafana', result, true, true, mock)
  },
  updateWorkspaceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkspace', 'Grafana', result, false, true, mock)
  },
  updateWorkspaceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkspace', 'Grafana', result, true, false, mock)
  },
  updateWorkspaceAuthentication: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkspaceAuthentication', 'Grafana', result, true, true, mock)
  },
  updateWorkspaceAuthenticationAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkspaceAuthentication', 'Grafana', result, false, true, mock)
  },
  updateWorkspaceAuthenticationThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateWorkspaceAuthentication', 'Grafana', result, true, false, mock)
  },
}
