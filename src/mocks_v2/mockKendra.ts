
/**
* @description mocks_v2:mockKendra.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockKendra = {
  batchDeleteDocument: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteDocument', 'Kendra', result, true, true, mock)
  },
  batchDeleteDocumentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteDocument', 'Kendra', result, false, true, mock)
  },
  batchDeleteDocumentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDeleteDocument', 'Kendra', result, true, false, mock)
  },
  batchGetDocumentStatus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchGetDocumentStatus', 'Kendra', result, true, true, mock)
  },
  batchGetDocumentStatusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchGetDocumentStatus', 'Kendra', result, false, true, mock)
  },
  batchGetDocumentStatusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchGetDocumentStatus', 'Kendra', result, true, false, mock)
  },
  batchPutDocument: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchPutDocument', 'Kendra', result, true, true, mock)
  },
  batchPutDocumentAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchPutDocument', 'Kendra', result, false, true, mock)
  },
  batchPutDocumentThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchPutDocument', 'Kendra', result, true, false, mock)
  },
  clearQuerySuggestions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('clearQuerySuggestions', 'Kendra', result, true, true, mock)
  },
  clearQuerySuggestionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('clearQuerySuggestions', 'Kendra', result, false, true, mock)
  },
  clearQuerySuggestionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('clearQuerySuggestions', 'Kendra', result, true, false, mock)
  },
  createDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSource', 'Kendra', result, true, true, mock)
  },
  createDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSource', 'Kendra', result, false, true, mock)
  },
  createDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDataSource', 'Kendra', result, true, false, mock)
  },
  createFaq: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFaq', 'Kendra', result, true, true, mock)
  },
  createFaqAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFaq', 'Kendra', result, false, true, mock)
  },
  createFaqThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createFaq', 'Kendra', result, true, false, mock)
  },
  createIndex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createIndex', 'Kendra', result, true, true, mock)
  },
  createIndexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createIndex', 'Kendra', result, false, true, mock)
  },
  createIndexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createIndex', 'Kendra', result, true, false, mock)
  },
  createQuerySuggestionsBlockList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createQuerySuggestionsBlockList', 'Kendra', result, true, true, mock)
  },
  createQuerySuggestionsBlockListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createQuerySuggestionsBlockList', 'Kendra', result, false, true, mock)
  },
  createQuerySuggestionsBlockListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createQuerySuggestionsBlockList', 'Kendra', result, true, false, mock)
  },
  createThesaurus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createThesaurus', 'Kendra', result, true, true, mock)
  },
  createThesaurusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createThesaurus', 'Kendra', result, false, true, mock)
  },
  createThesaurusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createThesaurus', 'Kendra', result, true, false, mock)
  },
  deleteDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSource', 'Kendra', result, true, true, mock)
  },
  deleteDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSource', 'Kendra', result, false, true, mock)
  },
  deleteDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDataSource', 'Kendra', result, true, false, mock)
  },
  deleteFaq: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFaq', 'Kendra', result, true, true, mock)
  },
  deleteFaqAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFaq', 'Kendra', result, false, true, mock)
  },
  deleteFaqThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteFaq', 'Kendra', result, true, false, mock)
  },
  deleteIndex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIndex', 'Kendra', result, true, true, mock)
  },
  deleteIndexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIndex', 'Kendra', result, false, true, mock)
  },
  deleteIndexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteIndex', 'Kendra', result, true, false, mock)
  },
  deletePrincipalMapping: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePrincipalMapping', 'Kendra', result, true, true, mock)
  },
  deletePrincipalMappingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePrincipalMapping', 'Kendra', result, false, true, mock)
  },
  deletePrincipalMappingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deletePrincipalMapping', 'Kendra', result, true, false, mock)
  },
  deleteQuerySuggestionsBlockList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteQuerySuggestionsBlockList', 'Kendra', result, true, true, mock)
  },
  deleteQuerySuggestionsBlockListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteQuerySuggestionsBlockList', 'Kendra', result, false, true, mock)
  },
  deleteQuerySuggestionsBlockListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteQuerySuggestionsBlockList', 'Kendra', result, true, false, mock)
  },
  deleteThesaurus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteThesaurus', 'Kendra', result, true, true, mock)
  },
  deleteThesaurusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteThesaurus', 'Kendra', result, false, true, mock)
  },
  deleteThesaurusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteThesaurus', 'Kendra', result, true, false, mock)
  },
  describeDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataSource', 'Kendra', result, true, true, mock)
  },
  describeDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataSource', 'Kendra', result, false, true, mock)
  },
  describeDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDataSource', 'Kendra', result, true, false, mock)
  },
  describeFaq: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFaq', 'Kendra', result, true, true, mock)
  },
  describeFaqAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFaq', 'Kendra', result, false, true, mock)
  },
  describeFaqThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeFaq', 'Kendra', result, true, false, mock)
  },
  describeIndex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeIndex', 'Kendra', result, true, true, mock)
  },
  describeIndexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeIndex', 'Kendra', result, false, true, mock)
  },
  describeIndexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeIndex', 'Kendra', result, true, false, mock)
  },
  describePrincipalMapping: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePrincipalMapping', 'Kendra', result, true, true, mock)
  },
  describePrincipalMappingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePrincipalMapping', 'Kendra', result, false, true, mock)
  },
  describePrincipalMappingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describePrincipalMapping', 'Kendra', result, true, false, mock)
  },
  describeQuerySuggestionsBlockList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeQuerySuggestionsBlockList', 'Kendra', result, true, true, mock)
  },
  describeQuerySuggestionsBlockListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeQuerySuggestionsBlockList', 'Kendra', result, false, true, mock)
  },
  describeQuerySuggestionsBlockListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeQuerySuggestionsBlockList', 'Kendra', result, true, false, mock)
  },
  describeQuerySuggestionsConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeQuerySuggestionsConfig', 'Kendra', result, true, true, mock)
  },
  describeQuerySuggestionsConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeQuerySuggestionsConfig', 'Kendra', result, false, true, mock)
  },
  describeQuerySuggestionsConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeQuerySuggestionsConfig', 'Kendra', result, true, false, mock)
  },
  describeThesaurus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeThesaurus', 'Kendra', result, true, true, mock)
  },
  describeThesaurusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeThesaurus', 'Kendra', result, false, true, mock)
  },
  describeThesaurusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeThesaurus', 'Kendra', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Kendra', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Kendra', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Kendra', result, true, false, mock)
  },
  getQuerySuggestions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getQuerySuggestions', 'Kendra', result, true, true, mock)
  },
  getQuerySuggestionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getQuerySuggestions', 'Kendra', result, false, true, mock)
  },
  getQuerySuggestionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getQuerySuggestions', 'Kendra', result, true, false, mock)
  },
  listDataSourceSyncJobs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSourceSyncJobs', 'Kendra', result, true, true, mock)
  },
  listDataSourceSyncJobsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSourceSyncJobs', 'Kendra', result, false, true, mock)
  },
  listDataSourceSyncJobsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSourceSyncJobs', 'Kendra', result, true, false, mock)
  },
  listDataSources: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSources', 'Kendra', result, true, true, mock)
  },
  listDataSourcesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSources', 'Kendra', result, false, true, mock)
  },
  listDataSourcesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDataSources', 'Kendra', result, true, false, mock)
  },
  listFaqs: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFaqs', 'Kendra', result, true, true, mock)
  },
  listFaqsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFaqs', 'Kendra', result, false, true, mock)
  },
  listFaqsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listFaqs', 'Kendra', result, true, false, mock)
  },
  listGroupsOlderThanOrderingId: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGroupsOlderThanOrderingId', 'Kendra', result, true, true, mock)
  },
  listGroupsOlderThanOrderingIdAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGroupsOlderThanOrderingId', 'Kendra', result, false, true, mock)
  },
  listGroupsOlderThanOrderingIdThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listGroupsOlderThanOrderingId', 'Kendra', result, true, false, mock)
  },
  listIndices: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIndices', 'Kendra', result, true, true, mock)
  },
  listIndicesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIndices', 'Kendra', result, false, true, mock)
  },
  listIndicesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listIndices', 'Kendra', result, true, false, mock)
  },
  listQuerySuggestionsBlockLists: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listQuerySuggestionsBlockLists', 'Kendra', result, true, true, mock)
  },
  listQuerySuggestionsBlockListsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listQuerySuggestionsBlockLists', 'Kendra', result, false, true, mock)
  },
  listQuerySuggestionsBlockListsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listQuerySuggestionsBlockLists', 'Kendra', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Kendra', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Kendra', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Kendra', result, true, false, mock)
  },
  listThesauri: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listThesauri', 'Kendra', result, true, true, mock)
  },
  listThesauriAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listThesauri', 'Kendra', result, false, true, mock)
  },
  listThesauriThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listThesauri', 'Kendra', result, true, false, mock)
  },
  putPrincipalMapping: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPrincipalMapping', 'Kendra', result, true, true, mock)
  },
  putPrincipalMappingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPrincipalMapping', 'Kendra', result, false, true, mock)
  },
  putPrincipalMappingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putPrincipalMapping', 'Kendra', result, true, false, mock)
  },
  query: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('query', 'Kendra', result, true, true, mock)
  },
  queryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('query', 'Kendra', result, false, true, mock)
  },
  queryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('query', 'Kendra', result, true, false, mock)
  },
  startDataSourceSyncJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataSourceSyncJob', 'Kendra', result, true, true, mock)
  },
  startDataSourceSyncJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataSourceSyncJob', 'Kendra', result, false, true, mock)
  },
  startDataSourceSyncJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDataSourceSyncJob', 'Kendra', result, true, false, mock)
  },
  stopDataSourceSyncJob: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDataSourceSyncJob', 'Kendra', result, true, true, mock)
  },
  stopDataSourceSyncJobAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDataSourceSyncJob', 'Kendra', result, false, true, mock)
  },
  stopDataSourceSyncJobThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDataSourceSyncJob', 'Kendra', result, true, false, mock)
  },
  submitFeedback: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('submitFeedback', 'Kendra', result, true, true, mock)
  },
  submitFeedbackAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('submitFeedback', 'Kendra', result, false, true, mock)
  },
  submitFeedbackThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('submitFeedback', 'Kendra', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Kendra', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Kendra', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Kendra', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Kendra', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Kendra', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Kendra', result, true, false, mock)
  },
  updateDataSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSource', 'Kendra', result, true, true, mock)
  },
  updateDataSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSource', 'Kendra', result, false, true, mock)
  },
  updateDataSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDataSource', 'Kendra', result, true, false, mock)
  },
  updateIndex: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateIndex', 'Kendra', result, true, true, mock)
  },
  updateIndexAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateIndex', 'Kendra', result, false, true, mock)
  },
  updateIndexThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateIndex', 'Kendra', result, true, false, mock)
  },
  updateQuerySuggestionsBlockList: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateQuerySuggestionsBlockList', 'Kendra', result, true, true, mock)
  },
  updateQuerySuggestionsBlockListAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateQuerySuggestionsBlockList', 'Kendra', result, false, true, mock)
  },
  updateQuerySuggestionsBlockListThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateQuerySuggestionsBlockList', 'Kendra', result, true, false, mock)
  },
  updateQuerySuggestionsConfig: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateQuerySuggestionsConfig', 'Kendra', result, true, true, mock)
  },
  updateQuerySuggestionsConfigAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateQuerySuggestionsConfig', 'Kendra', result, false, true, mock)
  },
  updateQuerySuggestionsConfigThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateQuerySuggestionsConfig', 'Kendra', result, true, false, mock)
  },
  updateThesaurus: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateThesaurus', 'Kendra', result, true, true, mock)
  },
  updateThesaurusAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateThesaurus', 'Kendra', result, false, true, mock)
  },
  updateThesaurusThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateThesaurus', 'Kendra', result, true, false, mock)
  },
}
