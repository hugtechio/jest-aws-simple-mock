
/**
* @description mocks_v2:mockIoTEventsData.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockIoTEventsData = {
  batchAcknowledgeAlarm: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchAcknowledgeAlarm', 'IoTEventsData', result, true, true, mock)
  },
  batchAcknowledgeAlarmAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchAcknowledgeAlarm', 'IoTEventsData', result, false, true, mock)
  },
  batchAcknowledgeAlarmThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchAcknowledgeAlarm', 'IoTEventsData', result, true, false, mock)
  },
  batchDisableAlarm: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDisableAlarm', 'IoTEventsData', result, true, true, mock)
  },
  batchDisableAlarmAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDisableAlarm', 'IoTEventsData', result, false, true, mock)
  },
  batchDisableAlarmThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchDisableAlarm', 'IoTEventsData', result, true, false, mock)
  },
  batchEnableAlarm: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchEnableAlarm', 'IoTEventsData', result, true, true, mock)
  },
  batchEnableAlarmAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchEnableAlarm', 'IoTEventsData', result, false, true, mock)
  },
  batchEnableAlarmThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchEnableAlarm', 'IoTEventsData', result, true, false, mock)
  },
  batchPutMessage: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchPutMessage', 'IoTEventsData', result, true, true, mock)
  },
  batchPutMessageAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchPutMessage', 'IoTEventsData', result, false, true, mock)
  },
  batchPutMessageThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchPutMessage', 'IoTEventsData', result, true, false, mock)
  },
  batchResetAlarm: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchResetAlarm', 'IoTEventsData', result, true, true, mock)
  },
  batchResetAlarmAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchResetAlarm', 'IoTEventsData', result, false, true, mock)
  },
  batchResetAlarmThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchResetAlarm', 'IoTEventsData', result, true, false, mock)
  },
  batchSnoozeAlarm: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchSnoozeAlarm', 'IoTEventsData', result, true, true, mock)
  },
  batchSnoozeAlarmAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchSnoozeAlarm', 'IoTEventsData', result, false, true, mock)
  },
  batchSnoozeAlarmThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchSnoozeAlarm', 'IoTEventsData', result, true, false, mock)
  },
  batchUpdateDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateDetector', 'IoTEventsData', result, true, true, mock)
  },
  batchUpdateDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateDetector', 'IoTEventsData', result, false, true, mock)
  },
  batchUpdateDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('batchUpdateDetector', 'IoTEventsData', result, true, false, mock)
  },
  describeAlarm: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAlarm', 'IoTEventsData', result, true, true, mock)
  },
  describeAlarmAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAlarm', 'IoTEventsData', result, false, true, mock)
  },
  describeAlarmThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeAlarm', 'IoTEventsData', result, true, false, mock)
  },
  describeDetector: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDetector', 'IoTEventsData', result, true, true, mock)
  },
  describeDetectorAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDetector', 'IoTEventsData', result, false, true, mock)
  },
  describeDetectorThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDetector', 'IoTEventsData', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoTEventsData', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoTEventsData', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'IoTEventsData', result, true, false, mock)
  },
  listAlarms: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlarms', 'IoTEventsData', result, true, true, mock)
  },
  listAlarmsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlarms', 'IoTEventsData', result, false, true, mock)
  },
  listAlarmsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listAlarms', 'IoTEventsData', result, true, false, mock)
  },
  listDetectors: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDetectors', 'IoTEventsData', result, true, true, mock)
  },
  listDetectorsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDetectors', 'IoTEventsData', result, false, true, mock)
  },
  listDetectorsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDetectors', 'IoTEventsData', result, true, false, mock)
  },
}
