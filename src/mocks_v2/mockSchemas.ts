
/**
* @description mocks_v2:mockSchemas.ts module is mocks for AWS-SDK V2
* 
* This mock module has generated from mock_generator.
* Generator Version: {v0.1.0}
* Author: hugtechio
*/

import {attachMock} from './common'
export const mockSchemas = {
  createDiscoverer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDiscoverer', 'Schemas', result, true, true, mock)
  },
  createDiscovererAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDiscoverer', 'Schemas', result, false, true, mock)
  },
  createDiscovererThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createDiscoverer', 'Schemas', result, true, false, mock)
  },
  createRegistry: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createRegistry', 'Schemas', result, true, true, mock)
  },
  createRegistryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createRegistry', 'Schemas', result, false, true, mock)
  },
  createRegistryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createRegistry', 'Schemas', result, true, false, mock)
  },
  createSchema: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSchema', 'Schemas', result, true, true, mock)
  },
  createSchemaAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSchema', 'Schemas', result, false, true, mock)
  },
  createSchemaThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('createSchema', 'Schemas', result, true, false, mock)
  },
  deleteDiscoverer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDiscoverer', 'Schemas', result, true, true, mock)
  },
  deleteDiscovererAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDiscoverer', 'Schemas', result, false, true, mock)
  },
  deleteDiscovererThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteDiscoverer', 'Schemas', result, true, false, mock)
  },
  deleteRegistry: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteRegistry', 'Schemas', result, true, true, mock)
  },
  deleteRegistryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteRegistry', 'Schemas', result, false, true, mock)
  },
  deleteRegistryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteRegistry', 'Schemas', result, true, false, mock)
  },
  deleteResourcePolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourcePolicy', 'Schemas', result, true, true, mock)
  },
  deleteResourcePolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourcePolicy', 'Schemas', result, false, true, mock)
  },
  deleteResourcePolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteResourcePolicy', 'Schemas', result, true, false, mock)
  },
  deleteSchema: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchema', 'Schemas', result, true, true, mock)
  },
  deleteSchemaAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchema', 'Schemas', result, false, true, mock)
  },
  deleteSchemaThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchema', 'Schemas', result, true, false, mock)
  },
  deleteSchemaVersion: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchemaVersion', 'Schemas', result, true, true, mock)
  },
  deleteSchemaVersionAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchemaVersion', 'Schemas', result, false, true, mock)
  },
  deleteSchemaVersionThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('deleteSchemaVersion', 'Schemas', result, true, false, mock)
  },
  describeCodeBinding: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCodeBinding', 'Schemas', result, true, true, mock)
  },
  describeCodeBindingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCodeBinding', 'Schemas', result, false, true, mock)
  },
  describeCodeBindingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeCodeBinding', 'Schemas', result, true, false, mock)
  },
  describeDiscoverer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDiscoverer', 'Schemas', result, true, true, mock)
  },
  describeDiscovererAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDiscoverer', 'Schemas', result, false, true, mock)
  },
  describeDiscovererThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeDiscoverer', 'Schemas', result, true, false, mock)
  },
  describeRegistry: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeRegistry', 'Schemas', result, true, true, mock)
  },
  describeRegistryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeRegistry', 'Schemas', result, false, true, mock)
  },
  describeRegistryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeRegistry', 'Schemas', result, true, false, mock)
  },
  describeSchema: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSchema', 'Schemas', result, true, true, mock)
  },
  describeSchemaAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSchema', 'Schemas', result, false, true, mock)
  },
  describeSchemaThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('describeSchema', 'Schemas', result, true, false, mock)
  },
  destroy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Schemas', result, true, true, mock)
  },
  destroyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Schemas', result, false, true, mock)
  },
  destroyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('destroy', 'Schemas', result, true, false, mock)
  },
  exportSchema: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('exportSchema', 'Schemas', result, true, true, mock)
  },
  exportSchemaAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('exportSchema', 'Schemas', result, false, true, mock)
  },
  exportSchemaThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('exportSchema', 'Schemas', result, true, false, mock)
  },
  getCodeBindingSource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCodeBindingSource', 'Schemas', result, true, true, mock)
  },
  getCodeBindingSourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCodeBindingSource', 'Schemas', result, false, true, mock)
  },
  getCodeBindingSourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getCodeBindingSource', 'Schemas', result, true, false, mock)
  },
  getDiscoveredSchema: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDiscoveredSchema', 'Schemas', result, true, true, mock)
  },
  getDiscoveredSchemaAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDiscoveredSchema', 'Schemas', result, false, true, mock)
  },
  getDiscoveredSchemaThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getDiscoveredSchema', 'Schemas', result, true, false, mock)
  },
  getResourcePolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourcePolicy', 'Schemas', result, true, true, mock)
  },
  getResourcePolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourcePolicy', 'Schemas', result, false, true, mock)
  },
  getResourcePolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('getResourcePolicy', 'Schemas', result, true, false, mock)
  },
  listDiscoverers: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDiscoverers', 'Schemas', result, true, true, mock)
  },
  listDiscoverersAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDiscoverers', 'Schemas', result, false, true, mock)
  },
  listDiscoverersThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listDiscoverers', 'Schemas', result, true, false, mock)
  },
  listRegistries: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRegistries', 'Schemas', result, true, true, mock)
  },
  listRegistriesAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRegistries', 'Schemas', result, false, true, mock)
  },
  listRegistriesThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listRegistries', 'Schemas', result, true, false, mock)
  },
  listSchemaVersions: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemaVersions', 'Schemas', result, true, true, mock)
  },
  listSchemaVersionsAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemaVersions', 'Schemas', result, false, true, mock)
  },
  listSchemaVersionsThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemaVersions', 'Schemas', result, true, false, mock)
  },
  listSchemas: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemas', 'Schemas', result, true, true, mock)
  },
  listSchemasAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemas', 'Schemas', result, false, true, mock)
  },
  listSchemasThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listSchemas', 'Schemas', result, true, false, mock)
  },
  listTagsForResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Schemas', result, true, true, mock)
  },
  listTagsForResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Schemas', result, false, true, mock)
  },
  listTagsForResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('listTagsForResource', 'Schemas', result, true, false, mock)
  },
  putCodeBinding: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putCodeBinding', 'Schemas', result, true, true, mock)
  },
  putCodeBindingAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putCodeBinding', 'Schemas', result, false, true, mock)
  },
  putCodeBindingThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putCodeBinding', 'Schemas', result, true, false, mock)
  },
  putResourcePolicy: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putResourcePolicy', 'Schemas', result, true, true, mock)
  },
  putResourcePolicyAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putResourcePolicy', 'Schemas', result, false, true, mock)
  },
  putResourcePolicyThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('putResourcePolicy', 'Schemas', result, true, false, mock)
  },
  searchSchemas: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchSchemas', 'Schemas', result, true, true, mock)
  },
  searchSchemasAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchSchemas', 'Schemas', result, false, true, mock)
  },
  searchSchemasThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('searchSchemas', 'Schemas', result, true, false, mock)
  },
  startDiscoverer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDiscoverer', 'Schemas', result, true, true, mock)
  },
  startDiscovererAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDiscoverer', 'Schemas', result, false, true, mock)
  },
  startDiscovererThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('startDiscoverer', 'Schemas', result, true, false, mock)
  },
  stopDiscoverer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDiscoverer', 'Schemas', result, true, true, mock)
  },
  stopDiscovererAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDiscoverer', 'Schemas', result, false, true, mock)
  },
  stopDiscovererThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('stopDiscoverer', 'Schemas', result, true, false, mock)
  },
  tagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Schemas', result, true, true, mock)
  },
  tagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Schemas', result, false, true, mock)
  },
  tagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('tagResource', 'Schemas', result, true, false, mock)
  },
  untagResource: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Schemas', result, true, true, mock)
  },
  untagResourceAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Schemas', result, false, true, mock)
  },
  untagResourceThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('untagResource', 'Schemas', result, true, false, mock)
  },
  updateDiscoverer: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDiscoverer', 'Schemas', result, true, true, mock)
  },
  updateDiscovererAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDiscoverer', 'Schemas', result, false, true, mock)
  },
  updateDiscovererThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateDiscoverer', 'Schemas', result, true, false, mock)
  },
  updateRegistry: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRegistry', 'Schemas', result, true, true, mock)
  },
  updateRegistryAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRegistry', 'Schemas', result, false, true, mock)
  },
  updateRegistryThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateRegistry', 'Schemas', result, true, false, mock)
  },
  updateSchema: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSchema', 'Schemas', result, true, true, mock)
  },
  updateSchemaAll: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSchema', 'Schemas', result, false, true, mock)
  },
  updateSchemaThrow: (result:any, mock?: jest.SpyInstance): jest.SpyInstance => {
    return attachMock('updateSchema', 'Schemas', result, true, false, mock)
  },
}
